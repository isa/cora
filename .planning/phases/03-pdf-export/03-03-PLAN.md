---
phase: 03-pdf-export
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - packages/cora/package.json
  - packages/cora/.npmrc
  - packages/cora/src/cli/output.ts
  - packages/cora/src/cli/paths.ts
  - packages/cora/src/cli/playwrightInstall.ts
  - packages/cora/src/renderer/renderToPDFHighQuality.ts
  - packages/cora/src/cli/commands/render.ts
  - packages/cora/tests/pdf/chromium-prompt.test.ts
  - packages/cora/tests/pdf/high-quality.test.ts
autonomous: true
requirements:
  - EXP-03
  - EXP-04
tags: [pdf, playwright, chromium, lazy-install, high-quality]

must_haves:
  truths:
    - "`cora render diagram.yaml -o out.pdf --quality=high --yes` produces a Chromium-rendered PDF on a clean machine after one-time lazy Chromium download"
    - "Chromium installs to `$HOME/.config/cora/browsers/` (Linux/mac) or `%LOCALAPPDATA%/cora/browsers/` (Windows) — NOT to `~/.cache/ms-playwright/`"
    - "Non-interactive run (no TTY OR --format=json OR CI=1) without --yes / CORA_AUTO_INSTALL=1 emits a CHROMIUM_NOT_INSTALLED JSON error and exits non-zero"
    - "No silent fallback to resvg when --quality=high fails (D-09)"
    - "Subsequent runs after first install skip the prompt (cache directory is the install marker)"
  artifacts:
    - path: "packages/cora/.npmrc"
      provides: "Disables Playwright postinstall Chromium download for `npm install cora` consumers"
      contains: "playwright_skip_browser_download=1"
    - path: "packages/cora/src/cli/paths.ts"
      provides: "CORA_CONFIG_DIR, CHROMIUM_DIR resolution (cross-platform)"
      exports: ["CORA_CONFIG_DIR", "CHROMIUM_DIR"]
    - path: "packages/cora/src/cli/output.ts"
      provides: "isNonInteractive + shouldAutoInstall predicates (extends existing module)"
      exports: ["isNonInteractive", "shouldAutoInstall"]
    - path: "packages/cora/src/cli/playwrightInstall.ts"
      provides: "chromiumInstalled() + installChromium() + promptUser()"
      exports: ["chromiumInstalled", "installChromium", "promptUser"]
    - path: "packages/cora/src/renderer/renderToPDFHighQuality.ts"
      provides: "renderToPDFHighQuality(svg, opts) → Promise<Buffer> using Playwright"
      exports: ["renderToPDFHighQuality"]
  key_links:
    - from: "packages/cora/src/cli/commands/render.ts"
      to: "packages/cora/src/cli/playwrightInstall.ts"
      via: "chromiumInstalled() + installChromium() gating"
      pattern: "chromiumInstalled\\(\\)"
    - from: "packages/cora/src/cli/commands/render.ts"
      to: "packages/cora/src/renderer/renderToPDFHighQuality.ts"
      via: "dynamic import only in --quality=high branch"
      pattern: "import\\(['\"].*renderToPDFHighQuality"
    - from: "packages/cora/src/cli/playwrightInstall.ts"
      to: "$HOME/.config/cora/browsers/"
      via: "PLAYWRIGHT_BROWSERS_PATH env var passed to spawned process"
      pattern: "PLAYWRIGHT_BROWSERS_PATH"
---

<objective>
Deliver the `--quality=high` vertical lane end-to-end: lazy Chromium download to Cora's cache dir, non-interactive predicate split with `CHROMIUM_NOT_INSTALLED` JSON error, TTY prompt for interactive download, and a Playwright-driven PDF renderer that produces high-fidelity output. Ship a `.npmrc` that blocks Playwright's default postinstall Chromium download so `npm install cora` stays fast (EXP-05 guarantee).

Purpose: After this plan, a user can opt into WYSIWYG-grade PDF via `--quality=high` and CI can run the lane non-interactively via `--yes` or `CORA_AUTO_INSTALL=1`. Default-path users (Plan 02) pay no cost — Playwright is dynamic-imported only when needed.

Output: Four new modules under `src/cli/` and `src/renderer/`, `.npmrc`, extended `render.ts` --quality branch (replaces Plan 02's fallthrough comment), two test files converted from RED stubs to green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-pdf-export/03-CONTEXT.md
@.planning/phases/03-pdf-export/03-RESEARCH.md
@.planning/phases/03-pdf-export/03-PATTERNS.md
@.planning/phases/03-pdf-export/03-VALIDATION.md
@.planning/phases/03-pdf-export/03-02-PLAN.md
@packages/cora/src/cli/index.ts
@packages/cora/src/cli/output.ts
@packages/cora/src/cli/commands/render.ts

<interfaces>
<!-- Existing predicates from output.ts (Phase 1) -->
- `isJsonOutput(opts)` returns true when `opts.format === 'json'` or `!process.stdout.isTTY`. Phase 3 adds `isNonInteractive(opts)` (broader — also includes `CI=1` env) and `shouldAutoInstall({yes})` (true when `yes === true` OR `CORA_AUTO_INSTALL=1`).

<!-- --yes flag registration (LOCKED, verified against packages/cora/src/cli/index.ts line 25-26) -->
- `--yes` is already registered GLOBALLY on `program` in `src/cli/index.ts` (Phase 1). Plan 03 MUST NOT re-register it on the render subcommand. Read it via `program.opts().yes` inside the render action — see Task 3 for the exact pattern.

<!-- Path resolution rule per CONTEXT and Pitfall 6 -->
- Linux/mac: hardcode `$HOME/.config/cora/` per PROJECT.md (do NOT use env-paths — it returns `~/Library/Application Support/cora-nodejs/` on macOS which contradicts PROJECT.md).
- Windows: use `%LOCALAPPDATA%/cora/` (fallback to env-paths cache).
- `CHROMIUM_DIR` = `<CORA_CONFIG_DIR>/browsers`.
- Override via `CORA_CONFIG_DIR` env var (tests use this for isolation).

<!-- extractSvgDimensions (provided by Plan 02 pageSize.ts) -->
- `extractSvgDimensions(svg: string): { width: number; height: number }` is exported from `packages/cora/src/renderer/pdf/pageSize.ts` (added in Plan 02 Task 1). Plan 03 Task 2 imports and uses it directly — no need to re-implement viewBox parsing.

<!-- renderToPDFHighQuality signature -->
- `async function renderToPDFHighQuality(svg: string, opts: { page?: PageName; margin?: number }): Promise<Buffer>` — sets `process.env.PLAYWRIGHT_BROWSERS_PATH = CHROMIUM_DIR`, launches headless chromium, wraps SVG in print-CSS HTML, calls `page.pdf()` with `preferCSSPageSize: true`, closes browser in `finally`.

<!-- CHROMIUM_NOT_INSTALLED JSON shape -->
- `{ code: 'CHROMIUM_NOT_INSTALLED', path: '/quality', message: '<actionable hint>', suggestion: '<example command>' }` — single-element array per existing JSON error contract (render.ts:135-147).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend output.ts + create paths.ts (predicate split + cross-platform paths)</name>
  <read_first>
    - packages/cora/src/cli/output.ts (entire file — existing isJsonOutput pattern)
    - .planning/PROJECT.md (User data paths section — `$HOME/.config/cora/`)
    - .planning/phases/03-pdf-export/03-RESEARCH.md (Code Examples §1 "Detecting non-interactive mode"; Pattern 4 lines 362-376; Pitfall 6)
    - .planning/phases/03-pdf-export/03-PATTERNS.md (paths.ts + output.ts MODIFY sections)
  </read_first>
  <behavior>
    output.ts (extension):
    - `isNonInteractive({format:'json'})` → true
    - `isNonInteractive()` with `process.stdout.isTTY=false` → true
    - `isNonInteractive()` with `CI=1` env → true
    - `isNonInteractive()` with TTY=true, no CI, no format=json → false
    - `shouldAutoInstall({yes:true})` → true
    - `shouldAutoInstall({})` with `CORA_AUTO_INSTALL=1` → true
    - `shouldAutoInstall({})` with no env, no flag → false

    paths.ts:
    - On Linux/mac (HOME=/tmp/coratest), `CORA_CONFIG_DIR` resolves to `/tmp/coratest/.config/cora`.
    - With `CORA_CONFIG_DIR=/custom/dir` env set, the constant respects it.
    - `CHROMIUM_DIR` is `<CORA_CONFIG_DIR>/browsers`.
  </behavior>
  <action>
First convert the relevant assertions in `packages/cora/tests/pdf/chromium-prompt.test.ts` (Wave 0 stub) into real tests for `isNonInteractive`, `shouldAutoInstall`, and the path resolution. Use vitest's `vi.stubEnv` + `vi.stubGlobal` to manipulate `process.env`, `process.stdout.isTTY`, `process.platform`. For paths.ts tests, dynamically `await import()` the module fresh after stubbing `HOME` / `CORA_CONFIG_DIR` (since the module computes constants at load time).

Then implement:

`packages/cora/src/cli/output.ts` — append (do NOT modify existing exports):
- `export function isNonInteractive(opts: OutputOptions = {}): boolean` — returns true when `opts.format === 'json'` OR `process.env.CI === '1' || process.env.CI === 'true'` OR `!process.stdout.isTTY`. See RESEARCH.md Code Examples §1.
- `export function shouldAutoInstall(flags: { yes?: boolean } = {}): boolean` — returns `flags.yes === true || process.env.CORA_AUTO_INSTALL === '1'`.

`packages/cora/src/cli/paths.ts` (NEW) — per RESEARCH.md Pattern 4 lines 362-376 AND Pitfall 6:
- `export const CORA_CONFIG_DIR = process.env.CORA_CONFIG_DIR ?? (process.platform === 'win32' ? join(process.env.LOCALAPPDATA ?? <fallback>, 'cora') : join(process.env.HOME ?? '~', '.config', 'cora'));`
- For Windows fallback when `LOCALAPPDATA` is unset, use `env-paths('cora', { suffix: '' }).cache` — install `env-paths` via `bun add env-paths` (runtime dep). Do NOT use env-paths on Linux/mac per Pitfall 6.
- `export const CHROMIUM_DIR = join(CORA_CONFIG_DIR, 'browsers');`
- Module-shape: top-level const exports, no class, no default. Mirror `output.ts` style.

Run the converted tests — they MUST pass.
  </action>
  <acceptance_criteria>
- `packages/cora/src/cli/output.ts` exports `isNonInteractive` and `shouldAutoInstall` (in addition to existing exports).
- `packages/cora/src/cli/paths.ts` exists with `CORA_CONFIG_DIR` and `CHROMIUM_DIR` const exports.
- `env-paths` is in `packages/cora/package.json` `dependencies`.
- On `process.platform === 'linux'` with `HOME=/tmp/foo` (and `CORA_CONFIG_DIR` unset), `CORA_CONFIG_DIR === '/tmp/foo/.config/cora'`.
- With `CORA_CONFIG_DIR=/custom` set, the constant equals `/custom`.
- `CHROMIUM_DIR` equals `<CORA_CONFIG_DIR>/browsers`.
- `bun x vitest run tests/pdf/chromium-prompt.test.ts` — the predicate + path test cases (not yet the full CLI integration cases) pass.
- All existing tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun run typecheck && bun x vitest run tests/pdf/chromium-prompt.test.ts -t "predicate|path" && bun x vitest run tests/pdf/ && bun run test:golden</automated>
  </verify>
  <done>Predicate split + cross-platform path resolution complete; output.ts extension preserves all existing exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: playwrightInstall.ts (chromiumInstalled, installChromium, promptUser) + renderToPDFHighQuality.ts</name>
  <read_first>
    - packages/cora/src/cli/paths.ts (Task 1 output — CHROMIUM_DIR)
    - packages/cora/src/renderer/pdf/pageSize.ts (Plan 02 Task 1 output — exports extractSvgDimensions)
    - packages/cora/tests/render-golden.mjs (lines 5, 60-62 — execFileSync pattern, only in-repo child_process call site)
    - .planning/phases/03-pdf-export/03-RESEARCH.md (Pattern 4 entire section lines 348-419; Assumption A2, A5, A6, A7)
    - .planning/phases/03-pdf-export/03-PATTERNS.md (playwrightInstall.ts + renderToPDFHighQuality.ts sections; "No Analog Found" notes)
    - packages/cora/tests/pdf/high-quality.test.ts (Wave 0 gated stub — convert to real test)
  </read_first>
  <behavior>
    playwrightInstall.ts:
    - `chromiumInstalled()` returns true when `CHROMIUM_DIR` exists AND `readdirSync(CHROMIUM_DIR).length > 0`; false otherwise.
    - `installChromium({quiet})` spawns `npx playwright install chromium` with `PLAYWRIGHT_BROWSERS_PATH=<CHROMIUM_DIR>`; resolves when child exits 0; rejects with non-zero.
    - The spawned process receives an explicit `env` object (allowlist for V14 ASVS — must include at minimum PATH, HOME, LOCALAPPDATA, PLAYWRIGHT_BROWSERS_PATH; do NOT spread `process.env` blindly — pass only the needed vars per security threat T-03-02).
    - `promptUser(message)` returns Promise<boolean>; uses `node:readline/promises` against `process.stdin`/`process.stdout`; accepts y/Y/yes as true, everything else false.

    renderToPDFHighQuality.ts:
    - Top-level `import { chromium } from 'playwright'` — load cost is paid only when the consumer dynamic-imports this module.
    - `renderToPDFHighQuality(svg, opts)` sets `process.env.PLAYWRIGHT_BROWSERS_PATH = CHROMIUM_DIR` (if not already set), launches `chromium.launch({ headless: true })`, creates a page, `setContent(<print-CSS-wrapped HTML containing the SVG>)` with `waitUntil: 'networkidle'`, calls `page.pdf({ width, height, printBackground: true, preferCSSPageSize: true })`, closes browser in `finally`.
    - The HTML harness MUST include `html, body { margin: 0; padding: 0 }` and `@page { size: <w>pt <h>pt; margin: 0 }` (Pitfall 5).
    - Returns `Buffer` (or `Uint8Array` — match pdf-lib's return type to keep render.ts symmetric).
  </behavior>
  <action>
First convert assertions in `packages/cora/tests/pdf/chromium-prompt.test.ts` for `chromiumInstalled` and the install-spawn pattern. Use a temp dir via `mkdtempSync` for `CORA_CONFIG_DIR`. Test the spawn by mocking `node:child_process` with `vi.mock` to capture invocation args without actually spawning Playwright (the integration test in `high-quality.test.ts` exercises the real spawn under the gated `CORA_TEST_PLAYWRIGHT=1` lane).

Convert `packages/cora/tests/pdf/high-quality.test.ts` (gated by `CORA_TEST_PLAYWRIGHT=1`) into a real integration test: build the package, set `CORA_CONFIG_DIR` to a temp dir, run `installChromium({quiet:true})`, assert `chromiumInstalled() === true`, then call `renderToPDFHighQuality(svg, {page:'a4'})` against a small fixture SVG, assert the return is a Buffer starting with `%PDF`, and that pdf-parse extracts the diagram's text.

Then implement:

`packages/cora/src/cli/playwrightInstall.ts` — per RESEARCH.md Pattern 4 lines 378-391:
- Imports: `node:child_process` (`spawn`), `node:fs` (`existsSync`, `readdirSync`), `node:readline/promises`, `./paths.js`.
- `export function chromiumInstalled(): boolean` — implementation per RESEARCH lines 374-376.
- `export async function installChromium(opts: { quiet: boolean }): Promise<void>` — spawn `npx playwright install chromium` with explicit allowlisted env: `{ PATH: process.env.PATH ?? '', HOME: process.env.HOME ?? '', LOCALAPPDATA: process.env.LOCALAPPDATA ?? '', PLAYWRIGHT_BROWSERS_PATH: CHROMIUM_DIR }`. stdio: `opts.quiet ? 'pipe' : 'inherit'`. Resolve on exit 0; reject otherwise.
- `export async function promptUser(message: string): Promise<boolean>` — uses `readline/promises`'s `createInterface({input: process.stdin, output: process.stdout})`, asks `${message} [y/N] `, returns `/^y(es)?$/i.test(answer.trim())`. Close the interface in `finally`.

`packages/cora/src/renderer/renderToPDFHighQuality.ts` — per RESEARCH.md Pattern 4 lines 394-419:
- Top-level `import { chromium } from 'playwright'`.
- `import { CHROMIUM_DIR } from '../cli/paths.js'`.
- `import { computePageGeometry, extractSvgDimensions, type PageName } from './pdf/pageSize.js'`.
- `export async function renderToPDFHighQuality(svg: string, opts: { page?: PageName; margin?: number }): Promise<Buffer>` — full body per the sketch. To obtain the diagram width/height needed by `computePageGeometry`, call `extractSvgDimensions(svg)` (provided by Plan 02 pageSize.ts) — do NOT re-implement viewBox parsing here. Pass the returned `{ width, height }` directly into `computePageGeometry`.

Add `playwright` to `packages/cora/package.json` `dependencies` via `bun add playwright` (NOT devDependencies — runtime). Also `bun add` already covered `env-paths` in Task 1. Verify install does NOT trigger Chromium download by checking `~/.cache/ms-playwright/` does not exist after `bun add playwright`. If it does, the .npmrc from Task 3 is the fix.

Run tests — predicate + spawn-mock cases pass without `CORA_TEST_PLAYWRIGHT=1`. With `CORA_TEST_PLAYWRIGHT=1`, the gated `high-quality.test.ts` runs and exercises the real Chromium download + render.
  </action>
  <acceptance_criteria>
- `packages/cora/src/cli/playwrightInstall.ts` exports `chromiumInstalled`, `installChromium`, `promptUser`.
- `chromiumInstalled()` returns false when `CHROMIUM_DIR` does not exist; true when it exists with non-empty contents.
- `installChromium` invokes `spawn` with `args = ['playwright', 'install', 'chromium']` and `env.PLAYWRIGHT_BROWSERS_PATH === CHROMIUM_DIR` (assert via vi.mock of `node:child_process`).
- `installChromium` passes an explicit env object — does NOT spread `...process.env` (verify by inspecting the env object passed to spawn; only the allowlisted keys appear).
- `packages/cora/src/renderer/renderToPDFHighQuality.ts` exports `renderToPDFHighQuality`.
- `renderToPDFHighQuality.ts` imports `extractSvgDimensions` from `./pdf/pageSize.js` (verify: `grep -q "extractSvgDimensions" src/renderer/renderToPDFHighQuality.ts`).
- `playwright` is in `packages/cora/package.json` `dependencies`.
- Under `CORA_TEST_PLAYWRIGHT=1` with `CORA_CONFIG_DIR` set to a temp dir, the gated test passes: install → render → assert `%PDF` header + text selectable.
- The default vitest run (without `CORA_TEST_PLAYWRIGHT=1`) does NOT download Chromium (skips the gated lane).
- `bun run typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun run typecheck && bun run build && bun x vitest run tests/pdf/ && grep -q '"playwright"' package.json && grep -q 'allowlist\|PLAYWRIGHT_BROWSERS_PATH' src/cli/playwrightInstall.ts && grep -L '\.\.\.process\.env' src/cli/playwrightInstall.ts && grep -q "extractSvgDimensions" src/renderer/renderToPDFHighQuality.ts</automated>
  </verify>
  <done>Install plumbing + Playwright renderer in place; mocked spawn tests green; gated integration lane works under CORA_TEST_PLAYWRIGHT=1; renderToPDFHighQuality imports extractSvgDimensions from Plan 02's pageSize.ts (no duplicated viewBox parser).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire --quality=high branch in render.ts + ship .npmrc</name>
  <read_first>
    - packages/cora/src/cli/commands/render.ts (current state after Plan 02 — the `Plan 03 wires high-quality lane here` comment marks the insertion site)
    - packages/cora/src/cli/index.ts (lines 25-26 — `--yes` is registered on `program` globally; read via `program.opts().yes`)
    - packages/cora/src/cli/playwrightInstall.ts (Task 2 output)
    - packages/cora/src/cli/output.ts (Task 1 output — isNonInteractive, shouldAutoInstall)
    - packages/cora/src/cli/paths.ts (Task 1 output — CHROMIUM_DIR)
    - .planning/phases/03-pdf-export/03-CONTEXT.md (Decisions D-06 through D-10)
    - .planning/phases/03-pdf-export/03-RESEARCH.md (Code Examples §2 "CHROMIUM_NOT_INSTALLED JSON error" lines 538-568; Pitfall 4)
    - .planning/phases/03-pdf-export/03-PATTERNS.md (render.ts MODIFY section — error-shape pattern)
  </read_first>
  <action>
Find the comment `Plan 03 wires high-quality lane here` in `packages/cora/src/cli/commands/render.ts` (left by Plan 02 Task 3). Replace the surrounding fallthrough logic with the high-quality branch per CONTEXT D-06 through D-10 and RESEARCH.md Code Examples §2.

Imports to add at the top of render.ts:
- `import { isNonInteractive, shouldAutoInstall } from '../output.js';`
- `import { chromiumInstalled, installChromium, promptUser } from '../playwrightInstall.js';`
- `import { CHROMIUM_DIR } from '../paths.js';`

**`--yes` flag handling (LOCKED):** Verified against `packages/cora/src/cli/index.ts` line 25-26 — `--yes` is already registered on the global `program` from Phase 1. Do NOT register `.option('-y, --yes', ...)` on the render subcommand. Read it inside the render action via `program.opts().yes` (Commander exposes the program instance; if `program` is not in scope inside `commands/render.ts`, import it from `../index.js` or accept it as a parameter — match how other Phase 1 commands access global opts; if no precedent exists, accept `program` as the constructor parameter and store `program.opts().yes` into a local `yes` const at the top of the action).

Inside the PDF format branch (currently the `else if (format === 'pdf')` Plan 02 added), refactor the body to:

```
const yes = program.opts().yes === true; // resolved once at the top of the PDF branch
if (options.quality === 'high') {
  // Branch A: high-quality lane
  if (!chromiumInstalled()) {
    if (shouldAutoInstall({ yes })) {
      await installChromium({ quiet: isNonInteractive({ format: options.format }) });
    } else if (isNonInteractive({ format: options.format })) {
      const err = { code: 'CHROMIUM_NOT_INSTALLED', path: '/quality', message: '<text>', suggestion: '<text>' };
      // emit per isJsonOutput / pc.red pattern at lines 132-150
      process.exitCode = 1; return;
    } else {
      const confirmed = await promptUser(`Cora needs to download Chromium (~170MB) to ${CHROMIUM_DIR}. Proceed?`);
      if (!confirmed) { process.exitCode = 1; return; }
      await installChromium({ quiet: false });
    }
  }
  const { renderToPDFHighQuality } = await import('../../renderer/renderToPDFHighQuality.js');
  const pdfBytes = await renderToPDFHighQuality(svg, { page: options.page as PageName | undefined });
  writeFileSync(options.output, pdfBytes);
} else {
  // Branch B: default resvg + pdf-lib lane (Plan 02 implementation, unchanged)
  const pdfBytes = await renderToPDF(layouted, svg, { page: options.page as PageName | undefined, ciMode: process.env.CI === '1' || process.env.CI === 'true' });
  writeFileSync(options.output, Buffer.from(pdfBytes));
}
```

The error message text per D-08: `'Chromium is required for --quality=high but is not installed. Pass --yes, set CORA_AUTO_INSTALL=1, or run interactively to accept the prompt.'`
Suggestion text: `` `cora render … --quality=high --yes  (downloads Chromium to ${CHROMIUM_DIR})` ``

CRITICAL: the `await import('../../renderer/renderToPDFHighQuality.js')` MUST be a dynamic import (not a top-level static import) so the resvg-only path doesn't pay the Playwright module-load cost. Verify with `grep -L "^import.*renderToPDFHighQuality" src/cli/commands/render.ts` (it should NOT appear at top of file).

Convert remaining stub cases in `packages/cora/tests/pdf/chromium-prompt.test.ts` into real integration tests using `execFileSync(process.execPath, [cliPath, 'render', input, '-o', output, '--quality=high', '--format=json'])` with `env.CORA_CONFIG_DIR=<empty temp dir>`. Assert:
- Exit code != 0
- stdout JSON contains `"code": "CHROMIUM_NOT_INSTALLED"` and `"path": "/quality"`
- Also test `--yes` triggers install: mock by setting `PATH` to a stub directory containing a fake `npx` shim that just creates `<CHROMIUM_DIR>/chromium-test/` and exits 0; assert the CLI proceeds to render (the render itself will fail because the fake chromium isn't real, but the install step should have been attempted — assert via filesystem side effect).

Create `packages/cora/.npmrc` with the single line:
```
playwright_skip_browser_download=1
```
(npm reads this as the env var `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` when running scripts — disables Playwright's postinstall download per RESEARCH.md Pitfall 4.) Ensure `.npmrc` is included in the published package by checking `packages/cora/package.json` `files` array: `["dist"]` currently — extend to `["dist", ".npmrc"]`.

Also add to `package.json` a `postinstall` script (per RESEARCH lines 67-71):
```
"postinstall": "echo \"cora: playwright Chromium downloads on first --quality=high use; see AGENTS.md\""
```
This is informational only — actual download is gated by `.npmrc`.
  </action>
  <acceptance_criteria>
- The string `Plan 03 wires high-quality lane here` no longer appears in `render.ts` (replaced with real logic).
- `render.ts` does NOT contain a top-level `import` of `renderToPDFHighQuality` — only a `await import('../../renderer/renderToPDFHighQuality.js')` inside the high branch.
- `render.ts` does NOT register `.option('-y, --yes', ...)` (verify: `grep -E "\\.option\\([^)]*--yes" src/cli/commands/render.ts | wc -l` returns `0`). The `--yes` value is read via `program.opts().yes`.
- `cora render examples/valid/box-arrows.yaml -o /tmp/out.pdf --quality=high --format=json` with empty `CORA_CONFIG_DIR=/tmp/empty` exits non-zero and stdout JSON contains `"CHROMIUM_NOT_INSTALLED"` and `"/quality"`.
- The error JSON shape matches the existing `[{code,path,message,suggestion}]` array convention from `render.ts` line 132-147.
- `cora --help` (top-level) shows `--yes` (already registered globally; not on the subcommand).
- `packages/cora/.npmrc` exists and contains `playwright_skip_browser_download=1`.
- `packages/cora/package.json` `files` array includes `.npmrc` (otherwise npm pack will not ship it).
- `packages/cora/package.json` has a `postinstall` script.
- Under `CORA_TEST_PLAYWRIGHT=1`, `bun x vitest run tests/pdf/high-quality.test.ts` exercises the full CLI lane: install + render via `cora render … --quality=high --yes` produces a valid PDF.
- `bun x vitest run tests/pdf/` (default lane) passes.
- `bun run test:golden` still passes.
- No silent fallback: if `renderToPDFHighQuality` throws, the CLI exits non-zero (NOT a fallback to default resvg path) — verify by adding a test that mocks the dynamic import to throw, asserts exit code != 0.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun run build && bun run typecheck && bun x vitest run tests/pdf/ && bun run test:golden && grep -L "^import.*renderToPDFHighQuality" src/cli/commands/render.ts && grep -q "await import.*renderToPDFHighQuality" src/cli/commands/render.ts && test "$(grep -E "\\.option\\([^)]*--yes" src/cli/commands/render.ts | wc -l | tr -d ' ')" = "0" && test -f .npmrc && grep -q 'playwright_skip_browser_download=1' .npmrc && grep -q '"\.npmrc"' package.json && grep -q '"postinstall"' package.json && CORA_CONFIG_DIR=/tmp/cora-empty-$$ rm -rf /tmp/cora-empty-$$ && CORA_CONFIG_DIR=/tmp/cora-empty-$$ node dist/cli.js render ../../examples/valid/box-arrows.yaml -o /tmp/out-hq.pdf --quality=high --format=json 2>&1 | grep -q CHROMIUM_NOT_INSTALLED</automated>
  </verify>
  <done>--quality=high lane works end-to-end: lazy install + non-interactive JSON error + interactive prompt + Playwright PDF; .npmrc blocks postinstall Chromium download; --yes is read from global program.opts() (not re-registered on subcommand).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `npx playwright install chromium` child process | Spawning external binary; env vars cross the boundary |
| Chromium download from playwright.azureedge.net | Network → local disk; relies on Playwright's signature verification |
| User TTY prompt → install decision | User-supplied confirmation flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Elevation | Child process env inheritance (NODE_OPTIONS, npm_config_*) | mitigate | `spawn` receives an explicit allowlisted env object: `{ PATH, HOME, LOCALAPPDATA, PLAYWRIGHT_BROWSERS_PATH }` — does NOT spread `process.env` (Task 2 acceptance criteria enforce this) |
| T-03-03 | Tampering | Chromium binary download MITM | accept | Inherit Playwright's signed-download mechanism (azureedge.net HTTPS + SHA checks); we explicitly avoid hand-rolling a downloader (RESEARCH.md Don't Hand-Roll table) |
| T-03-04 | Denial of service | `npm install cora` downloads 170MB Chromium unexpectedly | mitigate | `.npmrc` ships `playwright_skip_browser_download=1`; Plan 04 clean-install smoke test asserts no `~/.cache/ms-playwright` after install |
| T-03-05 | Spoofing | Silent fallback to resvg masks --quality=high failure | mitigate | D-09: hard exit on Playwright errors; explicit `throw`/exit-non-zero in render.ts; no try/catch swallow |
| T-03-SC | Tampering | `bun add playwright env-paths` | mitigate | playwright is Microsoft-maintained (~10M weekly downloads); env-paths is sindresorhus (~80M weekly). Both [ASSUMED legitimate] per 03-RESEARCH Package Legitimacy Audit. **Checkpoint:** If slopcheck is available in execution environment, run it before `bun add`. Otherwise proceed (very low risk). |
</threat_model>

<verification>
- Default-path users unaffected: `cora render diagram.yaml -o /tmp/out.pdf` (no --quality) still works without Playwright loaded (verify by checking that `node --inspect-brk dist/cli.js render … -o /tmp/out.pdf` does NOT show `playwright` in loaded modules)
- `cora render … -o out.pdf --quality=high --yes` with empty `CORA_CONFIG_DIR` triggers install, then renders
- `cora render … -o out.pdf --quality=high --format=json` (non-interactive, no --yes) emits CHROMIUM_NOT_INSTALLED JSON and exits non-zero
- `npm install cora` (smoke test in Plan 04) does NOT download Chromium
- No silent fallback: forcing renderToPDFHighQuality to throw causes the CLI to exit non-zero (NOT to retry with default path)
</verification>

<success_criteria>
1. `--quality=high` Playwright lane works on a clean machine after one lazy Chromium download (CONTEXT D-06)
2. Non-interactive + missing Chromium + no auto-install flags → `CHROMIUM_NOT_INSTALLED` JSON error, non-zero exit (CONTEXT D-07, D-08)
3. `--yes` and `CORA_AUTO_INSTALL=1` enable non-interactive install for CI (CONTEXT D-07; requirement EXP-04)
4. Interactive TTY prompts once; subsequent runs skip the prompt (CONTEXT D-10)
5. No silent fallback to default resvg on high-quality failure (CONTEXT D-09)
6. Chromium installs to `$HOME/.config/cora/browsers/` (NOT `~/.cache/ms-playwright/`)
7. `.npmrc` prevents Playwright postinstall from downloading Chromium (EXP-05 — Plan 04 smoke confirms)
8. Default-path users pay zero Playwright load cost (dynamic import gating)
</success_criteria>

<output>
Create `.planning/phases/03-pdf-export/03-03-SUMMARY.md` when done.
</output>
</content>
</invoke>