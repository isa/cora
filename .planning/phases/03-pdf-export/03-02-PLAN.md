---
phase: 03-pdf-export
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/cora/package.json
  - packages/cora/src/renderer/pdf/coords.ts
  - packages/cora/src/renderer/pdf/pageSize.ts
  - packages/cora/src/renderer/pdf/resvgCapture.ts
  - packages/cora/src/renderer/pdf/textOverlay.ts
  - packages/cora/src/renderer/renderToPDF.ts
  - packages/cora/src/cli/commands/render.ts
  - packages/cora/tests/pdf/page-size.test.ts
  - packages/cora/tests/pdf/render-pdf.test.ts
  - packages/cora/tests/pdf/resvg-warning.test.ts
autonomous: true
requirements:
  - EXP-02
tags: [pdf, resvg, pdf-lib, default-path]

must_haves:
  truths:
    - "`cora render examples/valid/box-arrows.yaml -o /tmp/out.pdf` produces a valid PDF on a clean machine"
    - "Opening the PDF in a reader shows selectable text matching every node label"
    - "Text positions in the PDF match the SVG output within 1pt for the same diagram"
    - "Default page is fit-to-content (bbox + 2*margin); `--page=a4` scales-to-fit a single page"
    - "When CI=1 is set, a resvg `No match for font-family` warning forces non-zero exit"
  artifacts:
    - path: "packages/cora/src/renderer/pdf/coords.ts"
      provides: "svgToPdf(x, y, pageH, scale, offsetX, offsetY) Y-flip helper"
      exports: ["svgToPdf"]
    - path: "packages/cora/src/renderer/pdf/pageSize.ts"
      provides: "PAGE_SIZES table; computePageGeometry; scaleSvgToPage; extractSvgDimensions"
      exports: ["PAGE_SIZES", "computePageGeometry", "scaleSvgToPage", "extractSvgDimensions", "PageName"]
    - path: "packages/cora/src/renderer/pdf/resvgCapture.ts"
      provides: "rasteriseWithWarningCapture wraps Resvg, returns {png, warnings}"
      exports: ["rasteriseWithWarningCapture"]
    - path: "packages/cora/src/renderer/pdf/textOverlay.ts"
      provides: "buildTextOverlay(layouted) → TextDraw[]; pure data, zero pdf-lib imports"
      exports: ["buildTextOverlay", "TextDraw"]
    - path: "packages/cora/src/renderer/renderToPDF.ts"
      provides: "renderToPDF(layouted, svg, options) → Promise<Uint8Array>"
      exports: ["renderToPDF", "RenderToPDFOptions"]
  key_links:
    - from: "packages/cora/src/cli/commands/render.ts"
      to: "packages/cora/src/renderer/renderToPDF.ts"
      via: "format === 'pdf' branch in render command"
      pattern: "renderToPDF\\("
    - from: "packages/cora/src/renderer/renderToPDF.ts"
      to: "packages/cora/src/renderer/pdf/textOverlay.ts"
      via: "buildTextOverlay(layouted)"
      pattern: "buildTextOverlay"
    - from: "packages/cora/src/renderer/renderToPDF.ts"
      to: "packages/cora/src/renderer/assets/fonts.ts"
      via: "pdfLibFontBuffers()"
      pattern: "pdfLibFontBuffers"
---

<objective>
Deliver the default PDF render path end-to-end: `cora render diagram.yaml -o out.pdf` produces a valid, selectable-text PDF using resvg + pdf-lib, driven from the LayoutedDiagram IR (NOT by re-parsing SVG text). Add `--page=<size>` flag with fit-to-content default + scale-to-fit fallback. Wire the resvg stderr-warning capture so D-11 CI failure works.

Purpose: This is the Phase 3 walking slice. After this plan, a user can render any v1 diagram kind to PDF on a clean machine with zero browser deps (EXP-02 + EXP-05 default-path guarantee).

Output: Five new renderer modules under `renderer/pdf/`, one new `renderToPDF.ts` orchestrator, extended `render.ts` CLI dispatch, three test files converted from RED stubs to green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pdf-export/03-CONTEXT.md
@.planning/phases/03-pdf-export/03-RESEARCH.md
@.planning/phases/03-pdf-export/03-PATTERNS.md
@.planning/phases/03-pdf-export/03-VALIDATION.md
@.planning/phases/03-pdf-export/03-01-PLAN.md
@packages/cora/src/renderer/renderToPNG.ts
@packages/cora/src/cli/commands/render.ts

<interfaces>
<!-- LayoutedDiagram IR is the source of truth for text positions per Pattern 1. -->
<!-- Read packages/cora/src/renderer/layout-ir.ts (or wherever LayoutedDiagram is exported) before implementing textOverlay.ts. -->

From `packages/cora/src/renderer/Diagram.tsx`, `edges/EdgeLabel.tsx`, and `nodes/*.tsx`, the IR exposes:
- `layouted.nodes[i]`: `{ id, label, x, y, measuredWidth, measuredHeight }` — node label centered at (x + w/2, y + h/2)
- `layouted.edges[i]`: `{ id, label?, labelX?, labelY?, points }` — edge label only if `label && labelX != null && labelY != null`
- `layouted.groups[i]`: `{ id, label, x, y, width, height }` — group title top-centered
- `layouted.theme.nodeLabel`: `{ fontSize, fontWeight, fill, fontFamily }`
- `layouted.theme.edgeLabel`: same shape
- `layouted.width`, `layouted.height`: total diagram bbox in SVG coords

If any of these accessors differ from the actual layout-ir.ts, prefer the actual IR — the source file wins over this summary.

Existing `outputFormat()` (render.ts:24-35) returns `'svg' | 'png'` — extend to `'svg' | 'png' | 'pdf'`.
Existing JSON-error branch in render.ts:130-152 is the template for the new error code if PDF render fails.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pure helpers — coords.ts, pageSize.ts, resvgCapture.ts</name>
  <read_first>
    - packages/cora/src/renderer/renderToPNG.ts (lines 32-52 scaleSvgDimensions; lines 90-103 Resvg invocation)
    - packages/cora/src/renderer/viewBox.ts (sibling pure geometry helper pattern)
    - .planning/phases/03-pdf-export/03-RESEARCH.md (Pattern 2 lines 285-323; Pattern 3 lines 334-346; Pitfall 2 lines 466-484)
    - .planning/phases/03-pdf-export/03-PATTERNS.md (sections for pageSize.ts, coords.ts, resvgCapture.ts)
    - packages/cora/tests/pdf/page-size.test.ts (Wave 0 stub to convert)
    - packages/cora/tests/pdf/resvg-warning.test.ts (Wave 0 stub to convert)
  </read_first>
  <behavior>
    coords.ts:
    - `svgToPdf(0, 0, 100, 1, 0, 0)` → `{x: 0, y: 100}`
    - `svgToPdf(10, 20, 100, 2, 5, 5)` → `{x: 5 + 10*2, y: 100 - (5 + 20*2)} = {x: 25, y: 55}`
    pageSize.ts:
    - `PAGE_SIZES['a4-portrait']` → `[595.28, 841.89]` (within 0.01)
    - `PAGE_SIZES['letter']` → `[792, 612]`
    - `computePageGeometry({width:100,height:100}, {})` (no page set, default margin 24) → `{pageW:148, pageH:148, scale:1, offsetX:24, offsetY:24}`
    - `computePageGeometry({width:2000,height:1000}, {page:'a4'})` → scale < 1, diagram fits within usable area, offsets re-center diagram
    - `scaleSvgToPage('<svg viewBox="0 0 100 50">…</svg>', 2)` returns SVG with `width="200" height="100"` injected (mirror renderToPNG.ts:33-52 logic)
    - `extractSvgDimensions('<svg viewBox="0 0 200 100">…</svg>')` returns `{ width: 200, height: 100 }`. Also handles explicit `width="200" height="100"` attributes when viewBox is absent.
    resvgCapture.ts:
    - When the SVG references an unknown font-family, the returned `warnings` array is non-empty and contains a string matching `/font-family/i`.
    - When the SVG uses 'Noto Sans' (bundled), `warnings` is empty.
    - `png` is a non-empty Buffer in both cases.
  </behavior>
  <action>
Convert `packages/cora/tests/pdf/page-size.test.ts` and `packages/cora/tests/pdf/resvg-warning.test.ts` from `.skip`/`.todo` stubs into real `describe`/`it` tests covering the behaviors above. Run tests — they MUST fail (RED) because the helpers don't exist yet.

Then implement three files:

`packages/cora/src/renderer/pdf/coords.ts` — single exported function `svgToPdf(svgX, svgY, pageH, scale, offsetX, offsetY): {x, y}` per RESEARCH.md Pattern 3 (lines 334-346). Pure, no imports beyond types.

`packages/cora/src/renderer/pdf/pageSize.ts` — exports: `PageName` type (`'a4' | 'letter' | 'a4-portrait' | 'letter-portrait'`), `PAGE_SIZES` const record per RESEARCH.md Pattern 2 lines 286-294 (verified against ISO 216 / ANSI Y14.1), `computePageGeometry(layouted: {width:number; height:number}, opts: {page?:PageName; margin?:number}): {pageW, pageH, scale, offsetX, offsetY}` per RESEARCH.md lines 296-323, `scaleSvgToPage(svg: string, scale: number): string` (lift `scaleSvgDimensions` from renderToPNG.ts:33-52 verbatim, renamed), AND `extractSvgDimensions(svg: string): { width: number; height: number }` — parse the `viewBox` attribute first (`<svg[^>]*viewBox="0 0 ([\d.]+) ([\d.]+)"`), fall back to explicit `width`/`height` attrs; throw a descriptive Error if neither is present. Plan 03 Task 2 imports this from `./pdf/pageSize.js`.

`packages/cora/src/renderer/pdf/resvgCapture.ts` — single exported function `rasteriseWithWarningCapture(svg: string, opts: ResvgRenderOptions): {png: Buffer; warnings: string[]}` per RESEARCH.md Pitfall 2 lines 466-484. The stderr-capture pattern: replace `process.stderr.write`, run `new Resvg(svg, {...opts, logLevel:'warn'}).render().asPng()`, collect chunks matching `/No match for font-family|warn/i`, restore stderr in `finally`. Returns warnings array (empty when no warnings).

Re-run the converted tests — they MUST now pass (GREEN). Default margin: 24pt (per CONTEXT D-01 discretion suggestion).
  </action>
  <acceptance_criteria>
- `packages/cora/src/renderer/pdf/coords.ts` exports `svgToPdf` with the formula `{x: offsetX + svgX*scale, y: pageH - (offsetY + svgY*scale)}`.
- `packages/cora/src/renderer/pdf/pageSize.ts` exports `PAGE_SIZES`, `computePageGeometry`, `scaleSvgToPage`, `extractSvgDimensions`, and `PageName` (verify via `grep -E "^export (function|const|type)" src/renderer/pdf/pageSize.ts | grep -c .` ≥ 5).
- `PAGE_SIZES` has all four keys: `a4`, `letter`, `a4-portrait`, `letter-portrait`. A4 portrait = `[595.28, 841.89]`.
- `computePageGeometry` returns scale = 1 when no `page` opt is set; returns scale < 1 when diagram larger than usable area; centers offsets `(pageW - scaledW)/2`.
- `scaleSvgToPage` returns input SVG with `width`/`height` attributes set to `viewBoxWidth * scale` / `viewBoxHeight * scale` (mirror renderToPNG behavior).
- `extractSvgDimensions` returns `{width, height}` from viewBox; falls back to width/height attrs; throws on malformed input.
- `rasteriseWithWarningCapture` returns `{png, warnings}`; warnings is `string[]`; restores `process.stderr.write` even on error.
- `bun x vitest run packages/cora/tests/pdf/page-size.test.ts packages/cora/tests/pdf/resvg-warning.test.ts` passes.
- All three helpers have no I/O beyond what resvgCapture inherits (filesystem reads happen via fonts.ts in Task 2, not here).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun run typecheck && bun x vitest run tests/pdf/page-size.test.ts tests/pdf/resvg-warning.test.ts</automated>
  </verify>
  <done>Three pure helpers exist with passing unit tests; coords.ts owns the only Y-flip in the codebase; pageSize.ts exposes `extractSvgDimensions` for Plan 03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement textOverlay.ts + renderToPDF.ts orchestrator</name>
  <read_first>
    - packages/cora/src/renderer/Diagram.tsx (how label positions are computed for SVG output)
    - packages/cora/src/renderer/edges/EdgeLabel.tsx (edge label IR accessors)
    - packages/cora/src/renderer/nodes/ (any one file — node label centering)
    - packages/cora/src/renderer/renderToPNG.ts (entire file — analog module shape)
    - packages/cora/src/renderer/pdf/coords.ts (Task 1 output)
    - packages/cora/src/renderer/pdf/pageSize.ts (Task 1 output)
    - packages/cora/src/renderer/pdf/resvgCapture.ts (Task 1 output)
    - packages/cora/src/renderer/assets/fonts.ts (Plan 01 Task 3 output — pdfLibFontBuffers)
    - .planning/phases/03-pdf-export/03-RESEARCH.md (Pattern 1 lines 197-275 — full renderToPDF sketch; "Don't Hand-Roll" guidance)
    - .planning/phases/03-pdf-export/03-PATTERNS.md (renderToPDF.ts + textOverlay.ts sections)
    - packages/cora/tests/pdf/render-pdf.test.ts (Wave 0 stub to convert — happy-path integration test)
  </read_first>
  <behavior>
    textOverlay.ts (LOCKED CONTRACT — pure data, zero pdf-lib imports):
    - `buildTextOverlay(layouted)` returns a flat array of `TextDraw` records, one per visible label: every node's label, every edge with `label && labelX && labelY`, every group's label.
    - Each `TextDraw` carries `{cx, cy, text, weight: 'regular'|'semibold', size, color}` where `cx, cy` are the GEOMETRIC CENTER of the label in SVG coordinates (NOT a bottom-left/baseline anchor and NOT Y-flipped). All pdf-lib measurement and Y-flip math happens in renderToPDF.
    - Node label cx = `node.x + node.measuredWidth/2`, cy = `node.y + node.measuredHeight/2`.
    - Edge label cx = `edge.labelX`, cy = `edge.labelY` (the IR already centers).
    - Group label cx = `group.x + group.width/2`, cy = `group.y + GROUP_TITLE_PAD` (top-centered; document the exact pad constant inline if the IR doesn't supply it).
    - Weight selection: `theme.nodeLabel.fontWeight >= 600 ? 'semibold' : 'regular'`.
    - textOverlay.ts MUST NOT import from `pdf-lib`, `@pdf-lib/fontkit`, or any module that does. It is pure IR → data transformation.

    renderToPDF.ts (owns ALL pdf-lib measurement + Y-flip):
    - `renderToPDF(layouted, svg, {})` (no opts) returns `Uint8Array` (or Buffer) representing a valid PDF.
    - The PDF parses with `pdf-parse` and `data.text` contains every `layouted.nodes[i].label` substring.
    - With `opts.ciMode === true` and a synthetic SVG containing `font-family="DefinitelyNotInstalled"`, the call throws an Error whose message contains "resvg font warnings".
    - With `opts.page === 'a4'` and a large diagram, the resulting PDF has a single page of dimensions ~595×841 (within 0.5pt).
    - With no `opts.page` and a 200×100 diagram (margin 24), the resulting PDF page is ~248×148.
    - Position math (LOCKED): for each TextDraw,
        textWidth = font.widthOfTextAtSize(text, size * scale)
        const { x: cxPdf, y: cyPdf } = svgToPdf(cx, cy, pageH, scale, offsetX, offsetY)
        pdfX = cxPdf - textWidth / 2
        pdfY = cyPdf - (size * scale) * BASELINE_FACTOR
      where `BASELINE_FACTOR = 0.3` is a single named constant exported (or top-level const) in renderToPDF.ts. The position-tolerance test (≤1pt) explicitly references this factor; any change here MUST update both the constant and the test's expected delta.
  </behavior>
  <action>
First convert `packages/cora/tests/pdf/render-pdf.test.ts` from `.skip`/`.todo` to real tests covering the behaviors above. Use `examples/valid/box-arrows.yaml` (or any existing valid fixture) as the input — invoke the full pipeline (`parseFile` → `validateDocument` → `measureNodes` → `computeLayout` → `renderToSVG` → `renderToPDF`) so the integration test exercises the same code path as the CLI. Parse PDF output with `pdf-parse` to assert text selectability. For the position-tolerance test: parse `<text x=… y=…>` from the SVG of the same diagram, parse the PDF's text positions via `pdf-lib` re-load (`PDFDocument.load(bytes)` + iterate page content streams) OR via `pdf-parse` page-level coordinates; assert deltas ≤1pt per CONTEXT D-05. The expected PDF Y must use the same `BASELINE_FACTOR = 0.3` constant exported from `renderToPDF.ts` (import it into the test) — do not duplicate the literal.

Then implement:

`packages/cora/src/renderer/pdf/textOverlay.ts` — exports `interface TextDraw { cx: number; cy: number; text: string; weight: 'regular' | 'semibold'; size: number; color: string }` and `buildTextOverlay(layouted: LayoutedDiagram): TextDraw[]`. Walk `layouted.nodes`, `layouted.edges`, `layouted.groups`. Populate `cx, cy` per the LOCKED behavior above (geometric centers, no font measurement, no Y-flip). Use `theme.nodeLabel` / `theme.edgeLabel` for size+weight+color. CRITICAL: this file imports ONLY from the IR types module (and `node:` builtins if absolutely necessary) — zero pdf-lib, zero @pdf-lib/fontkit, zero coords.ts. Add a header comment: `// Pure IR → TextDraw[] transformation. pdf-lib measurement + Y-flip happens in renderToPDF.ts.`

`packages/cora/src/renderer/renderToPDF.ts` — exports `interface RenderToPDFOptions { page?: PageName; margin?: number; ciMode?: boolean }`, `export const BASELINE_FACTOR = 0.3` (named constant — the position-tolerance test imports this), and `async function renderToPDF(layouted, svg, options = {}): Promise<Uint8Array>`. Orchestrate per RESEARCH.md Pattern 1 lines 207-275:
1. Compute geometry via `computePageGeometry(layouted, options)`.
2. Strip `<text…>…</text>` from SVG via regex `/<text\b[^>]*>[\s\S]*?<\/text>/g`.
3. Apply `scaleSvgToPage(svgVectorOnly, 2)` for 2× rasterisation crispness.
4. Call `rasteriseWithWarningCapture` with resvg options matching renderToPNG (`fontBuffers: resvgFontBuffers()`, `loadSystemFonts: false`, `defaultFontFamily: 'Noto Sans'`, `sansSerifFamily: 'Noto Sans'`). If `options.ciMode && warnings.length > 0`, throw `new Error('resvg font warnings in CI mode: ' + warnings.join('; '))`.
5. `PDFDocument.create()`, `doc.registerFontkit(fontkit)`, `doc.embedFont(pdfLibFontBuffers().regular)`, embed semibold similarly. Use `embedFont(buf, { subset: true })` to keep PDF size down.
6. `doc.addPage([pageW, pageH])`. `doc.embedPng(png)`. `page.drawImage(image, { x: offsetX, y: offsetY, width: image.width / 2, height: image.height / 2 })` (downscale 2× PNG to page coords — per RESEARCH anti-pattern warning).
7. For each `TextDraw` from `buildTextOverlay(layouted)`: pick `regular`/`semibold` font, then compute exactly per the LOCKED position math: `textWidth = font.widthOfTextAtSize(draw.text, draw.size * scale)`, `{ x: cxPdf, y: cyPdf } = svgToPdf(draw.cx, draw.cy, pageH, scale, offsetX, offsetY)`, `pdfX = cxPdf - textWidth/2`, `pdfY = cyPdf - (draw.size * scale) * BASELINE_FACTOR`. Call `page.drawText(draw.text, {x: pdfX, y: pdfY, font, size: draw.size * scale, color: rgb(...)})`. Add a small helper `hexToRgb(hex)` (3 lines) or use pdf-lib's `rgb` directly with parsed components.
8. Return `await doc.save()`.

Add `pdf-lib` and `@pdf-lib/fontkit` to `packages/cora/package.json` `dependencies` via `bun add pdf-lib @pdf-lib/fontkit` (NOT devDependencies — runtime deps).

Run the test file — it MUST now pass (GREEN). Then run `bun run test:golden` to confirm SVG/PNG paths still work (no regression).
  </action>
  <acceptance_criteria>
- `pdf-lib` and `@pdf-lib/fontkit` appear in `packages/cora/package.json` `dependencies` (NOT devDependencies).
- `packages/cora/src/renderer/pdf/textOverlay.ts` exports `buildTextOverlay` and a `TextDraw` interface with fields `{cx, cy, text, weight, size, color}` (NOT `x, y`).
- **textOverlay.ts has zero `pdf-lib` imports.** Verify: `grep -E "from ['\"](pdf-lib|@pdf-lib)" src/renderer/pdf/textOverlay.ts | grep -v '^#' | wc -l` returns `0`.
- **textOverlay.ts performs no Y-flip and no font measurement.** Verify: `grep -E "widthOfTextAtSize|pageH \\-|svgToPdf" src/renderer/pdf/textOverlay.ts | grep -v '^#' | wc -l` returns `0`.
- `packages/cora/src/renderer/renderToPDF.ts` exports `renderToPDF`, `RenderToPDFOptions`, and `BASELINE_FACTOR`.
- The text-stripping regex removes `<text>` elements only (test by feeding a string with `<text>foo</text><rect/>` and asserting only `<rect/>` remains).
- Integration test asserts every `node.label` substring appears in pdf-parse output of `renderToPDF`'s bytes.
- Position-tolerance test imports `BASELINE_FACTOR` from `renderToPDF.ts` and asserts every label's (x, y) in PDF differs from its SVG (x, y_flipped with the same baseline factor) by ≤1pt.
- ciMode + bogus font-family → thrown Error with message matching `/resvg font warnings/`.
- All Y-flip arithmetic goes through `svgToPdf` from coords.ts — no inline `pageH - y` in renderToPDF.ts (verify by grepping `renderToPDF.ts` for `pageH \-` outside import lines and finding zero matches).
- `bun x vitest run tests/pdf/` passes.
- `bun run test:golden` still passes (no regression in SVG/PNG paths).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun run typecheck && bun run build && bun x vitest run tests/pdf/render-pdf.test.ts && bun run test:golden && test "$(grep -E "from ['\"](pdf-lib|@pdf-lib)" src/renderer/pdf/textOverlay.ts | grep -v '^#' | wc -l | tr -d ' ')" = "0" && test "$(grep -E 'widthOfTextAtSize|svgToPdf' src/renderer/pdf/textOverlay.ts | grep -v '^#' | wc -l | tr -d ' ')" = "0" && grep -E '^export const BASELINE_FACTOR' src/renderer/renderToPDF.ts && (grep -nE 'pageH[[:space:]]*-' src/renderer/renderToPDF.ts | grep -v '^[^:]*:[[:space:]]*//' | grep -v 'import' | wc -l | tr -d ' ' | grep -qx 0 || (echo "Found inline Y-flip — refactor to use svgToPdf"; exit 1))</automated>
  </verify>
  <done>`renderToPDF(layouted, svg, opts)` produces selectable-text PDFs; position fidelity within 1pt; ciMode + warning forces throw; SVG/PNG paths unaffected; textOverlay.ts is pure IR→data with zero pdf-lib coupling.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire .pdf extension dispatch into render.ts CLI</name>
  <read_first>
    - packages/cora/src/cli/commands/render.ts (entire file — outputFormat, option registration, format branch, JSON error pattern, existing try/catch shape)
    - packages/cora/src/cli/output.ts (isJsonOutput — re-used by the new catch)
    - packages/cora/src/renderer/renderToPDF.ts (Task 2 output — function signature)
    - .planning/phases/03-pdf-export/03-PATTERNS.md (render.ts MODIFY section — extension switch, option registration, format branch, error shape)
    - examples/valid/ (fixtures to test against)
  </read_first>
  <action>
Extend `packages/cora/src/cli/commands/render.ts`:

1. Update `outputFormat()` (currently lines 24-35) to return `'svg' | 'png' | 'pdf'`. Add `if (ext === '.pdf') return 'pdf';`. Update the error message to mention `.pdf`.

2. Add new Commander options to the render command registration (mirror existing `.option(...)` pattern from lines 47-53):
   - `.option('--page <size>', 'PDF page size: a4, letter, a4-portrait, letter-portrait (default: fit-to-content)')`
   - `.option('--quality <level>', 'PDF quality: high (uses Playwright) — default uses bundled resvg + pdf-lib')` — Plan 03 implements the high branch; this plan only validates the flag exists. If `options.quality && options.quality !== 'high'`, call `program.error('Invalid --quality value. Only "high" is supported.')`.

3. After the existing `format === 'png'` branch (line 115-126), add an `else if (format === 'pdf')` branch. The body MUST be wrapped in a DEDICATED `try/catch` (a new try/catch nested inside the format switch — do NOT merge into the file-level catch). Structure:

   ```
   } else if (format === 'pdf') {
     try {
       // (validation + ciMode detection + renderToPDF call below)
     } catch (error) {
       const isResvgWarning = error instanceof Error && error.message?.startsWith('resvg font warnings');
       const isChromiumMissing = (error as { code?: string })?.code === 'CHROMIUM_NOT_INSTALLED';
       const isLayout = error instanceof LayoutError;

       if (isChromiumMissing) {
         throw error; // Plan 03 owns CHROMIUM_NOT_INSTALLED emission; rethrow to its branch / outer handler
       }

       const code = isResvgWarning ? 'RESVG_FONT_WARNING' : (isLayout ? 'LAYOUT_ERROR' : undefined);
       if (!code) throw error; // re-throw anything we don't recognise

       const path = isResvgWarning ? '/render/resvg' : '/render/layout';
       const message = (error as Error).message;
       if (isJsonOutput(options)) {
         process.stdout.write(JSON.stringify([{ code, path, message }]) + '\n');
       } else {
         process.stderr.write(pc.red(`Error [${code}]: ${message}\n`));
       }
       process.exitCode = 1;
       return;
     }
   }
   ```

   The catch re-uses the existing `isJsonOutput(options)` predicate (already imported from `../output.js`) and the existing `pc` (picocolors) import. Reference the existing JSON error branch (render.ts lines 132-150) for the exact object shape and `process.stdout.write` call style.

4. Inside that `try`:
   - Import `renderToPDF` from `'../../renderer/renderToPDF.js'` and the `PageName` type from `'../../renderer/pdf/pageSize.js'`.
   - Validate `options.page` against the `PAGE_SIZES` keys; if invalid, `program.error(...)`.
   - Determine `ciMode = process.env.CI === '1' || process.env.CI === 'true'`.
   - If `options.quality === 'high'`: leave a `// Plan 03 wires high-quality lane here — falls through to default for now` comment AND fall through to the default-path call. Plan 03 will replace this with the Playwright branch. **Important:** the comment must be present so Plan 03's executor finds the exact insertion site.
   - Call `const pdfBytes = await renderToPDF(layouted, svg, { page: options.page as PageName | undefined, ciMode });`
   - `writeFileSync(options.output, Buffer.from(pdfBytes));`

5. Keep the final `else { writeFileSync(options.output, svg, 'utf8'); }` (SVG path) intact — it now corresponds to `format === 'svg'`.

End-to-end smoke: build, then run `node packages/cora/dist/cli.js render examples/valid/box-arrows.yaml -o /tmp/cora-smoke.pdf` and assert the file exists, is non-empty, and `head -c 4 /tmp/cora-smoke.pdf` is `%PDF`.
  </action>
  <acceptance_criteria>
- `outputFormat('/tmp/x.pdf')` returns `'pdf'`.
- `cora render` registers `--page` and `--quality` options (visible in `cora render --help`).
- `cora render examples/valid/box-arrows.yaml -o /tmp/out.pdf` exits 0 and produces a file starting with `%PDF`.
- The `format === 'pdf'` branch in `render.ts` is wrapped in its own dedicated `try/catch` (NOT merged into the file-level catch). Verify: in `render.ts`, between the start of `else if (format === 'pdf')` and its closing brace, exactly one new `try {` appears.
- The catch emits `code: 'RESVG_FONT_WARNING'` for resvg-warning errors. Verify: `grep -q "code: 'RESVG_FONT_WARNING'" src/cli/commands/render.ts` succeeds (or matches the equivalent quoted-property form actually used).
- The catch re-throws `CHROMIUM_NOT_INSTALLED` for Plan 03 to handle.
- `CI=1 cora render examples/valid/<fixture-with-only-bundled-fonts>.yaml -o /tmp/out.pdf` exits 0 (no warnings expected for bundled fonts).
- `CI=1 cora render examples/valid/box-arrows.yaml -o /tmp/out.pdf --format=json` — if resvg emits a font warning for any reason, the JSON output contains `"code": "RESVG_FONT_WARNING"` and exit code is non-zero. (Wave 0 stub `resvg-warning.test.ts` covers this synthetically.)
- `cora render diagram.yaml -o /tmp/out.pdf --quality=invalid` exits non-zero with a message mentioning `--quality`.
- `cora render diagram.yaml -o /tmp/out.pdf --quality=high` currently succeeds via the default resvg path (Plan 03 will replace this).
- The insertion-site comment `Plan 03 wires high-quality lane here` exists in render.ts.
- `bun run test:golden` still passes.
- `bun x vitest run tests/pdf/` still passes.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun run build && bun run typecheck && node dist/cli.js render ../../examples/valid/box-arrows.yaml -o /tmp/cora-smoke.pdf && test -s /tmp/cora-smoke.pdf && head -c 4 /tmp/cora-smoke.pdf | grep -q '%PDF' && node dist/cli.js render --help | grep -q -- '--page' && node dist/cli.js render --help | grep -q -- '--quality' && grep -q 'Plan 03 wires high-quality lane here' src/cli/commands/render.ts && grep -q "RESVG_FONT_WARNING" src/cli/commands/render.ts && bun x vitest run tests/pdf/ && bun run test:golden</automated>
  </verify>
  <done>`cora render diagram.yaml -o out.pdf [--page=a4]` works end-to-end on a clean machine; Plan 03's insertion point is marked; PDF branch has its own dedicated try/catch emitting RESVG_FONT_WARNING and re-throwing CHROMIUM_NOT_INSTALLED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User CLI input → file system (`-o`) | User-supplied path; standard `mkdirSync(recursive)` already in place; not server context |
| Diagram YAML → pdf-lib `drawText` | User content passed through pdf-lib's encoding layer (no PDF operator injection vector) |
| SVG string → resvg napi boundary | SVG generated internally from validated IR; not user-supplied to resvg directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | resvg silently substitutes a font when Noto Sans lookup fails | mitigate | `loadSystemFonts: false` (load-bearing — copied verbatim from renderToPNG); D-11 CI mode throws on warning capture; `RESVG_FONT_WARNING` JSON error code |
| T-03-02 | Information disclosure | PDF text operators encode arbitrary YAML content | accept | pdf-lib handles glyph encoding via fontkit; no PDF operator injection vector exists. Standard pdf-lib usage. |
| T-03-SC | Tampering | `bun add pdf-lib @pdf-lib/fontkit` | mitigate | Both packages [ASSUMED] legitimate per 03-RESEARCH Package Legitimacy Audit (Hopding org, 9 yrs, ~700k weekly downloads, named in STACK.md). slopcheck not run in research env. If executor has slopcheck available, run it before `bun add`; otherwise proceed (low risk per audit). **Checkpoint:** If executor cannot verify package legitimacy independently, insert a blocking-human checkpoint per planner instructions. |
</threat_model>

<verification>
- `cora render diagram.yaml -o /tmp/out.pdf` produces a file starting with `%PDF` for every fixture in `examples/valid/`
- `pdf-parse` extracts every node label from the output PDF
- `--page=a4` produces a 595.28×841.89 single-page PDF (or 841.89×595.28 if `a4` is landscape per the table)
- `CI=1` + injected font warning → exit code != 0 + `RESVG_FONT_WARNING` JSON
- No regression in SVG or PNG rendering paths (golden suite green)
</verification>

<success_criteria>
1. End-to-end vertical slice: `cora render examples/valid/<any>.yaml -o /tmp/out.pdf` works on a clean machine, with bundled deps only
2. PDF text is selectable; positions match SVG within 1pt (CONTEXT D-04, D-05)
3. `--page=a4|letter|*-portrait` scales-to-fit (CONTEXT D-02, D-03); default is fit-to-content with 24pt margin (D-01)
4. CI mode fails non-zero on resvg font-family warning (CONTEXT D-11)
5. `--quality=high` flag is registered (currently falls through to default; Plan 03 wires it)
6. SVG + PNG golden suite remains green
</success_criteria>

<output>
Create `.planning/phases/03-pdf-export/03-02-SUMMARY.md` when done.
</output>
</content>
</invoke>