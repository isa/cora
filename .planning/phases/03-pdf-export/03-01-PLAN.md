---
phase: 03-pdf-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cora/package.json
  - packages/cora/vitest.config.ts
  - packages/cora/src/renderer/renderToPNG.ts
  - packages/cora/src/renderer/assets/fonts.ts
  - packages/cora/src/renderer/assets/fonts/NotoSans-Regular.ttf
  - packages/cora/src/renderer/assets/fonts/NotoSans-SemiBold.ttf
  - packages/cora/src/renderer/assets/fonts/SOURCES.md
  - packages/cora/tests/pdf/page-size.test.ts
  - packages/cora/tests/pdf/render-pdf.test.ts
  - packages/cora/tests/pdf/resvg-warning.test.ts
  - packages/cora/tests/pdf/chromium-prompt.test.ts
  - packages/cora/tests/pdf/high-quality.test.ts
  - packages/cora/tests/smoke/clean-install.sh
autonomous: true
requirements:
  - EXP-02
  - EXP-03
  - EXP-04
  - EXP-05
tags: [pdf, vitest, fonts, wave-0]

must_haves:
  truths:
    - "Vitest runs in this package and discovers tests under packages/cora/tests/pdf/"
    - "TTF Noto Sans assets are present in src and copied into dist on build"
    - "Both renderToPNG (existing) and the upcoming renderToPDF can resolve fonts through one shared helper"
    - "Wave 1+ tests have RED stub files committed (skip or failing) so the next wave drives them GREEN"
  artifacts:
    - path: "packages/cora/vitest.config.ts"
      provides: "vitest config — tests under packages/cora/tests/**"
    - path: "packages/cora/src/renderer/assets/fonts.ts"
      provides: "resolveFontPath, resvgFontBuffers, pdfLibFontBuffers exports"
      exports: ["resolveFontPath", "resvgFontBuffers", "pdfLibFontBuffers"]
    - path: "packages/cora/src/renderer/assets/fonts/NotoSans-Regular.ttf"
      provides: "TTF Noto Sans Regular for pdf-lib embedding"
    - path: "packages/cora/src/renderer/assets/fonts/NotoSans-SemiBold.ttf"
      provides: "TTF Noto Sans SemiBold for pdf-lib embedding"
    - path: "packages/cora/src/renderer/assets/fonts/SOURCES.md"
      provides: "Provenance for vendored TTF (Noto Sans OFL 1.1 source + commit)"
      contains: "OFL"
    - path: "packages/cora/tests/pdf/page-size.test.ts"
      provides: "Wave 1 RED stubs for fit-to-content + scale-to-fit math"
    - path: "packages/cora/tests/pdf/render-pdf.test.ts"
      provides: "Wave 1 RED stubs for default PDF path"
    - path: "packages/cora/tests/pdf/resvg-warning.test.ts"
      provides: "Wave 1 RED stubs for stderr warning capture + CI exit"
    - path: "packages/cora/tests/pdf/chromium-prompt.test.ts"
      provides: "Wave 2 RED stubs for CHROMIUM_NOT_INSTALLED predicate split"
    - path: "packages/cora/tests/pdf/high-quality.test.ts"
      provides: "Wave 2 RED stubs (gated by CORA_TEST_PLAYWRIGHT=1)"
    - path: "packages/cora/tests/smoke/clean-install.sh"
      provides: "Wave 3 smoke: npm install does NOT download Chromium"
  key_links:
    - from: "packages/cora/src/renderer/renderToPNG.ts"
      to: "packages/cora/src/renderer/assets/fonts.ts"
      via: "imports resvgFontBuffers"
      pattern: "from './assets/fonts"
    - from: "packages/cora/package.json"
      to: "vitest + pdf-parse dev deps"
      via: "devDependencies"
      pattern: "vitest"
---

<objective>
Wave 0 walking-slice prep for Phase 3: introduce the vitest test framework, vendor TTF Noto Sans assets, refactor font resolution into a shared helper consumed by both renderToPNG and the upcoming renderToPDF, and lay down RED test stub files for every Wave 1+ behavior so the next waves see a failing→passing transition rather than authoring tests and code in the same task.

Purpose: Establish the test surface and shared font plumbing so Plans 02 and 03 deliver pure end-to-end vertical slices without distracting framework setup.

Output: vitest config, two TTF files + SOURCES.md, shared `assets/fonts.ts` helper, refactored `renderToPNG.ts`, six stub test files committed RED-or-skipped, dev deps added (vitest, pdf-parse).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pdf-export/03-CONTEXT.md
@.planning/phases/03-pdf-export/03-RESEARCH.md
@.planning/phases/03-pdf-export/03-PATTERNS.md
@.planning/phases/03-pdf-export/03-VALIDATION.md
@packages/cora/src/renderer/renderToPNG.ts
@packages/cora/package.json

<interfaces>
<!-- Existing font resolution block being refactored. Source: renderToPNG.ts lines 54-81. -->

Current `resolveFontPath` (renderToPNG.ts:54-68) searches two candidates:
- `<base>/assets/fonts/<filename>` (dist layout after build copies assets)
- `<base>/../src/renderer/assets/fonts/<filename>` (dev layout when running tsx from dist parent)

Current `renderFontBuffers` (renderToPNG.ts:70-81) returns cached `Buffer[]` of WOFF files in this order: Regular, SemiBold.

After refactor, the new module `packages/cora/src/renderer/assets/fonts.ts` exports:
- `resolveFontPath(filename: string): string` — same two-candidate lookup
- `resvgFontBuffers(): Buffer[]` — cached WOFF buffers (renamed from renderFontBuffers)
- `pdfLibFontBuffers(): { regular: Buffer; semibold: Buffer }` — reads TTF files

`renderToPNG.ts` consumes `resvgFontBuffers()` only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vitest + pdf-parse dev deps and vitest config</name>
  <read_first>
    - packages/cora/package.json (scripts, devDependencies)
    - packages/cora/tests/render-golden.mjs (existing bespoke test harness — must keep working under `test:golden`)
    - .planning/phases/03-pdf-export/03-VALIDATION.md (Test Infrastructure table, Sampling Rate)
    - .planning/phases/03-pdf-export/03-RESEARCH.md (Validation Architecture section, Wave 0 Gaps)
  </read_first>
  <action>
Run `bun add -d vitest pdf-parse` inside `packages/cora/`. Add a `test` script to `packages/cora/package.json` invoking `vitest run` (NOT watch mode — D-VALIDATION forbids watch flags). Keep the existing `test:golden` script unchanged. Create `packages/cora/vitest.config.ts` exporting `defineConfig({ test: { include: ['tests/**/*.test.ts'], reporters: 'default', testTimeout: 30000 } })`. Do NOT include `tests/smoke/**` in the vitest include glob — clean-install.sh is a shell smoke test, not a vitest test. Verify the `vitest` and `pdf-parse` versions resolved by lockfile match what's documented in 03-RESEARCH.md "Standard Stack" (vitest 2.x, pdf-parse latest stable). No application code changes in this task.
  </action>
  <acceptance_criteria>
- `packages/cora/package.json` contains `vitest` and `pdf-parse` in `devDependencies`.
- `packages/cora/package.json` exposes a `test` script that runs `vitest run` (non-watch).
- `packages/cora/vitest.config.ts` exists with `include: ['tests/**/*.test.ts']`.
- Running `bun x vitest run` (from `packages/cora/`) discovers zero tests OR runs only files that exist (it MUST exit non-error when no tests match — vitest's `--passWithNoTests` may be required; set it if vitest 2.x defaults to fail-on-empty).
- `bun run test:golden` still works (existing harness untouched).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun x vitest run --passWithNoTests && bun run test:golden</automated>
  </verify>
  <done>Vitest installed, configured, and able to run (even with zero tests); existing golden harness intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Vendor TTF Noto Sans + document provenance</name>
  <read_first>
    - packages/cora/src/renderer/assets/fonts/ (existing WOFF siblings)
    - .planning/phases/03-pdf-export/03-RESEARCH.md (Open Questions §1 TTF source; Security Domain font supply-chain row)
    - .planning/phases/03-pdf-export/03-PATTERNS.md ("NotoSans-Regular.ttf + NotoSans-SemiBold.ttf + SOURCES.md" section)
  </read_first>
  <action>
Vendor `NotoSans-Regular.ttf` and `NotoSans-SemiBold.ttf` into `packages/cora/src/renderer/assets/fonts/` from the upstream `notofonts/notofonts.github.io` repo `fonts/NotoSans/hinted/ttf/` directory. Use the static (non-variable) Regular and SemiBold weights. Create `packages/cora/src/renderer/assets/fonts/SOURCES.md` documenting: upstream repo URL, exact commit SHA pulled from, file paths within upstream, SHA-256 of each TTF, OFL 1.1 license notice, and the date vendored (2026-05-22). Do NOT modify the existing WOFF files — both formats coexist (WOFF for resvg, TTF for pdf-lib). After dropping the TTFs, run `bun run build` and confirm `dist/renderer/assets/fonts/NotoSans-Regular.ttf` and `NotoSans-SemiBold.ttf` both exist (the build script's `cp -r src/renderer/assets dist/renderer/` step auto-publishes them per Pitfall 1).
  </action>
  <acceptance_criteria>
- `packages/cora/src/renderer/assets/fonts/NotoSans-Regular.ttf` exists (file size between 200KB and 1MB).
- `packages/cora/src/renderer/assets/fonts/NotoSans-SemiBold.ttf` exists (file size between 200KB and 1MB).
- `packages/cora/src/renderer/assets/fonts/SOURCES.md` exists and contains the substrings "OFL", "notofonts", and a commit SHA.
- After `bun run build`, both TTF files are present under `packages/cora/dist/renderer/assets/fonts/`.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun run build && test -f dist/renderer/assets/fonts/NotoSans-Regular.ttf && test -f dist/renderer/assets/fonts/NotoSans-SemiBold.ttf && grep -q OFL src/renderer/assets/fonts/SOURCES.md && grep -q notofonts src/renderer/assets/fonts/SOURCES.md</automated>
  </verify>
  <done>TTF assets vendored, provenance documented, build pipeline picks them up.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refactor font resolution into shared assets/fonts.ts helper</name>
  <read_first>
    - packages/cora/src/renderer/renderToPNG.ts (lines 1-103 — entire file)
    - .planning/phases/03-pdf-export/03-PATTERNS.md ("packages/cora/src/renderer/assets/fonts.ts" section — Exact code to lift block; refactor action)
    - .planning/phases/03-pdf-export/03-RESEARCH.md (Pitfall 1; Font path resolution code example lines 573-601)
  </read_first>
  <action>
Create `packages/cora/src/renderer/assets/fonts.ts` exporting three symbols: `resolveFontPath(filename)`, `resvgFontBuffers()`, and `pdfLibFontBuffers()`. Lift the existing `resolveFontPath` (renderToPNG.ts:54-68) and `renderFontBuffers` (renderToPNG.ts:70-81) verbatim into this module, but: (a) export `resolveFontPath`, (b) rename `renderFontBuffers` to `resvgFontBuffers` and export it, (c) preserve the existing two-candidate path lookup — do NOT add the third `../../src/...` candidate from the RESEARCH sketch (per 03-PATTERNS guidance; only add if a test later proves it necessary). Add `pdfLibFontBuffers(): { regular: Buffer; semibold: Buffer }` that reads `NotoSans-Regular.ttf` and `NotoSans-SemiBold.ttf` through `resolveFontPath` and caches the result in a module-local `let pdfLibCache`. In `renderToPNG.ts`, replace the lifted block with `import { resvgFontBuffers } from './assets/fonts.js';` and change `fontBuffers: renderFontBuffers()` to `fontBuffers: resvgFontBuffers()`. Verify `bun run test:golden` still produces byte-identical golden output (the renderer behavior must not change — this is a pure refactor).
  </action>
  <acceptance_criteria>
- `packages/cora/src/renderer/assets/fonts.ts` exists and exports `resolveFontPath`, `resvgFontBuffers`, `pdfLibFontBuffers`.
- `pdfLibFontBuffers()` returns `{ regular: Buffer, semibold: Buffer }` and caches across calls (assert by reference equality on second call).
- `packages/cora/src/renderer/renderToPNG.ts` no longer contains the literal string `function resolveFontPath` (was lifted).
- `renderToPNG.ts` imports from `./assets/fonts.js`.
- `bun run test:golden` passes (golden PNGs/SVGs byte-identical to pre-refactor — proves no behavior change).
- `bun run typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun run typecheck && bun run build && bun run test:golden && grep -L 'function resolveFontPath' src/renderer/renderToPNG.ts && grep -q "from './assets/fonts" src/renderer/renderToPNG.ts && grep -q 'export function pdfLibFontBuffers' src/renderer/assets/fonts.ts</automated>
  </verify>
  <done>Shared font helper exports three symbols; renderToPNG consumes the shared helper; golden tests prove no behavior change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Author RED stub test files under tests/pdf/ and tests/smoke/</name>
  <read_first>
    - .planning/phases/03-pdf-export/03-VALIDATION.md (Per-Task Verification Map; Wave 0 Requirements)
    - .planning/phases/03-pdf-export/03-RESEARCH.md (Phase Requirements → Test Map; Wave 0 Gaps)
    - packages/cora/tests/render-golden.mjs (existing harness pattern for `execFileSync(process.execPath, [cliPath, 'render', ...])`)
  </read_first>
  <action>
Create six stub files. Each vitest file MUST use `describe.skip(...)` OR `it.todo(...)` so the suite exits non-error (we want RED-but-non-crashing for Wave 0). The next waves convert `.skip`/`.todo` into real tests.

(1) `packages/cora/tests/pdf/page-size.test.ts` — `describe.skip('computePageGeometry', () => { it.todo('fit-to-content returns bbox + 2*margin'); it.todo('--page=a4 with oversized diagram returns scale<1 and centered offset'); it.todo('a4-portrait swaps width/height'); });`

(2) `packages/cora/tests/pdf/render-pdf.test.ts` — `describe.skip('renderToPDF default path', () => { it.todo('produces valid PDF byte stream'); it.todo('text is selectable via pdf-parse'); it.todo('text positions match SVG within 1pt tolerance'); });`

(3) `packages/cora/tests/pdf/resvg-warning.test.ts` — `describe.skip('resvg stderr capture', () => { it.todo('captures No match for font-family warnings'); it.todo('CI=1 + warnings → non-zero exit'); });`

(4) `packages/cora/tests/pdf/chromium-prompt.test.ts` — `describe.skip('--quality=high predicate split', () => { it.todo('non-interactive + no --yes + missing chromium → CHROMIUM_NOT_INSTALLED JSON'); it.todo('--yes triggers install via spawn'); it.todo('CORA_AUTO_INSTALL=1 triggers install'); });`

(5) `packages/cora/tests/pdf/high-quality.test.ts` — Gated lane. At top: `const enabled = process.env.CORA_TEST_PLAYWRIGHT === '1'; const d = enabled ? describe : describe.skip;` then `d('renderToPDFHighQuality', () => { it.todo('produces valid PDF via Playwright'); });`

(6) `packages/cora/tests/smoke/clean-install.sh` — Bash script with shebang `#!/usr/bin/env bash`, `set -euo pipefail`. Body for now: `echo "TODO: implement clean-install Chromium-absence assertion in Plan 04"; exit 0;` Make executable via `chmod +x`. Plan 04 fills the real body.

Verify `bun x vitest run --passWithNoTests` passes (all `.skip`/`.todo` is non-error). Verify `bash packages/cora/tests/smoke/clean-install.sh` exits 0.
  </action>
  <acceptance_criteria>
- All six stub files exist.
- `bun x vitest run` exits 0 — every test is skipped or todo, no failures, no parse errors.
- `bash packages/cora/tests/smoke/clean-install.sh` exits 0.
- `tests/smoke/clean-install.sh` is executable (mode includes user-execute bit).
- Vitest output shows at least 11 todos/skips across the 5 test files (sanity that the stubs are real, not empty files).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun x vitest run 2>&1 | tee /tmp/vitest-w0.log && grep -E '(todo|skip)' /tmp/vitest-w0.log | head -1 && test -x tests/smoke/clean-install.sh && bash tests/smoke/clean-install.sh</automated>
  </verify>
  <done>Six stub files committed RED-but-non-crashing; Wave 1+ can convert `.skip`/`.todo` into real tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TTF vendor → repo | Supply-chain crossing: third-party font binaries committed into source |
| npm install → dev environment | New dev deps (vitest, pdf-parse) execute scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W0-01 | Tampering | TTF assets vendored from upstream | mitigate | SOURCES.md records upstream repo + commit SHA + SHA-256 per font file; reviewer can re-fetch and diff |
| T-03-W0-SC | Tampering | `bun add -d vitest pdf-parse` | mitigate | Both packages have multi-year history, millions of weekly downloads, source repos visible (verified in 03-RESEARCH Package Legitimacy Audit row by extension); slopcheck not run in this environment — recorded as ASSUMED legitimate. If executor finds slopcheck available, run it; otherwise proceed (low risk: vitest is the de facto JS test runner; pdf-parse is a 9-year-old library). |

No CHECKPOINT required: vitest and pdf-parse are well-known packages, not [SUS] or [SLOP]. If the executor cannot independently verify, add a `checkpoint:human-verify` before `bun add`.
</threat_model>

<verification>
- `bun x vitest run` exits 0 with stubs registered as skip/todo
- `bun run test:golden` still passes (renderer behavior unchanged after refactor)
- `bun run build` produces TTF files under `dist/renderer/assets/fonts/`
- `bun run typecheck` passes
</verification>

<success_criteria>
1. vitest framework installed and discovering tests/pdf/**/*.test.ts (zero-result is OK; Wave 1 fills the bodies)
2. TTF Noto Sans assets present in src AND dist, with SOURCES.md documenting OFL provenance
3. Single `assets/fonts.ts` module exports `resolveFontPath`, `resvgFontBuffers`, `pdfLibFontBuffers`; `renderToPNG.ts` consumes it; goldens still pass
4. Six RED stub files in place so Plans 02–04 see failing→passing transitions per task
</success_criteria>

<output>
Create `.planning/phases/03-pdf-export/03-01-SUMMARY.md` when done.
</output>
