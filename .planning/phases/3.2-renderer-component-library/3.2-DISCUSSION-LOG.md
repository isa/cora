# Phase 3.2: Renderer Component Library - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-22
**Phase:** 3.2-Renderer Component Library
**Areas discussed:** catalog shape, style prop contract, specialized components, lines and markers, requirements update

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| All three | Discuss catalog shape, style prop contract, and line/marker model | ✓ |
| API + styles only | Focus on component prop design and normalized style vocabulary | |
| Other focus | User names a narrower or different set of decisions | |

**User's choice:** All three.
**Notes:** User wanted the component structure sorted before the codebase grows.

---

## Catalog Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Keep as legacy render components + add new catalog components | Preserve existing render path and add normalized catalog alongside it | |
| Fold existing shapes into the new catalog directly | Cleaner API sooner, with higher migration scope | ✓ |
| Rename existing components to semantic catalog names now | Aggressive cleanup before preview/extensions | |

**User's choice:** Fold existing shapes into the new catalog.
**Notes:** User specifically said to kill `CylinderNode`, `CloudNode`, `DiamondNode`, and `HexagonNode`; new components will replace them.

---

## Legacy Shape Support

| Option | Description | Selected |
|--------|-------------|----------|
| Internal compatibility mapping | Keep old YAML/schema support but map internally to new components | |
| Remove old shape support now | Update schema/examples/goldens/docs for consistency | ✓ |
| Keep old components privately only | Remove from public catalog but keep private old implementations | |

**User's choice:** Remove old shape support now.
**Notes:** User emphasized consistency at all times.

---

## Canonical Catalog

| Option | Description | Selected |
|--------|-------------|----------|
| Requested catalog exactly | Group, BoxNode, LabelNode, IconNode, LabelIconNode, WebsiteNode, PageNode, AppNode, DecisionNode, IssueNode, Line, markers | ✓ |
| Add a generic ShapeNode escape hatch | Same catalog plus a low-level odd-shape primitive | ✓ |
| Smaller catalog first | Defer website/page/app components | |

**User's choice:** Requested catalog plus `ShapeNode`.
**Notes:** Old shape-specific components are not part of the new public model.

---

## Style Prop Contract

| Option | Description | Selected |
|--------|-------------|----------|
| One shared BoxStyleProps | Shared box-like base type with common style fields | ✓ |
| Per-component style props | Each component owns its own style shape | |
| Token object only | Components receive a single style object | |

**User's choice:** One shared `BoxStyleProps`.
**Notes:** Shared fields are `backgroundColor`, `radius`, `borderStyle`, `borderColor`, `borderWidth`, `text`, `textColor`, and `size`.

---

## Size Representation

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit dimensions | `size: { width: number; height: number }` | |
| Named sizes | `size: "sm" | "md" | "lg"` | |
| Both dimensions and presets | Supports exact layout and convenient presets | ✓ |

**User's choice:** Both dimensions and presets.
**Notes:** Add `xl` and `xxl` presets, so preset values are `sm | md | lg | xl | xxl`.

---

## Specialized Props

| Option | Description | Selected |
|--------|-------------|----------|
| Lock exactly | Page types, issue icons, iconColor, skeleton colors, textless Website/App | ✓ |
| Add text to Website/App | WebsiteNode and AppNode also support optional labels | ✓ |
| Adjust vocabularies | Different page types, issue icons, or field names | |

**User's choice:** Lock specialized props and add optional text to Website/App.
**Notes:** User clarified that box-driven nodes can bear icons, while `IconNode` is icon-only and renders an SVG icon from a future library/pack.

---

## Line Geometry

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit SVG points | `points: Array<{ x: number; y: number }>` | ✓ |
| From/to endpoints only | Simple direct line API | |
| Both points and endpoints | More flexible but branchier | |

**User's choice:** Explicit SVG points.
**Notes:** Routing stays outside the component.

---

## Marker Vocabulary

| Option | Description | Selected |
|--------|-------------|----------|
| camelCase TypeScript values | `none | arrow | circle | filledCircle` | ✓ |
| hyphenated literal values | `none | arrow-head | circle | filled-circle` | |
| Support both aliases now | More forgiving, larger surface | |

**User's choice:** camelCase TypeScript values.
**Notes:** YAML/user-facing aliases can be handled later if needed.

---

## Public Edge Primitive

| Option | Description | Selected |
|--------|-------------|----------|
| Line becomes public; Arrow becomes compatibility/internal | Public catalog uses `Line` with markers | ✓ |
| Keep both public | More compatible but less consistent | |
| Delete Arrow immediately | Cleanest catalog, highest migration risk | |

**User's choice:** `Line` becomes public; `Arrow` becomes compatibility/internal.
**Notes:** Existing arrow-style edges can map to `Line` with `endMarker: "arrow"`.

---

## Requirements Update

| Option | Description | Selected |
|--------|-------------|----------|
| Write context now | Leave REQUIREMENTS.md update for planning/execution | |
| Also update requirements | Add RCL requirements during discuss-phase | ✓ |
| Explore more | Ask another round before writing context | |

**User's choice:** Also update requirements.
**Notes:** RCL requirements were added to `.planning/REQUIREMENTS.md`.

## the agent's Discretion

- Exact preset size values, default colors, radii, marker sizes, and icon-slot fallback behavior.
- Exact file split within `renderer/components/`, as long as the public barrel and family organization stay clean.

## Deferred Ideas

- Phase 3.3 preview UI and live controls.
- Phase 5 extension/icon pack loading.
