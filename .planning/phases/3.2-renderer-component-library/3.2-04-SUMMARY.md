---
phase: 3.2-renderer-component-library
plan: 04
subsystem: renderer
tags: [renderer, schema, examples, goldens, docs]
requires:
  - phase: 3.2-renderer-component-library/03
    provides: clean public component barrel and Line-based renderer path
provides:
  - Public schema and examples migrated from shape to component
  - Renderer dispatch, measurement, and theme resolution aligned to component
  - Old shape node implementation files removed
  - Updated AGENTS.md component catalog docs
affects: [schema, examples, renderer, documentation, golden-svg]
tech-stack:
  added: []
  patterns:
    - YAML component discriminator maps to catalog renderer components
key-files:
  created: []
  modified:
    - packages/cora/src/core/schema/diagram.schema.json
    - packages/cora/src/core/types.ts
    - packages/cora/src/layout-ir.ts
    - packages/cora/src/core/measureText.ts
    - packages/cora/src/core/themeResolver.ts
    - packages/cora/src/renderer/themes/default.ts
    - packages/cora/src/renderer/Diagram.tsx
    - examples/valid/box-arrows.yaml
    - examples/valid/database.yaml
    - examples/valid/flowchart.yaml
    - examples/valid/infra.yaml
    - AGENTS.md
    - packages/cora/package.json
    - packages/cora/tests/golden/*.svg
key-decisions:
  - "Keep `component: shape` as the low-level catalog escape hatch required by the phase context."
  - "Use default renderer fallbacks for icon/page/issue details because YAML does not expose those component-specific props yet."
  - "Add a package-local `cora` script so package-level verification can run `bun run cora schema`."
patterns-established:
  - "Schema and examples expose component vocabulary only."
  - "Renderer dispatch uses node.component and catalog adapters."
requirements-completed: [RCL-01, RCL-02, RCL-03, RCL-04, RCL-05, RCL-06, RCL-07, RCL-08, RCL-09, RCL-10]
duration: 7 min
completed: 2026-05-22
---

# Phase 3.2 Plan 04: Component Vocabulary Sweep Summary

**Schema, examples, renderer dispatch, theme keys, docs, and goldens now use the component catalog vocabulary**

## Performance

- **Duration:** 7 min
- **Started:** 2026-05-22T15:18:55Z
- **Completed:** 2026-05-22T15:25:49Z
- **Tasks:** 4
- **Files modified:** 28

## Accomplishments

- Replaced public `shape` schema fields with `component` values: `box`, `label`, `icon`, `labelIcon`, `website`, `page`, `app`, `decision`, `issue`, and `shape`.
- Moved measurement, style resolution, default theme keys, and renderer dispatch to `node.component ?? 'box'`.
- Removed old shape-specific renderer implementation files.
- Migrated valid examples and AGENTS.md to the final component catalog.
- Refreshed golden SVG baselines and verified the final public declaration surface.

## Task Commits

1. **Task 3.2-04-01: Replace public shape schema with catalog component vocabulary** - `a3fa815` (feat)
2. **Task 3.2-04-02: Update measurement, theme resolution, and renderer dispatch to component** - `ab19453` (feat)
3. **Task 3.2-04-03: Update examples, docs, schema output, and golden baselines** - `9d45ace` (docs)
4. **Task 3.2-04-04: Run full phase regression and source cleanup checks** - `b87eaee` (chore)

## Files Created/Modified

- `packages/cora/src/core/schema/diagram.schema.json` - Public node discriminator migrated to `component`.
- `packages/cora/src/core/types.ts` and `packages/cora/src/layout-ir.ts` - Diagram node component type added and legacy shape field removed.
- `packages/cora/src/core/measureText.ts` - Decision measurement now uses `component === 'decision'`.
- `packages/cora/src/core/themeResolver.ts` and `packages/cora/src/renderer/themes/default.ts` - Theme lookup and keys now use the component catalog.
- `packages/cora/src/renderer/Diagram.tsx` - Renderer dispatch maps components to catalog nodes.
- `packages/cora/src/renderer/components/nodes/*` - Old shape-specific node files removed.
- `examples/valid/*.yaml` - Examples use `component` or default `box`.
- `AGENTS.md` - Agent-facing docs describe the final component catalog.
- `packages/cora/package.json` - Added package-local `cora` verification script.
- `packages/cora/tests/golden/*.svg` - Updated render baselines.

## Decisions Made

The plan had an internal contradiction: it required `component` to include the `shape` value, while one grep criterion also rejected `"shape"` in the schema. I preserved `component: shape` because that is part of the locked catalog and is referenced by the phase context and examples/docs contract.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Package-level `bun run cora schema` was unavailable**
- **Found during:** Task 3.2-04-03 (Update examples, docs, schema output, and golden baselines)
- **Issue:** The plan verification command runs from `packages/cora`, but `packages/cora/package.json` did not define a `cora` script.
- **Fix:** Added `"cora": "node dist/cli.js"` to the package scripts.
- **Files modified:** `packages/cora/package.json`
- **Verification:** `cd packages/cora && bun run cora schema | rg '"component"'` passed.
- **Committed in:** `9d45ace`

**2. [Rule 3 - Blocking] Acceptance criterion contradicted required `component: shape` value**
- **Found during:** Task 3.2-04-01 (Replace public shape schema with catalog component vocabulary)
- **Issue:** The schema must include `"shape"` as a valid component value, but the task grep also rejected any `"shape"` occurrence.
- **Fix:** Kept `"shape"` in the component enum and verified old values `rounded`, `cylinder`, `cloud`, `diamond`, and `hexagon` are absent.
- **Files modified:** `packages/cora/src/core/schema/diagram.schema.json`
- **Verification:** Final cleanup assertion rejects old shape values and `shape:` YAML usage while preserving the catalog escape hatch.
- **Committed in:** `a3fa815`

---

**Total deviations:** 2 auto-fixed (Rule 3 blocking).
**Impact on plan:** Both changes preserve the phase intent and make verification commands reliable.

## Issues Encountered

None beyond the documented verification-command and plan-spec issues.

## User Setup Required

None - no external service configuration required.

## Verification

- `cd packages/cora && bun run typecheck` passed.
- `cd packages/cora && bun run build` passed.
- `cd packages/cora && bun run test` passed 48 tests with 1 skipped Playwright-gated test.
- `cd packages/cora && bun run test:golden` passed 5/5 fixtures.
- `dist/renderer/components/index.d.ts` contains `BoxStyleProps`, `Line`, `MarkerType`, `IconNode`, `PageNode`, `IssueNode`, and `ShapeNode`.
- Public source assertions passed: no old shape-specific exports, no top-level component exports, and no old shape values or `shape:` usage in schema/examples/AGENTS.md.

## Next Phase Readiness

Phase 3.2 implementation is complete. The component catalog is ready for Phase 3.3 preview canvas planning and implementation.

---
*Phase: 3.2-renderer-component-library*
*Completed: 2026-05-22*
