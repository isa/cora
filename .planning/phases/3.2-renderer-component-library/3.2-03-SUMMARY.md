---
phase: 3.2-renderer-component-library
plan: 03
subsystem: renderer
tags: [renderer, diagram, migration, line, barrel]
requires:
  - phase: 3.2-renderer-component-library/01
    provides: Line and marker primitives
  - phase: 3.2-renderer-component-library/02
    provides: named node catalog components
provides:
  - Current renderer edge path migrated to Line
  - Central marker definitions in Diagram defs
  - Public component barrel without Arrow or old shape node exports
affects: [renderer-components, package-exports, golden-svg]
tech-stack:
  added: []
  patterns:
    - Public component import target remains cora/renderer/components
key-files:
  created: []
  modified:
    - packages/cora/src/renderer/Diagram.tsx
    - packages/cora/src/renderer/components/index.ts
    - packages/cora/src/renderer/components/edges/Arrow.tsx
    - packages/cora/src/renderer/components/lines/Line.tsx
    - packages/cora/tests/golden/box-arrows.svg
    - packages/cora/tests/golden/database.svg
    - packages/cora/tests/golden/flowchart.svg
    - packages/cora/tests/golden/infra.svg
    - packages/cora/tests/golden/microservice.svg
key-decisions:
  - "Diagram now renders edges through Line and marker definitions instead of public Arrow."
  - "Arrow remains internal only as a compatibility adapter."
  - "Golden baselines were refreshed because marker defs and marker-end attributes change SVG serialization."
patterns-established:
  - "Renderer composition can consume catalog primitives while old schema compatibility is removed separately."
requirements-completed: [RCL-01, RCL-02, RCL-08, RCL-09, RCL-10]
duration: 7 min
completed: 2026-05-22
---

# Phase 3.2 Plan 03: Renderer Migration Summary

**Current renderer edge output now uses the public Line primitive and the component barrel exposes only the new catalog surface**

## Performance

- **Duration:** 7 min
- **Started:** 2026-05-22T15:11:40Z
- **Completed:** 2026-05-22T15:18:53Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Replaced `Diagram.tsx` edge rendering with `Line` and centralized `LineMarkerDefs`.
- Converted `Arrow` into an internal adapter path and removed it from the public barrel.
- Removed `RoundedNode`, `CylinderNode`, `CloudNode`, `DiamondNode`, and `HexagonNode` from `cora/renderer/components`.
- Verified generated component declarations expose the new catalog and not the old public shape model.

## Task Commits

1. **Task 3.2-03-01: Render current edges through Line and marker defs** - `0644dd4` (feat)
2. **Task 3.2-03-02: Clean public component barrel while preserving current renderer compatibility** - `46b3455` (refactor)
3. **Task 3.2-03-03: Verify public package component export artifact** - `99db7d2` (chore)

## Files Created/Modified

- `packages/cora/src/renderer/Diagram.tsx` - Edge rendering moved to `Line`; private old node imports kept temporarily.
- `packages/cora/src/renderer/components/index.ts` - Public barrel cleaned to the catalog surface.
- `packages/cora/src/renderer/components/edges/Arrow.tsx` - Internal adapter delegates to `Line`.
- `packages/cora/src/renderer/components/lines/Line.tsx` - Supports an explicit path for internal compatibility.
- `packages/cora/tests/golden/*.svg` - Baselines refreshed for SVG marker serialization.

## Decisions Made

The renderer still imports old shape-specific implementation modules privately until Plan 04 removes old schema vocabulary. This keeps the public component barrel clean without forcing schema migration into this plan.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Golden SVGs changed after marker migration**
- **Found during:** Task 3.2-03-01 (Render current edges through Line and marker defs)
- **Issue:** `bun run test:golden` failed for all golden fixtures because centralized SVG marker defs and `marker-end` attributes intentionally changed serialized SVG output.
- **Fix:** Refreshed golden SVG baselines after verifying typecheck and build passed.
- **Files modified:** `packages/cora/tests/golden/*.svg`
- **Verification:** `UPDATE_GOLDEN=1 node tests/render-golden.mjs` followed by `bun run test:golden` passed 5/5 fixtures.
- **Committed in:** `0644dd4`

---

**Total deviations:** 1 auto-fixed (Rule 3 blocking).
**Impact on plan:** Render semantics moved to the intended marker primitive; the deviation is limited to golden baseline timing.

## Issues Encountered

None beyond the documented golden baseline refresh.

## User Setup Required

None - no external service configuration required.

## Verification

- `cd packages/cora && bun run typecheck` passed.
- `cd packages/cora && bun run build` passed.
- `cd packages/cora && bun run test:golden` passed 5/5 fixtures.
- `cd packages/cora && bun run test` passed 48 tests with 1 skipped Playwright-gated test.
- `dist/renderer/components/index.d.ts` contains `Line`, `BoxStyleProps`, and `IconNode`, and does not contain `Arrow`, `CylinderNode`, `CloudNode`, `DiamondNode`, or `HexagonNode`.

## Next Phase Readiness

Ready for Plan 04. The public component barrel is clean; remaining old shape vocabulary is confined to schema/types/examples/theme/runtime compatibility paths.

---
*Phase: 3.2-renderer-component-library*
*Completed: 2026-05-22*
