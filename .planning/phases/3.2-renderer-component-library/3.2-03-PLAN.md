---
phase: 3.2-renderer-component-library
plan: 03
type: execute
wave: 2
depends_on: ["3.2-01", "3.2-02"]
files_modified:
  - packages/cora/src/renderer/Diagram.tsx
  - packages/cora/src/renderer/components/index.ts
  - packages/cora/src/renderer/components/edges/Arrow.tsx
  - packages/cora/src/renderer/components/edges/EdgeLabel.tsx
  - packages/cora/src/renderer/components/lines/Line.tsx
  - packages/cora/src/renderer/components/lines/markers.tsx
  - packages/cora/src/renderer/viewBox.ts
  - packages/cora/tests/renderer/components.test.tsx
autonomous: true
requirements:
  - RCL-01
  - RCL-02
  - RCL-08
  - RCL-09
  - RCL-10
tags: [renderer, diagram, migration, line, barrel]
decision_refs:
  - D-01
  - D-02
  - D-04
  - D-18
  - D-19
  - D-20
  - D-21
  - D-22

must_haves:
  truths:
    - "`Diagram.tsx` renders current edges through `Line` with `endMarker='arrow'`"
    - "`Diagram.tsx` includes centralized line marker definitions in `<defs>`"
    - "`Arrow` is not exported from `packages/cora/src/renderer/components/index.ts`"
    - "Old shape-specific node components are not exported from the public component barrel"
    - "The current render path still imports renderer components only from `./components/index.js`"
    - "Golden SVG output remains stable after the render path migrates to `Line`"
  artifacts:
    - path: "packages/cora/src/renderer/Diagram.tsx"
      provides: "Current renderer composition root migrated to catalog primitives"
      contains: "Line"
    - path: "packages/cora/src/renderer/components/index.ts"
      provides: "Clean public barrel"
      contains: "Line"
    - path: "packages/cora/src/renderer/components/edges/Arrow.tsx"
      provides: "Internal compatibility adapter or removed public surface"
      contains: "Line"
---

<objective>
Migrate the current renderer composition path to the new line primitive and clean up the public component barrel.

This plan keeps schema/examples/goldens semantically unchanged. It may keep internal compatibility for old shape values until Plan 04 removes them from the public schema/docs.
</objective>

<tasks>

<task type="execute" id="3.2-03-01">
  <name>Render current edges through Line and marker defs</name>
  <read_first>
    - packages/cora/src/renderer/Diagram.tsx
    - packages/cora/src/renderer/components/edges/Arrow.tsx
    - packages/cora/src/renderer/components/edges/decorations.ts
    - packages/cora/src/renderer/components/edges/EdgeLabel.tsx
    - packages/cora/src/renderer/components/lines/Line.tsx
    - packages/cora/src/renderer/components/lines/markers.tsx
    - packages/cora/src/renderer/viewBox.ts
  </read_first>
  <action>
Update `packages/cora/src/renderer/Diagram.tsx` so the current edge group renders `Line` instead of `Arrow`:

- import `Line` and `LineMarkerDefs` from `./components/index.js`
- add `<LineMarkerDefs color={diagram.theme.edge.stroke} />` inside the existing `<defs>`
- replace `<Arrow edge={edge} theme={diagram.theme} />` with `<Line points={edge.points} strokeColor={diagram.theme.edge.stroke} strokeWidth={diagram.theme.edge.strokeWidth} endMarker="arrow" />`
- preserve current group/node/edge-label SVG group ordering
- preserve existing edge keys

If `Arrow.tsx` remains, change it to an internal compatibility adapter that delegates to `Line` and is not exported from the public barrel. It may still accept `EdgeComponentProps` internally.
  </action>
  <acceptance_criteria>
- `packages/cora/src/renderer/Diagram.tsx` imports `Line` from `./components/index.js`.
- `packages/cora/src/renderer/Diagram.tsx` contains `LineMarkerDefs`.
- `packages/cora/src/renderer/Diagram.tsx` contains `endMarker="arrow"`.
- `packages/cora/src/renderer/Diagram.tsx` does not contain `<Arrow`.
- `packages/cora/src/renderer/components/edges/Arrow.tsx`, if it exists, contains `Line`.
- `cd packages/cora && bun run typecheck` exits 0.
- `cd packages/cora && bun run build` exits 0.
- `cd packages/cora && bun run test:golden` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "Line|LineMarkerDefs|endMarker=\"arrow\"" packages/cora/src/renderer/Diagram.tsx && ! rg "<Arrow" packages/cora/src/renderer/Diagram.tsx && cd packages/cora && bun run typecheck && bun run build && bun run test:golden</automated>
  </verify>
</task>

<task type="execute" id="3.2-03-02">
  <name>Clean public component barrel while preserving current renderer compatibility</name>
  <read_first>
    - packages/cora/src/renderer/components/index.ts
    - packages/cora/src/renderer/components/edges/Arrow.tsx
    - packages/cora/src/renderer/components/nodes/RoundedNode.tsx
    - packages/cora/src/renderer/components/nodes/CylinderNode.tsx
    - packages/cora/src/renderer/components/nodes/CloudNode.tsx
    - packages/cora/src/renderer/components/nodes/DiamondNode.tsx
    - packages/cora/src/renderer/components/nodes/HexagonNode.tsx
    - packages/cora/src/renderer/Diagram.tsx
    - packages/cora/package.json
    - packages/cora/tsdown.config.ts
  </read_first>
  <action>
Update `packages/cora/src/renderer/components/index.ts` so the public catalog exports the new component surface and does not export old public shape-specific components:

Keep exporting:

- `NodeComponentProps`, `EdgeComponentProps`, `GroupComponentProps`
- `BoxStyleProps`, style/size types and helpers
- `Group`
- `BoxNode`, `LabelNode`, `IconNode`, `LabelIconNode`, `WebsiteNode`, `PageNode`, `AppNode`, `DecisionNode`, `IssueNode`, `ShapeNode`
- `Line`, `LineProps`, `LineStyle`, `MarkerType`, `LineMarkerDefs`, `markerUrl`, `lineDasharray`
- `EdgeLabel` and edge decoration helpers that current layout/viewBox code still imports

Stop exporting:

- `Arrow`
- `RoundedNode`
- `CylinderNode`
- `CloudNode`
- `DiamondNode`
- `HexagonNode`

If `Diagram.tsx` still needs old shape-specific compatibility during this plan, import private compatibility modules directly from implementation paths only inside `Diagram.tsx`, and leave a short TODO pointing to Plan 04 removal. The public barrel must not expose them.
  </action>
  <acceptance_criteria>
- `packages/cora/src/renderer/components/index.ts` exports `BoxNode`, `LabelNode`, `IconNode`, `LabelIconNode`, `WebsiteNode`, `PageNode`, `AppNode`, `DecisionNode`, `IssueNode`, `ShapeNode`, `Line`, and `LineMarkerDefs`.
- `packages/cora/src/renderer/components/index.ts` does not export `Arrow`.
- `packages/cora/src/renderer/components/index.ts` does not export `RoundedNode`, `CylinderNode`, `CloudNode`, `DiamondNode`, or `HexagonNode`.
- `packages/cora/src/renderer/Diagram.tsx` still imports from `./components/index.js`.
- `cd packages/cora && bun run typecheck` exits 0.
- `cd packages/cora && bun run test:golden` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "BoxNode|LabelNode|IconNode|LabelIconNode|WebsiteNode|PageNode|AppNode|DecisionNode|IssueNode|ShapeNode|Line|LineMarkerDefs" packages/cora/src/renderer/components/index.ts && ! rg "export \\{ Arrow|RoundedNode|CylinderNode|CloudNode|DiamondNode|HexagonNode" packages/cora/src/renderer/components/index.ts && rg "./components/index.js" packages/cora/src/renderer/Diagram.tsx && cd packages/cora && bun run typecheck && bun run test:golden</automated>
  </verify>
</task>

<task type="execute" id="3.2-03-03">
  <name>Verify public package component export artifact</name>
  <read_first>
    - packages/cora/package.json
    - packages/cora/tsdown.config.ts
    - packages/cora/src/renderer/components/index.ts
    - packages/cora/src/index.ts
  </read_first>
  <action>
Build the package and verify the component subpath declaration file reflects the new public catalog and not the old public shape model.

Do not add component exports to `packages/cora/src/index.ts`. The public component import target remains `cora/renderer/components`.
  </action>
  <acceptance_criteria>
- `cd packages/cora && bun run build` exits 0.
- `packages/cora/dist/renderer/components/index.d.ts` exists.
- `packages/cora/dist/renderer/components/index.d.ts` contains `Line`.
- `packages/cora/dist/renderer/components/index.d.ts` contains `BoxStyleProps`.
- `packages/cora/dist/renderer/components/index.d.ts` contains `IconNode`.
- `packages/cora/dist/renderer/components/index.d.ts` does not contain `Arrow`.
- `packages/cora/dist/renderer/components/index.d.ts` does not contain `CylinderNode`, `CloudNode`, `DiamondNode`, or `HexagonNode`.
- `packages/cora/src/index.ts` does not contain `BoxStyleProps`, `Line`, `IconNode`, `PageNode`, or `IssueNode`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && bun run build && test -f dist/renderer/components/index.d.ts && rg "Line|BoxStyleProps|IconNode" dist/renderer/components/index.d.ts && ! rg "Arrow|CylinderNode|CloudNode|DiamondNode|HexagonNode" dist/renderer/components/index.d.ts && cd /Users/isa.goksu/Projects/playgrounds/node/cora && ! rg "BoxStyleProps|Line|IconNode|PageNode|IssueNode" packages/cora/src/index.ts</automated>
  </verify>
</task>

</tasks>

<verification>
Run from the repository root:

```bash
cd packages/cora
bun run typecheck
bun run build
bun run test:golden
bun run test
```
</verification>

<success_criteria>
- Current renderer uses `Line` for edge rendering.
- Marker definitions are centralized in SVG `<defs>`.
- `Arrow` is no longer public.
- Old shape-specific node components are no longer public.
- Package subpath declarations expose the new catalog.
- Existing examples still render and golden tests remain green.
</success_criteria>
