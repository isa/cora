# Phase 3.7 Plan 02 Summary - Generic Built-in Icons and Preview Lock-down

Executed all tasks in `03.7-02-PLAN.md` to introduce the default generic SVG icons set (`server`, `database`, `cloud`, `network`, `user`), wire them into the semantic validator and Diagram renderer, and update the preview workbench inspector with picker controls.

## Modified & Created Files

- [packages/cora/src/renderer/components/defaultIcons.tsx](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/src/renderer/components/defaultIcons.tsx) (Created)
  - Implemented 5 generic SVG icon components (`ServerIcon`, `DatabaseIcon`, `CloudIcon`, `NetworkIcon`, `UserIcon`) following the `SvgIconProps` interface.
  - Exposed `DEFAULT_ICON_REGISTRY` (the 5 generic icons) and `BUILTIN_ICON_REGISTRY` (combining generic icons and the 4 issue icons `bug`, `warning`, `error`, `stop`).

- [packages/cora/src/renderer/components/index.ts](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/src/renderer/components/index.ts)
  - Publicly exported the default icon components and registries from the barrel file.

- [packages/cora/src/core/errors.ts](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/src/core/errors.ts)
  - Updated validation logic: `provider: default` is now always treated as an installed extension (no external registry package required).
  - Validated that services under `provider: default` belong to the 9 known default/issue service names, flagging unrecognized names with `UNKNOWN_SERVICE` errors.

- [packages/cora/src/renderer/Diagram.tsx](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/src/renderer/Diagram.tsx)
  - Removed the hardcoded `WarningIcon` in `<IconNode>` and `<LabelIconNode>` rendering branches.
  - Added dynamic resolution via `resolveNodeIcon` utilizing `BUILTIN_ICON_REGISTRY` matching `node.provider` and `node.service`.

- [packages/cora/src/preview/controls/defaults.ts](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/src/preview/controls/defaults.ts)
  - Configured `iconName` control definition as a select-display enum option presenting all 9 built-in icons.
  - Restored `iconType` prop typing to `'ok' | 'nok' | 'question-mark'` (for status indicators) to prevent type errors across geometry and serialization.

- [packages/cora/src/preview/pack/builtins.tsx](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/src/preview/pack/builtins.tsx)
  - Modified preview defaults to start with `iconName: 'database'` and retrieve fallback icons dynamically.

- [packages/cora/src/preview/components/WorkbenchCanvas.tsx](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/src/preview/components/WorkbenchCanvas.tsx)
  - Updated Canvas component rendering to resolve the selected icon name from controls.

- [packages/cora/tests/renderer/default-icons.test.ts](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/tests/renderer/default-icons.test.ts) (Created)
  - Added unit tests asserting registry completeness, correct validator behavior on both valid and invalid default provider configs, and basic icon rendering checks.

- [packages/cora/tests/preview/controls.test.ts](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/tests/preview/controls.test.ts)
  - Updated controls tests to match the new enum structure for `iconName` controls.

## Verification Log

1. **Typecheck**: `bun run typecheck` passes cleanly without errors.
2. **Build**: `bun run build` compiled all workspaces successfully.
3. **Tests**: All 238 Vitest tests passed successfully, including new validations and component smoke tests. Obsolete snapshots were updated and cleaned.
4. **Integration**: Verified parsing behavior on `examples/valid/minimal.yaml` works flawlessly.
