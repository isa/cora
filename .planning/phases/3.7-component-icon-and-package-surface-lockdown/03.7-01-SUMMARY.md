# Phase 3.7 Plan 01 Summary

Removed preview from the published package surface while keeping it in development checkouts.

## Accomplished

- Conditional preview CLI registration via dynamic import when `packages/cora/src` exists.
- Moved `vite` from `dependencies` to `devDependencies`.
- Preview SPA builds to `preview-dist/` (outside `dist/`); added to `.gitignore`.
- Preview server resolves `src/preview`, then `preview-dist`.
- Separate `dist/commands/preview.js` entry; CLI bundle no longer includes Vite (~2.8MB removed).

## Verification

- `bun run build`, `bun run typecheck`, `bun x vitest run` pass.
- `dist/preview/` absent; `preview-dist/index.html` present.
- `cora validate` / `cora render` work; `cora preview --help` works in dev checkout.
