# Phase 3.7 Plan 01 Summary - Component Icon and Package Surface Lockdown

Executed all tasks in `03.7-01-PLAN.md` to remove the preview workbench from the distributed package surface, conditionalize CLI preview command registration, and isolate Vite and preview assets from the production bundle.

## Modified Files

- [packages/cora/src/cli/index.ts](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/src/cli/index.ts)
  - Removed top-level static import of `registerPreviewCommand` to prevent eagerly loading `vite` during other CLI commands.
  - Added filesystem-based `isDevEnvironment()` check looking for the `src/` checkout directory relative to the CLI entry point.
  - Wrapped CLI registration in an `async function main()`, conditionally dynamically importing and registering the preview command only in development environments.

- [packages/cora/package.json](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/package.json)
  - Relocated `"vite": "^8.0.14"` from `dependencies` to `devDependencies`.
  - Updated the `build` script to target `--outDir ../../preview-dist --emptyOutDir` instead of targeting `dist/preview`.

- [.gitignore](file:///Users/isa.goksu/Projects/playgrounds/node/cora/.gitignore)
  - Added `preview-dist` output folder to prevent Git tracking of development-only built SPA assets.

- [packages/cora/src/preview/server.ts](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/src/preview/server.ts)
  - Updated `packageRoot` candidates to search for built assets in `preview-dist` instead of `dist/preview` while maintaining the direct source `src/preview` fallback.

- [packages/cora/tsdown.config.ts](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/tsdown.config.ts)
  - Added `deps: { neverBundle: ['vite'] }` to ensure `vite` and its optional dependencies (like `rolldown` and its native platform bindings) are not bundled into `dist/cli.js`, preventing runtime native binding load failures when dynamic import resolves.

- [packages/cora/tests/preview/package.test.ts](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/tests/preview/package.test.ts)
  - Redirected `previewDist` test resolution directory to `preview-dist/` to match the new build target.

- [packages/cora/src/types/external.d.ts](file:///Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora/src/types/external.d.ts)
  - Declared module `elkjs/lib/elk-worker.min.js?url` to fix a pre-existing TypeScript compiler error and enable clean `tsc --noEmit` checks.

## Verification Log

1. **Clean build**: `bun run build` built `preview-dist/index.html` successfully and cleaned any previous `dist/preview`.
2. **Typecheck**: `bun run typecheck` exited `0` with no errors.
3. **Tests**: All 228 vitest tests passed successfully, including CLI help test (`packages/cora/tests/preview/package.test.ts`) which asserts the availability of `preview --help` options in dev environments.
4. **Integration**: Verified command behaviors on `examples/valid/minimal.yaml`:
   - `node packages/cora/dist/cli.js validate examples/valid/minimal.yaml --format json` successfully printed `[]`.
   - `node packages/cora/dist/cli.js render examples/valid/minimal.yaml` successfully rendered the plain text diagram.
