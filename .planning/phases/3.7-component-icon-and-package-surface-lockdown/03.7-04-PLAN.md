---
phase: 3.7-component-icon-and-package-surface-lockdown
plan: 04
type: execute
wave: 3
depends_on: ["03.7-01", "03.7-02", "03.7-03"]
files_modified:
  - AGENTS.md
  - README.md
  - SKILL.md
  - packages/cora/src/renderer/components/index.ts
autonomous: true
requirements:
  - PACK-03
  - PACK-07
  - PACK-08
requirements_addressed:
  - PACK-03
  - PACK-07
  - PACK-08
decision_refs:
  - D-09

must_haves:
  truths:
    - "D-09: ./renderer/components is a supported public subpath export — documented in AGENTS.md"
    - "AGENTS.md Preview workbench section describes preview as a development tool, not an installed user feature"
    - "AGENTS.md Renderer Components section documents the default icon set and built-in provider contract"
    - "README.md no longer presents cora preview as a user-installable workflow"
    - "SKILL.md reflects the supported shipped surface (no preview references)"
    - "Public/internal API boundary is explicitly documented"
  artifacts:
    - path: AGENTS.md
      provides: Updated agent docs reflecting package surface lockdown
    - path: README.md
      provides: Updated README reflecting shipped commands and icon set
    - path: SKILL.md
      provides: Updated skill docs reflecting shipped surface
---

## Phase Goal

**As an** AI agent or human user reading Cora docs, **I want** README, AGENTS.md, and SKILL.md to accurately describe only the supported shipped surface, **so that** I know what's available after `npm install cora` and what requires a development checkout.

<objective>
Reconcile all documentation to reflect the Phase 3.7 package surface: preview is development-only, default icons are built-in, public component API is documented, and CLI help matches shipped commands.

Purpose: Ensure docs match the actual installable package and don't advertise development-only features.
Output: Updated AGENTS.md, README.md, SKILL.md.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/3.7-component-icon-and-package-surface-lockdown/03.7-CONTEXT.md
@AGENTS.md
@README.md
@SKILL.md
</context>

<tasks>

<task type="auto" id="3.7-04-01">
  <name>Update AGENTS.md: preview section, default icons, public API</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md
    - packages/cora/src/renderer/components/index.ts
    - packages/cora/src/renderer/components/defaultIcons.tsx
  </read_first>
  <action>
Update AGENTS.md with the following changes:

1. **Preview workbench section** (lines 29-39): Reframe `cora preview` as a **development-only** tool. Update to clearly state:
   - `cora preview` is available only in development checkouts (not after `npm install cora`)
   - It is not part of the shipped package surface
   - It remains useful for component development and visual testing during Cora development

2. **Quick start section** (lines 22-23): Remove or move `cora preview` from the installed-user quick start examples. Move it to a separate "Development" section or add a clear note that it requires a source checkout.

3. **Renderer Components section** (around line 255): Add documentation for the default icon set:
   ```markdown
   ## Default Icons

   Cora ships a built-in set of generic icons under the `default` provider:

   | Service Name | Icon | Purpose |
   |-------------|------|---------|
   | `server` | Server/rack | Generic server or compute |
   | `database` | Cylinder | Database or data store |
   | `cloud` | Cloud | Cloud service or platform |
   | `network` | Connected nodes | Network or connectivity |
   | `user` | Person | User or actor |
   | `bug` | Circle crosshairs | Bug/defect |
   | `warning` | Triangle ! | Warning |
   | `error` | Circle X | Error |
   | `stop` | Octagon | Stop/blocked |

   Use in YAML:
   ```yaml
   nodes:
     - id: db
       label: PostgreSQL
       component: icon
       provider: default
       service: database
   ```
   ```

4. **Public API section**: Add or update a section documenting the public package exports:
   ```markdown
   ## Package Exports

   | Subpath | Purpose |
   |---------|---------|
   | `cora` | Main library (validate, render, schema) |
   | `cora/core` | Core validation and layout engine |
   | `cora/renderer` | SVG renderer |
   | `cora/renderer/components` | Typed React component catalog for extensions |
   ```

5. Update the Default Look section if it references preview sharing — clarify that `catalogDefaultProps()` is shared between renderer and development preview, not shipped preview.
  </action>
  <acceptance_criteria>
- AGENTS.md preview section describes it as development-only
- AGENTS.md documents default icon set with YAML usage example
- AGENTS.md documents public package exports
- cora preview is not in the quick start as a user-installable command
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "development" AGENTS.md | head -5 && rg "default.*provider\|provider.*default" AGENTS.md && rg "Package Exports\|cora/renderer/components" AGENTS.md</automated>
  </verify>
  <done>AGENTS.md reflects the locked package surface with default icons and public API docs</done>
</task>

<task type="auto" id="3.7-04-02">
  <name>Update README.md: remove preview as user feature, add icon docs</name>
  <files>README.md</files>
  <read_first>
    - README.md
  </read_first>
  <action>
Update README.md with the following changes:

1. **Status table** (line 14): Update the Phase 3.3 row to clarify preview is a development tool, not a shipped feature. Consider adding a Phase 3.7 row: "Phase 3.7 — Package Surface Lockdown | Complete | Built-in icons, clean package surface"

2. **Quick Start** (lines 74-75): Remove `cora preview` from the main quick start examples or move it to a "Development" section.

3. **Preview command section** (lines 182-198): Either:
   a. Remove the full preview command documentation section, OR
   b. Move it to a "Development" section with a clear heading like "## Development Tools" and a note that preview requires a source checkout

4. Add a new section for default icons:
   ```markdown
   ## Built-in Icons

   Cora includes a set of generic icons under the `default` provider that work without extensions:

   ```bash
   # Use in your diagram YAML
   nodes:
     - id: api
       label: API Server
       component: icon
       provider: default
       service: server
   ```

   Available: `server`, `database`, `cloud`, `network`, `user`, `bug`, `warning`, `error`, `stop`
   ```

5. Ensure the "Today" summary (line 18) no longer mentions preview as a shipped feature.
  </action>
  <acceptance_criteria>
- README.md does not present cora preview as a user-installable feature in main sections
- README.md documents built-in default icons
- README.md has a clear separation between user features and development tools
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "Built-in Icons\|default.*provider" README.md && rg -c "cora preview" README.md</automated>
  </verify>
  <done>README.md reflects shipped surface with icons and development-only preview</done>
</task>

<task type="auto" id="3.7-04-03">
  <name>Update SKILL.md to reflect shipped surface</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md
  </read_first>
  <action>
Update the root SKILL.md:

1. Ensure it does not reference `cora preview` as an available command after installation.

2. If it mentions preview, either remove the reference or clearly note it's development-only.

3. Add a brief note about the default icon set:
   ```
   ## Built-in Icons

   Use `provider: default` with service names `server`, `database`, `cloud`,
   `network`, `user`, `bug`, `warning`, `error`, `stop` for icon and labelIcon
   components without requiring extensions.
   ```

4. Ensure the skill file describes the actual post-install experience accurately.
  </action>
  <acceptance_criteria>
- SKILL.md does not present cora preview as an installed command
- SKILL.md mentions the default icon set
- SKILL.md accurately describes the post-install CLI surface
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "provider.*default\|Built-in Icons\|default.*icon" SKILL.md</automated>
  </verify>
  <done>SKILL.md reflects the shipped package surface</done>
</task>

<task type="auto" id="3.7-04-04">
  <name>Final build, typecheck, and full test suite verification</name>
  <files></files>
  <read_first>
    - packages/cora/package.json
  </read_first>
  <action>
Run the complete verification pipeline:

1. `bun run build` — full build with tsdown + vite preview + asset copy
2. `bun run typecheck` — TypeScript type checking
3. `bun x vitest run` — full test suite (unit + smoke + golden)
4. `bun run cora validate examples/valid/minimal.yaml --format json` — CLI validate works
5. `bun run cora render examples/valid/minimal.yaml` — CLI text render works
6. `bun run cora schema` — CLI schema export works
7. Verify `packages/cora/dist/preview/` does NOT exist
8. Verify `packages/cora/preview-dist/index.html` exists (for dev use)
9. Verify tarball test passes: `bun x vitest run tests/smoke/package.test.ts`

Fix any issues discovered during this sweep.
  </action>
  <acceptance_criteria>
- bun run build exits 0
- bun run typecheck exits 0
- bun x vitest run exits 0
- All three CLI commands (validate, render, schema) work
- dist/preview/ does not exist; preview-dist/ exists
- Tarball smoke test passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run build && cd packages/cora && bun run typecheck && bun x vitest run --reporter=verbose 2>&1 | tail -30 && test ! -d dist/preview && test -f ../../preview-dist/index.html 2>/dev/null || test -f preview-dist/index.html</automated>
  </verify>
  <done>Full verification pass — build, types, tests, CLI, and package surface all green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → user expectations | Docs define what users expect to be available; must match reality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.7-09 | Information Disclosure | Stale docs advertising preview | mitigate | Docs sweep removes preview from user-facing sections |
| T-3.7-10 | Spoofing | Missing icon docs | mitigate | Default icon table in AGENTS.md and README.md |
| T-3.7-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run build && cd packages/cora && bun run typecheck && bun x vitest run
</verification>

<success_criteria>
- All documentation accurately reflects the shipped package surface
- Default icon set documented for agents and users
- Preview clearly marked as development-only across all docs
- Public API exports documented
- Full build + test + CLI verification passes
</success_criteria>

<output>
Create `.planning/phases/3.7-component-icon-and-package-surface-lockdown/03.7-04-SUMMARY.md` when done
</output>
