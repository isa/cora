# Phase 3.7 - Plan 03 Summary

All tasks under `03.7-03-PLAN.md` have been implemented and verified successfully.

## Accomplishments

### 1. Created Tarball Integrity Test (`packages/cora/tests/smoke/package.test.ts`)
- Programmatically packs the local package using `npm pack` into a temporary workspace directory `.tmp/`.
- Extracts the generated tarball and recursively maps the packaged files.
- Asserts that all **Required** files exist:
  - `dist/cli.js` (CLI entry point)
  - `dist/index.js` (Main library)
  - `dist/core/index.js` (Core package subpath)
  - `dist/renderer/components/index.js` (Renderer components)
  - Bundled Noto Sans fonts (`dist/renderer/assets/fonts/NotoSans-Regular.ttf` and `NotoSans-SemiBold.ttf`)
  - `SKILL.md` (Agent skill guide)
  - `package.json`
- Asserts that **Forbidden** directories and configurations do NOT leak into the package:
  - Static preview SPA assets/directories (`preview/`, `preview-dist/`)
  - Dev/Source files (`src/`, `.planning/`, `node_modules/`, `**/*.ts` except `.d.ts`/`.d.ts.map`)
  - Config files (`vite.config*`, `tsdown.config*`)
- Evaluates and asserts that compressed and uncompressed sizes are under `5MB`.
- Deletes the temporary directories in `afterAll`.

### 2. Updated Shell Smoke Test (`packages/cora/tests/smoke/clean-install.sh`)
- Removed the old assertions for `dist/preview/index.html` and `cora preview --help`.
- Added a package integrity assertion validating that `dist/preview` does NOT exist in the installed package directory structure.
- Cleaned up comments and status messages to accurately reflect the preview asset exclusion.

## Verification Results

We verified that the newly added test suite and all existing tests pass smoothly:
- `bun run build && bun x vitest run tests/smoke/package.test.ts` passed successfully.
- Ran the suite via Vitest which reported **226 passed, 1 skipped, 0 failed**.
- Extracted and verified tarball contents containing only allowed files.
