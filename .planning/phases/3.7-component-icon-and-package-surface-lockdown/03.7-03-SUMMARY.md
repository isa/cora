# Phase 3.7 Plan 03 Summary

Added automated tarball integrity checks and updated clean-install smoke for preview exclusion.

## Accomplished

- Created `tests/smoke/package.test.ts` (required files, forbidden dev paths, no `dist/commands`).
- Updated `clean-install.sh` to assert `dist/preview/` is absent from installed package.
- `prepack` strips `dist/commands` and `dist/preview-*.js` before `npm pack`.
- Added `.npmignore` for documentation; `prepack` is the enforcement path.

## Verification

- `bun x vitest run tests/smoke/package.test.ts` passes.
- `bash packages/cora/tests/smoke/clean-install.sh` passes.
