---
phase: "03.5"
plan: "01"
subsystem: "preview"
tags: [preview, ui, shell, visual-system]
requires:
  - ".planning/phases/3.5-preview-visual-beauty/03.5-01-PLAN.md"
provides:
  - "Preview-local black/white/purple visual tokens"
  - "Reference-style shell foundation"
affects:
  - "packages/cora/src/preview"
tech_stack:
  added: []
  patterns:
    - "Preview-local CSS custom properties"
key_files:
  created: []
  modified:
    - "packages/cora/src/preview/styles.css"
    - "packages/cora/src/preview/App.tsx"
    - "packages/cora/src/preview/components/CatalogSidebar.tsx"
    - "packages/cora/tests/preview/workbench.test.tsx"
key_decisions:
  - "Keep the Phase 3.5 visual language in preview CSS and shell markup, not renderer component defaults."
requirements_completed:
  - BEAUTY-*
duration: "2 min"
completed: "2026-05-23"
---

# Phase 03.5 Plan 01: Preview Visual Shell Summary

Established the preview-local visual foundation for the NodeFlow-style workbench shell.

## Execution

- Added preview-local CSS tokens for the warm background, white surfaces, ink, muted text, border, purple accent, soft accent, destructive color, and shadow.
- Restyled the app shell into a three-zone desktop composition with soft rounded side panels and a dotted center workbench field.
- Demoted the old instruction strip to a quiet canvas hint and removed the long technical instruction sentence.
- Updated the catalog header to render prominent `Cora` identity and `Library / Components`.
- Updated shell-level preview tests for the new branding and copy contract.

## Verification

- `rg -- '--preview-accent: #6D28D9|--preview-bg: #FBFAF8|prefers-reduced-motion|border-radius: 24px' packages/cora/src/preview/styles.css` passed.
- `! rg "Select a canvas node to edit attributes\\. Add two nodes to inspect the connection\\." packages/cora/src/preview/App.tsx` passed.
- `cd packages/cora && bun x vitest run tests/preview` passed: 7 files, 26 tests.
- `rg "Library / Components|Cora" packages/cora/tests/preview/workbench.test.tsx` passed.
- `! rg "Select a canvas node to edit attributes" packages/cora/tests/preview/workbench.test.tsx` passed.
- `cd packages/cora && bun x vitest run tests/preview/workbench.test.tsx` passed: 3 tests.
- `cd packages/cora && bun run typecheck` passed.

## Deviations from Plan

- Added a minimal `CatalogSidebar.tsx` header update during Plan 01 so the shell and tests could render the required `Cora` and `Library / Components` markers before the fuller catalog polish in Plan 02.

**Total deviations:** 1 auto-fixed scope expansion. **Impact:** Low; the change is preview-local and aligns with Plan 02's direction.

## Self-Check: PASSED

Ready for 03.5-02.
