---
phase: 3.5-preview-visual-beauty
plan: 02
type: execute
wave: 2
depends_on:
  - 03.5-01
files_modified:
  - packages/cora/src/preview/components/CatalogSidebar.tsx
  - packages/cora/src/preview/components/NodePropPanel.tsx
  - packages/cora/src/preview/components/ConnectionPanel.tsx
  - packages/cora/src/preview/components/GroupPanel.tsx
  - packages/cora/src/preview/components/ControlInput.tsx
  - packages/cora/src/preview/styles.css
  - packages/cora/tests/preview/workbench.test.tsx
autonomous: true
requirements:
  - BEAUTY-*
requirements_addressed:
  - BEAUTY-*
tags: [preview, catalog, inspector, controls]
decision_refs:
  - D-03
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
  - D-18
  - D-19

must_haves:
  truths:
    - "D-03: Left panel is a branded library panel with prominent Cora identity, `Library / Components`, segmented navigation, polished element list, and a real mapped action only if available."
    - "D-11: Catalog uses reference-style larger polished rows, icon tiles, clear human labels, and `Diagram Elements` grouping."
    - "D-12: Search may remain, but it must not dominate the first impression."
    - "D-13: Inspector uses selected-component identity with icon tile, title, small pill metadata, visual `Inspector / Style` tabs, and grouped controls."
    - "D-14: `Inspector / Style` tabs are visual-only unless real tabs are trivial and low-risk."
    - "D-15: Controls use premium form styling: larger rounded inputs, clean labels, whitespace, swatches, toggles, black/destructive actions."
    - "D-18: Typography is large and clean."
    - "D-19: Visible generated technical IDs such as `node-1` are absent from primary UI."
  artifacts:
    - path: "packages/cora/src/preview/components/CatalogSidebar.tsx"
      provides: "Branded left library panel"
      contains: "Library / Components"
    - path: "packages/cora/src/preview/components/NodePropPanel.tsx"
      provides: "Reference-style node inspector"
      contains: "Inspector"
---

<objective>
Rebuild the left catalog and right inspector into the premium NodeFlow-style presentation while preserving existing preview controls and state behavior.
</objective>

<decision_coverage>
- D-03, D-11, D-12: Catalog becomes branded and reference-style without making search dominant or adding fake actions.
- D-13, D-14, D-15: Inspector gets identity, visual tabs, and premium controls without requiring real tab partitioning.
- D-18, D-19: Typography is clean and visible generated IDs leave the primary UI.
</decision_coverage>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-3.5-04: Branded catalog adds fake actions | medium | Add bottom action only if it maps to real preview behavior; otherwise omit. |
| T-3.5-05: Inspector hides controls behind decorative identity | medium | Keep all current controls rendered in one accessible flow below the identity header. |
| T-3.5-06: Removing visible node IDs breaks state/tests | low | Remove IDs only from primary UI text; keep DOM/state IDs and update tests to assert behavior instead of visible IDs. |
</threat_model>

<tasks>

<task type="execute" id="3.5-02-01">
  <name>Implement branded reference-style catalog panel</name>
  <read_first>
    - .planning/phases/3.5-preview-visual-beauty/03.5-CONTEXT.md
    - .planning/phases/3.5-preview-visual-beauty/03.5-UI-SPEC.md
    - .planning/phases/3.5-preview-visual-beauty/03.5-PATTERNS.md
    - packages/cora/src/preview/components/CatalogSidebar.tsx
    - packages/cora/src/preview/styles.css
    - packages/cora/tests/preview/workbench.test.tsx
  </read_first>
  <action>
Update `CatalogSidebar.tsx` to render a branded library panel:

- title text `Cora` or `Cora Preview`
- metadata text `Library / Components`
- visual segmented control with labels `Lib` and `Nodes`; it may be non-stateful if no real second tab exists
- section heading `Diagram Elements`
- larger component rows using existing `catalogItems(state)` data
- human component labels from existing component labels, with any `Node` suffix removed for visible copy only
- keep `filterComponents(state, query)` and drag data behavior intact
- search may remain but should be visually secondary to the branded library and element list

Update CSS for `.component-tile`, `.component-icon`, `.component-copy`, and catalog section classes to match the UI-SPEC premium row treatment.
  </action>
  <acceptance_criteria>
- `CatalogSidebar.tsx` renders `Library / Components`.
- `CatalogSidebar.tsx` renders `Diagram Elements`.
- `CatalogSidebar.tsx` contains `Lib` and `Nodes`.
- `filterComponents(createDefaultWorkbenchState(), 'issue')` still returns an item whose label is `IssueNode` at the data level or the test is intentionally adjusted to validate visible human copy separately.
- `cd packages/cora && bun x vitest run tests/preview/workbench.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "Library / Components|Diagram Elements|Lib|Nodes" packages/cora/src/preview/components/CatalogSidebar.tsx && cd packages/cora && bun x vitest run tests/preview/workbench.test.tsx</automated>
  </verify>
</task>

<task type="execute" id="3.5-02-02">
  <name>Implement reference-style inspector identity and premium controls</name>
  <read_first>
    - packages/cora/src/preview/components/NodePropPanel.tsx
    - packages/cora/src/preview/components/ConnectionPanel.tsx
    - packages/cora/src/preview/components/GroupPanel.tsx
    - packages/cora/src/preview/components/ControlInput.tsx
    - packages/cora/src/preview/components/ui/input.tsx
    - packages/cora/src/preview/components/ui/select.tsx
    - packages/cora/src/preview/components/ui/toggle.tsx
    - packages/cora/src/preview/styles.css
    - packages/cora/tests/preview/workbench.test.tsx
  </read_first>
  <action>
Update inspector components so every selected item type has a reference-style identity area:

- node inspector: icon tile, human component title, optional active/status pill, visual tabs `Inspector` and `Style`, then existing grouped controls
- connection inspector: identity title `Connection`, visual tabs, then existing Line/Markers/Routing controls
- group inspector: identity title `Group`, visual tabs, then existing group controls
- no primary visible `node-1`, `node-2`, or generated ID role label

Update `ControlInput.tsx` and CSS so fields use premium form styling:

- larger 12px radius inputs/selects/textareas
- color controls styled as swatches where feasible
- boolean controls styled as elegant toggles while preserving checkbox semantics or accessible equivalent
- clear uppercase labels and comfortable spacing

Do not hide existing controls behind real tabs in this plan.
  </action>
  <acceptance_criteria>
- `NodePropPanel.tsx` contains `Inspector` and `Style`.
- `NodePropPanel.tsx` does not render `node.id` as primary visible text.
- `ConnectionPanel.tsx` still renders controls for `lineStyle`, `strokeColor`, `strokeWidth`, `startMarker`, and `endMarker`.
- `GroupPanel.tsx` still renders group label/size controls.
- `ControlInput.tsx` still handles `enum`, `number`, `color`, `boolean`, `size`, and text controls.
- `cd packages/cora && bun x vitest run tests/preview` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "Inspector|Style" packages/cora/src/preview/components/NodePropPanel.tsx && rg "lineStyle|strokeColor|strokeWidth|startMarker|endMarker" packages/cora/src/preview/components/ConnectionPanel.tsx && rg "enum|number|color|boolean|size" packages/cora/src/preview/components/ControlInput.tsx && cd packages/cora && bun x vitest run tests/preview</automated>
  </verify>
</task>

</tasks>

<verification>
Run:

```bash
cd packages/cora
bun run typecheck
bun x vitest run tests/preview
```
</verification>

<success_criteria>
- Left catalog reads as a branded Cora library with reference-style element rows.
- Right inspector reads as a premium identity-first inspector, not a raw form.
- Existing preview controls remain available and test-covered.
- Visible generated node IDs are removed from primary UI.
</success_criteria>
