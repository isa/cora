---
phase: "03.5"
plan: "04"
subsystem: "preview"
tags: [preview, ui, qa, visual-contract]
requires:
  - ".planning/phases/3.5-preview-visual-beauty/03.5-04-PLAN.md"
provides:
  - "Preview visual contract test coverage"
  - "Desktop and narrow screenshot QA"
affects:
  - "packages/cora/tests/preview"
  - "packages/cora/src/preview"
tech_stack:
  added: []
  patterns:
    - "Structural visual contract tests"
key_files:
  created:
    - "packages/cora/tests/preview/visual-contract.test.tsx"
  modified:
    - "packages/cora/src/preview/App.tsx"
    - "packages/cora/src/preview/styles.css"
key_decisions:
  - "Visual tests assert visible text and stable tokens, not exact DOM snapshots."
  - "Generated IDs may remain in internal DOM attributes, but not visible primary chrome."
requirements_completed:
  - BEAUTY-*
duration: "6 min"
completed: "2026-05-23"
---

# Phase 03.5 Plan 04: Visual Contract And QA Summary

Locked the Phase 3.5 preview polish behind automated visual contract tests and browser screenshot QA.

## Execution

- Added `visual-contract.test.tsx` for the branded catalog, inspector tabs, split tools, absence of visible `Labels`, absence of visible `node-1`/`node-2`, and stable preview color tokens.
- Moved `Inspector / Attributes` and visual `Inspector` / `Style` tabs into the shared inspector panel so the first viewport matches the reference even with no selection.
- Adjusted side panels and workbench height to `calc(100vh - 32px)` with internal scrolling so split floating tools remain visible in the desktop first viewport.
- Tightened the visual match after screenshot review: removed search from the primary catalog, switched catalog icons to white square tiles with black glyphs, changed visible element names toward the reference vocabulary, removed duplicate selected-inspector chrome, and corrected the oversized no-selection `Attributes` header.
- Confirmed `packages/cora/src/index.ts` still has no preview public export.
- Captured desktop and narrow screenshots:
  - `/private/tmp/cora-preview-desktop.png`
  - `/private/tmp/cora-preview-narrow.png`
  - `/private/tmp/cora-preview-fontfix.png`
  - `/private/tmp/cora-preview-selected-fontfix.png`

## Verification

- `rg "Library / Components|Diagram Elements|Inspector|Style|node-1|Labels|#6D28D9" packages/cora/tests/preview packages/cora/src/preview` passed.
- `cd packages/cora && bun x vitest run tests/preview` passed: 8 files, 29 tests.
- `! rg "preview" packages/cora/src/index.ts` passed.
- `cd packages/cora && bun x vitest run tests/preview/package.test.ts` passed: 2 tests.
- `cd packages/cora && bun run typecheck` passed.
- `cd packages/cora && bun run build` passed.
- Browser QA passed at 1440x1000:
  - branded Cora library present
  - `Library / Components` and `Diagram Elements` present
  - inspector and style tabs present
  - two split floating tool groups visible in viewport
  - no visible `Labels`
  - no visible `node-1`/`node-2`
  - no obvious overlap in the screenshot
- Browser QA passed at 390x900:
  - no horizontal overflow
  - branded catalog remains readable
  - no visible `Labels`
  - no visible `node-1`/`node-2`
- Follow-up screenshot QA after correction passed:
  - no-selection inspector title is `No Selection`, not oversized generic `Attributes`
  - selected-node inspector has one tab strip and no duplicate top header
  - catalog rows use reference-style white icon tiles and neutral glyphs
  - preview tests remained green after the correction

## Deviations from Plan

- Browser plugin automation was unavailable in this runtime, so final browser QA used the repo's Playwright package as a fallback. Playwright Chromium had to be installed before screenshots could be captured.
- User screenshot review found the first visual pass still too far from the reference, especially typography, catalog row behavior, and lavender circular icon treatment. A follow-up correction commit tightened those items before phase close-out.

**Total deviations:** 2. **Impact:** Positive; screenshot review caught visual gaps that structural tests did not, and the final implementation is closer to the target reference while retaining passing automated checks.

## Self-Check: PASSED

Phase complete, ready for verification or Phase 3.6 discussion/planning.
