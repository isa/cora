---
phase: 3.5-preview-visual-beauty
plan: 03
type: execute
wave: 2
depends_on:
  - 03.5-01
files_modified:
  - packages/cora/src/preview/components/WorkbenchCanvas.tsx
  - packages/cora/src/preview/components/AttachmentOverlay.tsx
  - packages/cora/src/preview/styles.css
  - packages/cora/tests/preview/workbench.test.tsx
  - packages/cora/tests/preview/geometry.test.ts
autonomous: true
requirements:
  - BEAUTY-*
requirements_addressed:
  - BEAUTY-*
tags: [preview, canvas, toolbar, motion]
decision_refs:
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-20

must_haves:
  truths:
    - "D-06: Canvas emphasizes a premium dotted workspace, soft depth, floating nodes, and quiet non-boxy boundary."
    - "D-07: Selection chrome is minimal and elegant with thin outlines, small neutral handles, and low visual noise."
    - "D-08: Empty canvas is mostly open dotted space with subtle centered hint and no sample flow by default."
    - "D-09: Canvas controls are split into smaller floating tool groups rather than one dominant pill."
    - "D-10: Visible `Labels` toolbar control is removed from primary canvas chrome."
    - "D-20: Motion is expressive but inspection-safe and respects reduced motion."
  artifacts:
    - path: "packages/cora/src/preview/components/WorkbenchCanvas.tsx"
      provides: "Premium canvas surface and split floating tools"
      contains: "canvas-tool-group"
---

<objective>
Rework the preview canvas surface and toolbar chrome into the premium dotted studio experience while preserving drag, pan, zoom, selection, connection, and overlay behavior.
</objective>

<decision_coverage>
- D-06, D-08: Canvas becomes a premium dotted workspace with quiet empty state.
- D-07: Selection chrome stays minimal and low-noise.
- D-09, D-10: Toolbar becomes split floating tools and removes visible `Labels`.
- D-20: Canvas and tool motion is expressive but test-stable and reduced-motion-safe.
</decision_coverage>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-3.5-07: Styling changes break SVG coordinate mapping | medium | Keep `CANVAS_WIDTH`, `CANVAS_HEIGHT`, `viewBox`, and `toCanvasPoint` behavior intact. |
| T-3.5-08: Removing visible Labels hides needed debug capability entirely | low | Keep overlay labels hidden by default or debug-only if needed; remove only primary toolbar exposure. |
| T-3.5-09: Motion delays connection geometry | medium | Animate visual chrome only; connection points and line paths update immediately from state. |
</threat_model>

<tasks>

<task type="execute" id="3.5-03-01">
  <name>Convert canvas surface to premium dotted workspace</name>
  <read_first>
    - .planning/phases/3.5-preview-visual-beauty/03.5-CONTEXT.md
    - .planning/phases/3.5-preview-visual-beauty/03.5-UI-SPEC.md
    - packages/cora/src/preview/components/WorkbenchCanvas.tsx
    - packages/cora/src/preview/components/AttachmentOverlay.tsx
    - packages/cora/src/preview/styles.css
    - packages/cora/tests/preview/geometry.test.ts
  </read_first>
  <action>
Update `WorkbenchCanvas.tsx` and `styles.css` so:

- `.canvas-region` / `.preview-canvas` read as a premium dotted studio workspace
- the current hard bordered white SVG board becomes visually quiet and non-boxy
- empty state remains `Drag components here` but is subtle and centered
- selection outline uses thin neutral styling and low visual noise
- side guides, slot dots, and overlay labels remain subtle
- geometry logic, `CANVAS_WIDTH`, `CANVAS_HEIGHT`, `zoomViewBox`, `toCanvasPoint`, and connection computations remain behaviorally unchanged
  </action>
  <acceptance_criteria>
- `styles.css` contains a dotted background implementation for the canvas workspace using `radial-gradient` or equivalent CSS dots.
- `styles.css` contains `.selection-outline` with neutral stroke color, not heavy purple-only chrome.
- `WorkbenchCanvas.tsx` still contains `CANVAS_WIDTH = 760` and `CANVAS_HEIGHT = 520`.
- `cd packages/cora && bun x vitest run tests/preview/geometry.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "radial-gradient|selection-outline" packages/cora/src/preview/styles.css && rg "CANVAS_WIDTH = 760|CANVAS_HEIGHT = 520" packages/cora/src/preview/components/WorkbenchCanvas.tsx && cd packages/cora && bun x vitest run tests/preview/geometry.test.ts</automated>
  </verify>
</task>

<task type="execute" id="3.5-03-02">
  <name>Split floating tools and remove primary Labels control</name>
  <read_first>
    - packages/cora/src/preview/components/WorkbenchCanvas.tsx
    - packages/cora/src/preview/styles.css
    - packages/cora/tests/preview/workbench.test.tsx
  </read_first>
  <action>
Replace the current single top-right `.canvas-toolbar` with split floating tool groups:

- one group for destructive/actions: `Delete`, `Clear`
- one group for zoom: `Zoom out`, `Zoom in` using accessible labels even if visible text is icon-like
- optional third group for settings/debug only if it does not expose `Labels` as primary visible text

Remove the visible `Labels` toggle from primary canvas chrome. If `showOverlayLabels` remains in code, keep it default-hidden or expose it only through a non-primary debug path that does not render visible `Labels` text in the normal workbench markup.

Update tests so `workbench.test.tsx` asserts the absence of visible `Labels` and the presence of accessible delete/clear/zoom controls.
  </action>
  <acceptance_criteria>
- `WorkbenchCanvas.tsx` does not render visible child text `Labels`.
- `packages/cora/tests/preview/workbench.test.tsx` contains `not.toContain('Labels')` or equivalent absence assertion.
- rendered markup still exposes delete, clear, zoom in, and zoom out controls through visible text or `aria-label`.
- `cd packages/cora && bun x vitest run tests/preview/workbench.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && ! rg ">Labels<|Labels</" packages/cora/src/preview/components/WorkbenchCanvas.tsx && rg "Labels" packages/cora/tests/preview/workbench.test.tsx && cd packages/cora && bun x vitest run tests/preview/workbench.test.tsx</automated>
  </verify>
</task>

<task type="execute" id="3.5-03-03">
  <name>Add inspection-safe canvas motion</name>
  <read_first>
    - packages/cora/src/preview/styles.css
    - packages/cora/src/preview/components/WorkbenchCanvas.tsx
    - .planning/phases/3.5-preview-visual-beauty/03.5-UI-SPEC.md
  </read_first>
  <action>
Add CSS transitions/animations for:

- component tile hover/press
- node/selection outline state changes
- floating tool hover/focus
- panel/tab underline or opacity states
- component entry if feasible without delaying state or geometry

Keep all motion declarative and layout-stable. Add `@media (prefers-reduced-motion: reduce)` that removes transform animations and keeps transitions at or below 80ms.
  </action>
  <acceptance_criteria>
- `styles.css` contains `prefers-reduced-motion`.
- `styles.css` contains transition rules for `.component-tile`, `.selection-outline`, and floating tool classes.
- No task adds timers that delay geometry updates in `WorkbenchCanvas.tsx`.
- `cd packages/cora && bun run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "prefers-reduced-motion|component-tile.*transition|selection-outline" packages/cora/src/preview/styles.css && cd packages/cora && bun run typecheck</automated>
  </verify>
</task>

</tasks>

<verification>
Run:

```bash
cd packages/cora
bun run typecheck
bun x vitest run tests/preview
```
</verification>

<success_criteria>
- Canvas visually reads as premium dotted workspace.
- Primary toolbar no longer exposes `Labels`.
- Split floating tools preserve delete, clear, zoom, pan, and selection behavior.
- Motion is present but reduced-motion-safe.
</success_criteria>
