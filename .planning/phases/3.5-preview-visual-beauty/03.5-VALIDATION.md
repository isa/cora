---
phase: 3.5
slug: preview-visual-beauty
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-23
---

# Phase 3.5 — Validation Strategy

> Per-phase validation contract for preview visual polish.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest + TypeScript + Vite build + browser/screenshot check |
| **Config file** | `packages/cora/vitest.config.ts` if present; otherwise package Vitest defaults |
| **Quick run command** | `cd packages/cora && bun x vitest run tests/preview` |
| **Full suite command** | `cd packages/cora && bun run typecheck && bun x vitest run tests/preview && bun run build` |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd packages/cora && bun x vitest run tests/preview`
- **After every plan wave:** Run `cd packages/cora && bun run typecheck && bun x vitest run tests/preview`
- **Before `$gsd-verify-work`:** Full suite plus browser/screenshot verification must be green
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 3.5-01-01 | 01 | 1 | BEAUTY-* | T-3.5-01 / T-3.5-02 | N/A | preview/unit | `cd packages/cora && bun x vitest run tests/preview` | yes | pending |
| 3.5-02-01 | 02 | 2 | BEAUTY-* | T-3.5-03 | N/A | preview/unit | `cd packages/cora && bun x vitest run tests/preview` | yes | pending |
| 3.5-03-01 | 03 | 2 | BEAUTY-* | T-3.5-04 | N/A | preview/unit | `cd packages/cora && bun x vitest run tests/preview` | yes | pending |
| 3.5-04-01 | 04 | 3 | BEAUTY-* | T-3.5-05 | N/A | full/browser | `cd packages/cora && bun run typecheck && bun x vitest run tests/preview && bun run build` | yes | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements:

- `packages/cora/tests/preview/workbench.test.tsx`
- `packages/cora/tests/preview/state.test.ts`
- `packages/cora/tests/preview/geometry.test.ts`
- `packages/cora/tests/preview/package.test.ts`
- `packages/cora/src/preview/server.ts`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| NodeFlow-style visual match | BEAUTY-* | Visual quality cannot be fully proven by source assertions | Serve preview at desktop width, capture screenshot, compare against UI-SPEC screenshot acceptance list. |
| No overlapping text or controls | BEAUTY-* | Requires rendered viewport inspection | Inspect desktop screenshot and one narrow viewport after build/dev server starts. |
| Motion feels premium but not disruptive | BEAUTY-* | Motion quality is experiential | Interact with tile hover/drop, selection, toolbar hover, and panel controls; verify no delayed geometry or layout shift. |

---

## Validation Sign-Off

- [x] All tasks have automated verify or existing test dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all missing references
- [x] No watch-mode flags
- [x] Feedback latency < 90s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-23
