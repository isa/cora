---
phase: 3.5-preview-visual-beauty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cora/src/preview/styles.css
  - packages/cora/src/preview/App.tsx
  - packages/cora/tests/preview/workbench.test.tsx
autonomous: true
requirements:
  - BEAUTY-*
requirements_addressed:
  - BEAUTY-*
tags: [preview, ui, shell, visual-system]
decision_refs:
  - D-01
  - D-02
  - D-04
  - D-05
  - D-16
  - D-17
  - D-18
  - D-20

must_haves:
  truths:
    - "D-01: Preview uses a Focused Studio direction: functional workbench, visually premium, intentional, and canvas-centered."
    - "D-02: Preview follows the NodeFlow-style reference composition with large left library, dotted canvas, right inspector, soft white panels, black action surfaces, and purple accent."
    - "D-04: Center canvas uses a hybrid dotted studio composition while preserving a predictable working boundary."
    - "D-05: Preview remains a workbench, not a landing page or marketing screen."
    - "D-16: Preview UI palette is black/white plus controlled purple accent."
    - "D-17: Preview uses soft premium panels with rounded white surfaces, subtle shadows, light borders, and restrained depth."
    - "D-18: Typography is large and clean, with prominent Cora identity and uppercase section labels."
    - "D-20: Expressive motion is allowed only when it does not interfere with inspection."
  artifacts:
    - path: "packages/cora/src/preview/styles.css"
      provides: "Preview-local visual tokens and shell styling"
      contains: "--preview-accent"
    - path: "packages/cora/src/preview/App.tsx"
      provides: "Reference-style three-zone workbench shell"
      contains: "preview-shell"
---

<objective>
Establish the preview-only visual system and reference-style shell foundation for the NodeFlow-inspired workbench.
</objective>

<decision_coverage>
- D-01, D-02, D-04, D-05: The shell becomes a focused studio workbench with the same broad left/canvas/right composition as the reference.
- D-16, D-17, D-18: `styles.css` defines preview-local black/white/purple tokens, soft panel depth, and large clean typography.
- D-20: Motion tokens are added with `prefers-reduced-motion` protection.
</decision_coverage>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-3.5-01: Preview polish changes renderer defaults by accident | medium | Keep all visual tokens in `packages/cora/src/preview/styles.css`; do not edit `renderer/components` in this plan. |
| T-3.5-02: Shell becomes decorative and hides the workbench | medium | Preserve `CatalogSidebar`, `WorkbenchCanvas`, and inspector as first-screen regions; remove marketing-style copy. |
| T-3.5-03: Motion harms accessibility | low | Add `@media (prefers-reduced-motion: reduce)` that disables transform/transition motion. |
</threat_model>

<tasks>

<task type="execute" id="3.5-01-01">
  <name>Add preview visual tokens and shell composition</name>
  <read_first>
    - .planning/phases/3.5-preview-visual-beauty/03.5-CONTEXT.md
    - .planning/phases/3.5-preview-visual-beauty/03.5-UI-SPEC.md
    - .planning/phases/3.5-preview-visual-beauty/03.5-RESEARCH.md
    - .planning/phases/3.5-preview-visual-beauty/03.5-PATTERNS.md
    - packages/cora/src/preview/styles.css
    - packages/cora/src/preview/App.tsx
  </read_first>
  <action>
Update `packages/cora/src/preview/styles.css` to define preview-local CSS custom properties for:

- `--preview-bg: #FBFAF8`
- `--preview-surface: #FFFFFF`
- `--preview-ink: #0A0A0A`
- `--preview-muted: #6F6A72`
- `--preview-border: #E9E5E1`
- `--preview-accent: #6D28D9`
- `--preview-accent-soft: #EEE5FF`
- `--preview-danger: #B42318`
- `--preview-shadow: 0 18px 44px rgba(20, 16, 12, 0.10)`

Restyle `.preview-shell`, `.catalog-panel`, `.inspector-panel`, and `.workbench-column` so the desktop first screen reads as:

- rounded white left and right panels with 24px radius, light border, subtle shadow
- center workbench area on warm off-white background
- no hard full-height gutter borders as the dominant visual
- desktop grid retains left/canvas/right zones
- no page-level landing/hero composition

Update `App.tsx` to remove or demote the current `.empty-copy` instruction strip so it does not break the reference composition. Keep a minimal workbench hint only if it is visually quiet.
  </action>
  <acceptance_criteria>
- `packages/cora/src/preview/styles.css` contains `--preview-accent: #6D28D9`.
- `packages/cora/src/preview/styles.css` contains `--preview-bg: #FBFAF8`.
- `packages/cora/src/preview/styles.css` contains `.catalog-panel` and `border-radius: 24px`.
- `packages/cora/src/preview/styles.css` contains `prefers-reduced-motion`.
- `packages/cora/src/preview/App.tsx` no longer renders the exact string `Select a canvas node to edit attributes. Add two nodes to inspect the connection.`
- `cd packages/cora && bun x vitest run tests/preview` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg -- '--preview-accent: #6D28D9|--preview-bg: #FBFAF8|prefers-reduced-motion|border-radius: 24px' packages/cora/src/preview/styles.css && ! rg "Select a canvas node to edit attributes\\. Add two nodes to inspect the connection\\." packages/cora/src/preview/App.tsx && cd packages/cora && bun x vitest run tests/preview</automated>
  </verify>
</task>

<task type="execute" id="3.5-01-02">
  <name>Update shell-level preview assertions</name>
  <read_first>
    - packages/cora/tests/preview/workbench.test.tsx
    - packages/cora/src/preview/App.tsx
    - packages/cora/src/preview/styles.css
  </read_first>
  <action>
Update `packages/cora/tests/preview/workbench.test.tsx` so the shell test asserts the new visual contract at the source/markup level:

- rendered markup still contains `aria-label="Catalog"`, `aria-label="Canvas"`, and `aria-label="Inspector"`
- rendered markup contains `Cora` or `Cora Preview`
- rendered markup contains `Library / Components`
- rendered markup does not contain the old long instruction sentence from `App.tsx`

Do not add brittle CSS snapshot assertions.
  </action>
  <acceptance_criteria>
- `packages/cora/tests/preview/workbench.test.tsx` contains `Library / Components`.
- `packages/cora/tests/preview/workbench.test.tsx` contains `Cora`.
- `packages/cora/tests/preview/workbench.test.tsx` does not contain the old assertion text `Select a canvas node to edit attributes`.
- `cd packages/cora && bun x vitest run tests/preview/workbench.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "Library / Components|Cora" packages/cora/tests/preview/workbench.test.tsx && ! rg "Select a canvas node to edit attributes" packages/cora/tests/preview/workbench.test.tsx && cd packages/cora && bun x vitest run tests/preview/workbench.test.tsx</automated>
  </verify>
</task>

</tasks>

<verification>
Run:

```bash
cd packages/cora
bun run typecheck
bun x vitest run tests/preview
```
</verification>

<success_criteria>
- Preview shell has preview-local black/white/purple tokens.
- Left and right zones read as soft premium panels.
- Current instruction strip no longer breaks the reference composition.
- Existing preview behavior tests still pass.
</success_criteria>
