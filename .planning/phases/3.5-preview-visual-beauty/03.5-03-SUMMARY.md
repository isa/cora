---
phase: "03.5"
plan: "03"
subsystem: "preview"
tags: [preview, canvas, toolbar, motion]
requires:
  - ".planning/phases/3.5-preview-visual-beauty/03.5-03-PLAN.md"
provides:
  - "Premium dotted canvas surface"
  - "Split floating canvas tools"
affects:
  - "packages/cora/src/preview/components/WorkbenchCanvas.tsx"
  - "packages/cora/src/preview/styles.css"
  - "packages/cora/tests/preview/workbench.test.tsx"
tech_stack:
  added: []
  patterns:
    - "Preview-local floating tool groups"
key_files:
  created: []
  modified:
    - "packages/cora/src/preview/components/WorkbenchCanvas.tsx"
    - "packages/cora/src/preview/styles.css"
    - "packages/cora/tests/preview/workbench.test.tsx"
key_decisions:
  - "Attachment labels remain available in overlay code but hidden in the normal preview chrome."
  - "Canvas visual polish is CSS-only around the existing SVG dimensions and geometry path."
requirements_completed:
  - BEAUTY-*
duration: "2 min"
completed: "2026-05-23"
---

# Phase 03.5 Plan 03: Premium Canvas And Tools Summary

Converted the preview canvas chrome into a premium dotted studio surface with split floating tools.

## Execution

- Kept `CANVAS_WIDTH`, `CANVAS_HEIGHT`, viewBox, drag, zoom, pan, selection, and connection computations unchanged.
- Added dotted workspace styling to the canvas region and quieted the SVG board.
- Split toolbar chrome into action and zoom tool groups.
- Removed the visible primary `Labels` toggle from normal markup.
- Added accessible zoom labels while keeping compact visible symbols.
- Added inspection-safe transitions for catalog rows, canvas tools, nodes, and selection outlines.
- Updated preview tests to assert no visible `Labels` chrome and accessible zoom controls.

## Verification

- `rg "radial-gradient|selection-outline" packages/cora/src/preview/styles.css` passed.
- `rg "CANVAS_WIDTH = 760|CANVAS_HEIGHT = 520" packages/cora/src/preview/components/WorkbenchCanvas.tsx` passed.
- `cd packages/cora && bun x vitest run tests/preview/geometry.test.ts` passed: 10 tests.
- `! rg ">Labels<|Labels</" packages/cora/src/preview/components/WorkbenchCanvas.tsx` passed.
- `rg "Labels" packages/cora/tests/preview/workbench.test.tsx` passed via absence assertion.
- `cd packages/cora && bun x vitest run tests/preview/workbench.test.tsx` passed: 3 tests.
- `rg "prefers-reduced-motion|component-tile.*transition|selection-outline" packages/cora/src/preview/styles.css` passed.
- `cd packages/cora && bun run typecheck` passed.
- `cd packages/cora && bun x vitest run tests/preview` passed: 7 files, 26 tests.

## Deviations from Plan

- `AttachmentOverlay.tsx` and `geometry.test.ts` did not require edits after inspection; existing overlay/geometry behavior already satisfied the plan once `showLabels` was hidden from normal canvas chrome.

**Total deviations:** 1 no-op scope reduction. **Impact:** Low; fewer files changed while preserving behavior.

## Self-Check: PASSED

Ready for 03.5-04.
