---
phase: 3.3
slug: component-preview-canvas
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-22
---

# Phase 3.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest |
| **Config file** | `packages/cora/vitest.config.ts` |
| **Quick run command** | `bun x vitest run tests/preview/*.test.ts* tests/cli/*.test.ts*` |
| **Full suite command** | `bun x vitest run` |
| **Estimated runtime** | ~30-60 seconds |

---

## Sampling Rate

- **After every task commit:** Run the relevant focused Vitest command for the touched area.
- **After every plan wave:** Run `bun x vitest run`.
- **Before `$gsd-verify-work`:** `bun run build`, `bun x vitest run`, and preview packaging smoke must be green.
- **Max feedback latency:** 60 seconds for focused checks.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 3.3-01-01 | 01 | 1 | PREV-01, PREV-02 | T-3.3-01 | Vite root constrained to package preview source/assets | unit/integration | `bun x vitest run tests/preview/*.test.ts* tests/cli/*.test.ts*` | W0 | pending |
| 3.3-02-01 | 02 | 1 | PREV-02, PREV-03, PREV-04, PREV-06 | T-3.3-02 | Control schema prevents invalid prop values | unit | `bun x vitest run tests/preview/*.test.ts*` | W0 | pending |
| 3.3-03-01 | 03 | 2 | PREV-03, PREV-04, PREV-05 | T-3.3-03 | Dragging only mutates client state, never disk/YAML | unit/component | `bun x vitest run tests/preview/*.test.ts*` | W0 | pending |
| 3.3-04-01 | 04 | 3 | PREV-01, PREV-02, PREV-05 | T-3.3-04 | Packaged assets include only intended preview files | build/smoke | `bun run build && bun x vitest run` | W0 | pending |

*Status: pending · green · red · flaky*

---

## Wave 0 Requirements

- [ ] `packages/cora/tests/preview/` — unit tests for manifest, controls, scenarios, and attachment slots.
- [ ] `packages/cora/tests/cli/preview.test.ts` or equivalent — CLI command registration/help and server seam tests.
- [ ] Preview server helper accepts an open-disabled/test mode so CI does not launch a browser.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Browser workbench renders and is usable | PREV-01–06 | Requires visual inspection and pointer interaction | Run `bun run cora preview`; select primary/secondary nodes; drag them; edit props; toggle overlay labels; switch scenarios |

---

## Validation Sign-Off

- [x] All tasks have automated verify or Wave 0 dependencies.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify.
- [x] Wave 0 covers all missing preview test references.
- [x] No watch-mode flags.
- [x] Feedback latency target < 60s.
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** approved 2026-05-22
