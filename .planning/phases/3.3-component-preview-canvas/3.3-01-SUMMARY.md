---
phase: 3.3-component-preview-canvas
plan: 01
subsystem: cli
tags: [preview, cli, vite, react]
requires:
  - phase: 3.2-renderer-component-library
    provides: renderer component catalog
provides:
  - cora preview CLI command
  - Vite-backed preview server helper
  - packaged preview SPA entry shell
affects: [preview, cli, package]
tech-stack:
  added: [vite]
  patterns:
    - CLI command registration through commander command modules
    - Preview server configured with explicit root and configFile false
key-files:
  created:
    - packages/cora/src/cli/commands/preview.ts
    - packages/cora/src/preview/server.ts
    - packages/cora/src/preview/index.html
    - packages/cora/src/preview/main.tsx
    - packages/cora/tests/cli/preview.test.ts
    - packages/cora/tests/preview/server.test.ts
  modified:
    - packages/cora/package.json
    - packages/cora/src/cli/index.ts
key-decisions:
  - "Preview is a real CLI command with no diagram-file argument."
  - "The preview server uses Vite with configFile false and package-local roots."
patterns-established:
  - "Browser-opening is outside tests through --no-open and injectable command dependencies."
requirements-completed: [PREV-01, PREV-02, PREV-06]
duration: 0h
completed: 2026-05-22
---

# Phase 3.3 Plan 01: Preview CLI Server Shell Summary

**`cora preview` command with a Vite-backed local SPA shell and package build output**

## Performance

- **Duration:** 0h
- **Started:** 2026-05-22
- **Completed:** 2026-05-22
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Added `cora preview` with `--host`, `--port`, and `--no-open`.
- Added a Vite server helper with `configFile: false`, strict file serving, clean shutdown, and deterministic preview roots.
- Added the initial React entry shell and build step for `dist/preview`.

## Task Commits

1. **Preview CLI/server shell** - `accc5db` (`feat(3.3-01)`)

## Files Created/Modified

- `packages/cora/src/cli/commands/preview.ts` - Commander registration and shutdown handling.
- `packages/cora/src/preview/server.ts` - Vite server helper and browser open helper.
- `packages/cora/src/preview/index.html` - SPA HTML entry.
- `packages/cora/src/preview/main.tsx` - React mount entry.
- `packages/cora/package.json` - Adds Vite and builds `dist/preview`.

## Decisions Made

- Used Vite directly for the preview workbench because Phase 4 can reuse the server pattern.
- Kept browser opening outside the test path through `--no-open` and dependency injection.

## Deviations from Plan

None - plan executed as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Self-Check: PASSED

- `bun run typecheck` passed.
- `bun x vitest run tests/cli/preview.test.ts tests/preview/server.test.ts` passed.
- `bun run build` emitted `dist/preview`.

## Next Phase Readiness

Plan 02 can build the preview-local pack, control, scenario, and state model on top of the server shell.

---
*Phase: 3.3-component-preview-canvas*
*Completed: 2026-05-22*
