---
phase: 02-renderer-svg-export
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - packages/cora/src/renderer/nodes/BoxNode.tsx
  - packages/cora/src/renderer/nodes/CylinderNode.tsx
  - packages/cora/src/renderer/nodes/CloudNode.tsx
  - packages/cora/src/renderer/nodes/DiamondNode.tsx
  - packages/cora/src/renderer/nodes/HexagonNode.tsx
  - packages/cora/src/renderer/nodes/GroupNode.tsx
  - packages/cora/src/renderer/nodes/index.ts
  - packages/cora/src/renderer/edges/ArrowEdge.tsx
  - packages/cora/src/renderer/edges/index.ts
  - packages/cora/src/renderer/Diagram.tsx
  - packages/cora/src/renderer/themes/default.ts
  - packages/cora/src/renderer/renderToSVG.ts
autonomous: true
requirements:
  - REN-01
  - REN-02
  - REN-03
  - REN-04
  - REN-05
  - REN-06
  - REN-07
user_setup: []

must_haves:
  truths:
    - "All seven node shapes render as pure SVG primitives"
    - "Edge labels render at ELK routing midpoints"
    - "Groups render as dashed boundary rectangles containing child nodes"
    - "All five kind fixtures produce distinct valid SVG output"
    - "viewBox includes 24px padding preventing clip"
  artifacts:
    - path: packages/cora/src/renderer/nodes/CylinderNode.tsx
      provides: database cylinder shape
      contains: "path"
    - path: packages/cora/src/renderer/edges/ArrowEdge.tsx
      provides: orthogonal edge with arrowhead
      contains: "marker"
    - path: packages/cora/src/renderer/Diagram.tsx
      provides: kind-aware root component
      contains: "ArrowEdge"
  key_links:
    - from: packages/cora/src/renderer/Diagram.tsx
      to: packages/cora/src/renderer/nodes/index.ts
      via: "shape switch"
      pattern: "shape.*rounded|BoxNode"
    - from: packages/cora/src/renderer/edges/ArrowEdge.tsx
      to: LayoutedDiagram edge routing points
      via: "props.points"
      pattern: "routingPoints|sections"
---

## Phase Goal

**As an** human reviewer, **I want to** see polished professional diagrams with soft pastels and all v1 shapes, **so that** Cora output is suitable for architecture docs without extensions.

<objective>
Complete the React SVG renderer: all node shapes, group boundaries, edge routing with labels, polished default theme per D-01–D-04, and render all five kind fixtures.

Purpose: REN-01 through REN-07; REN-02 establishes same Diagram tree for future serve.
Output: `cora render` works for all examples/valid/*.yaml with professional appearance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/02-renderer-svg-export/02-02-SUMMARY.md
</execution_context>

<context>
@.planning/phases/02-renderer-svg-export/02-CONTEXT.md
@.planning/phases/02-renderer-svg-export/02-RESEARCH.md
@examples/valid/microservice.yaml
@examples/valid/infra.yaml
@examples/valid/database.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: All node shape components (REN-05, D-02, D-03)</name>
  <files>packages/cora/src/renderer/nodes/*.tsx, packages/cora/src/renderer/nodes/index.ts, packages/cora/src/renderer/themes/default.ts</files>
  <read_first>.planning/phases/02-renderer-svg-export/02-CONTEXT.md, .planning/phases/02-renderer-svg-export/02-RESEARCH.md, packages/cora/src/renderer/nodes/RoundedNode.tsx</read_first>
  <action>
    Implement stateless components: BoxNode (rect), RoundedNode (existing, refine), CylinderNode (path with elliptical caps), CloudNode (path blob), DiamondNode (polygon), HexagonNode (polygon 6 sides), GroupNode (dashed rect, label at top-left).
    Each accepts props: x, y, width, height, label, style tokens from default theme soft pastels per shape per RESEARCH hex examples.
    Apply subtle depth D-03: shared SVG filter definition drop-shadow in Diagram defs; reference filter on node groups.
    nodes/index.ts exports shape registry map keyed by shape enum.
    Semibold node labels D-08, 14px D-07, font-family 'Inter', sans-serif.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run build 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - Seven node component files exist under renderer/nodes/
    - Each node component file contains export function and no useEffect
    - default.ts defines distinct fill colors for at least rounded, cylinder, diamond, cloud
    - CylinderNode uses SVG path element not foreignObject
  </acceptance_criteria>
  <done>All v1 node shapes implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Edge routing and labels (REN-06, D-08)</name>
  <files>packages/cora/src/renderer/edges/ArrowEdge.tsx, packages/cora/src/renderer/edges/index.ts, packages/cora/src/renderer/Diagram.tsx</files>
  <read_first>packages/cora/src/core/types.ts, .planning/phases/02-renderer-svg-export/02-RESEARCH.md, examples/valid/flowchart.yaml</read_first>
  <action>
    ArrowEdge.tsx: render orthogonal polyline from ELK edge section points; arrowhead via SVG marker-end; stroke from theme edge tokens.
    Edge label: text at midpoint of routing path, regular weight 400 per D-08, 14px, positioned with small background rect for readability if needed.
    Diagram.tsx: render edges below nodes; map LayoutedDiagram edges to ArrowEdge with routingPoints from layout output.
    Define arrow marker in SVG defs once at root.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run build && node packages/cora/dist/cli.js render examples/valid/flowchart.yaml -o /tmp/fc.svg && grep -q "yes" /tmp/fc.svg</automated>
  </verify>
  <acceptance_criteria>
    - flowchart.yaml render output contains edge label text yes
    - ArrowEdge.tsx contains marker-end or markerEnd equivalent in SVG
    - Edge label uses font-weight 400 or fontWeight 400 in source
  </acceptance_criteria>
  <done>Edges route with labels visible.</done>
</task>

<task type="auto">
  <name>Task 3: Groups, viewBox, all kind fixtures (REN-07, REN-04)</name>
  <files>packages/cora/src/renderer/Diagram.tsx, packages/cora/src/renderer/renderToSVG.ts</files>
  <read_first>examples/valid/infra.yaml, examples/valid/microservice.yaml, examples/valid/database.yaml, .planning/phases/02-renderer-svg-export/02-CONTEXT.md</read_first>
  <action>
    Diagram.tsx: render groups as GroupNode behind contained nodes; compute group bounds from child positions + padding.
    renderToSVG.ts: compute bounding box of all rendered elements; set viewBox with 24px padding on all sides per REN-07; set width/height attributes on root svg.
    Verify cora render succeeds for all six files in examples/valid/*.yaml.
    Ensure all text elements remain direct children of root svg or single root g — not nested svg per PITFALLS Pitfall 3.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run build && for f in examples/valid/*.yaml; do node packages/cora/dist/cli.js render "$f" -o "/tmp/$(basename $f .yaml).svg" || exit 1; done && echo ALL_OK</automated>
  </verify>
  <acceptance_criteria>
    - All six examples/valid YAML files render exit 0
    - renderToSVG output includes viewBox attribute
    - infra.yaml output contains group label Region
    - database.yaml render includes cylinder shape path or node
    - No foreignObject in any rendered SVG
  </acceptance_criteria>
  <done>Full renderer covers all v1 kinds and shapes.</done>
</task>

</tasks>
