---
phase: 02-renderer-svg-export
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/cora/package.json
  - packages/cora/src/core/layoutWorker.ts
  - packages/cora/src/core/layout.ts
  - packages/cora/src/core/types.ts
  - packages/cora/src/core/index.ts
  - packages/cora/src/cli/commands/render.ts
autonomous: true
requirements:
  - LAY-01
  - LAY-02
  - LAY-03
  - LAY-04
user_setup: []

must_haves:
  truths:
    - "layout: auto runs ELK layered algorithm and assigns finite x/y to every node"
    - "layout: preserve uses YAML positions only and errors when positions missing"
    - "layout: hybrid fixes pinned nodes via elk.fixed while ELK lays out unpinned"
    - "ELK runs in a worker thread and does not block the main thread (LAY-04)"
  artifacts:
    - path: packages/cora/src/core/layoutWorker.ts
      provides: ELK worker wrapper
      exports: ["createElkWorker", "runElkLayout"]
    - path: packages/cora/src/core/layout.ts
      provides: auto/preserve/hybrid orchestration
      exports: ["computeLayout"]
  key_links:
    - from: packages/cora/src/core/layout.ts
      to: packages/cora/src/core/layoutWorker.ts
      via: "await runElkLayout(elkGraph)"
      pattern: "runElkLayout|layoutWorker"
    - from: packages/cora/src/cli/commands/render.ts
      to: packages/cora/src/core/layout.ts
      via: "replace stubLayout with computeLayout"
      pattern: "computeLayout"
---

## Phase Goal

**As an** AI coding agent, **I want to** render diagrams with ELK auto-layout and pinned-node support, **so that** I do not hand-place coordinates for every node in architecture diagrams.

<objective>
Replace stub layout with production ELK integration: worker-thread layout (LAY-04), layered algorithm (LAY-01), and layout mode switching auto | preserve | hybrid with pinned node support (LAY-02, LAY-03).

Purpose: Real graph layout is the core value of Cora render — walking skeleton positions are insufficient for multi-node diagrams.
Output: computeLayout() producing LayoutedDiagram with ELK edge routing points; render pipeline uses ELK for all layout modes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/02-renderer-svg-export/02-01-SUMMARY.md
</execution_context>

<context>
@.planning/phases/02-renderer-svg-export/02-CONTEXT.md
@.planning/phases/02-renderer-svg-export/02-RESEARCH.md
@.planning/research/PITFALLS.md
@examples/valid/microservice.yaml
@examples/valid/flowchart.yaml

<interfaces>
From 02-01 (must exist before this plan):
```typescript
export interface MeasuredNode extends DiagramNode {
  measuredWidth: number;
  measuredHeight: number;
}
export interface LayoutedDiagram {
  kind: DiagramKind;
  nodes: LayoutedNode[];
  edges: LayoutedEdge[];
  groups?: LayoutedGroup[];
  width: number;
  height: number;
}
export function measureNodes(nodes: DiagramNode[]): MeasuredNode[];
```

ELK explicit options per RESEARCH (never rely on defaults):
- elk.algorithm: layered
- elk.direction: DOWN (default); map diagram.direction LR → RIGHT, TB → DOWN
- elk.layered.nodePlacement.strategy: BRANDES_KOEPF
- elk.layered.edgeRouting: ORTHOGONAL
- elk.hierarchyHandling: INCLUDE_CHILDREN
- elk.spacing.nodeNode: 40 (D-04 balanced density)
- Pinned: layoutOptions 'org.eclipse.elk.fixed': 'true' + x/y from YAML position
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ELK worker thread setup (LAY-04)</name>
  <files>packages/cora/package.json, packages/cora/src/core/layoutWorker.ts</files>
  <read_first>.planning/phases/02-renderer-svg-export/02-RESEARCH.md, .planning/research/ARCHITECTURE.md, elkjs README Node worker section</read_first>
  <action>
    Add dependencies: elkjs@^0.11.1 (pin 0.11.x minor), web-worker (latest stable). Per RESEARCH Package Legitimacy: elkjs and web-worker are [VERIFIED] on npm.
    Create layoutWorker.ts: instantiate ELK from elkjs/lib/elk-api.js with workerUrl pointing to elkjs/lib/elk-worker.min.js via new URL(..., import.meta.url). Use web-worker package for Node worker_threads compatibility (same API as browser Worker for Phase 4 serve reuse).
    Export async runElkLayout(graph: ElkNode): Promise<ElkNode> that delegates to worker and returns laid-out graph with sections on edges. Export graceful worker termination helper for CLI cleanup.
    Do not run ELK synchronously on main thread — LAY-04 requires worker isolation.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun install && bun run build && node -e "import { runElkLayout } from './packages/cora/dist/core/index.js'; const g={id:'root',layoutOptions:{'elk.algorithm':'layered'},children:[{id:'a',width:80,height:40},{id:'b',width:80,height:40}],edges:[{id:'e',sources:['a'],targets:['b']}]}; runElkLayout(g).then(r=>console.log(r.children[0].x!==undefined))"</automated>
  </verify>
  <acceptance_criteria>
    - packages/cora/package.json lists elkjs@0.11.x and web-worker
    - layoutWorker.ts imports from elkjs/lib/elk-api.js
    - runElkLayout assigns x and y to ELK children in a 2-node smoke graph
    - No synchronous ELK.mainThread usage in layoutWorker.ts
  </acceptance_criteria>
  <done>ELK runs in worker thread and returns positioned graph.</done>
</task>

<task type="auto">
  <name>Task 2: Layout modes auto / preserve / hybrid (LAY-01, LAY-02)</name>
  <files>packages/cora/src/core/layout.ts, packages/cora/src/core/types.ts, packages/cora/src/core/index.ts</files>
  <read_first>.planning/phases/02-renderer-svg-export/02-RESEARCH.md, packages/cora/src/core/types.ts, packages/cora/src/core/stubLayout.ts</read_first>
  <action>
    Create layout.ts exporting async computeLayout(input: { diagram: Diagram; measuredNodes: MeasuredNode[] }): Promise<LayoutedDiagram>.
    Build ELK graph from measured nodes + edges + groups (nested children for group contains per elk.hierarchyHandling INCLUDE_CHILDREN). Every node must have width/height from measurement — never 0 (PITFALLS Pitfall 1).
    Mode auto (default when layout omitted): run ELK; ignore YAML position except pinned nodes (handled in task 3). LAY-01 satisfied via layered algorithm.
    Mode preserve: skip ELK entirely; require every node has position.x and position.y; throw structured error (code e.g. LAYOUT_ERROR or reuse SCHEMA-style with clear message) listing nodes missing positions if any. Map YAML positions to LayoutedNode x/y.
    Mode hybrid: pass all nodes to ELK; unpinned get computed positions; pinned handled in task 3.
    Map ELK edge sections to LayoutedEdge.points array and compute labelX/labelY at edge midpoint for renderer (full label styling in 02-03). Compute diagram width/height from ELK bounding box + 24px viewBox padding (REN-07).
    Export computeLayout from core/index.ts. Remove stubLayout.ts usage from render pipeline (delete stubLayout.ts or keep unused until cleanup in same task).
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run build && node packages/cora/dist/cli.js render examples/valid/microservice.yaml -o /tmp/ms.svg && test -f /tmp/ms.svg && grep -q viewBox /tmp/ms.svg</automated>
  </verify>
  <acceptance_criteria>
    - computeLayout exists and is exported from core/index.ts
    - cora render examples/valid/microservice.yaml -o /tmp/ms.svg exits 0 with auto layout
    - Every node in output has been positioned (no overlapping at 0,0 stack for 3+ node fixture)
    - layout: preserve on diagram without positions fails with actionable error
    - stubLayout.ts is no longer called from render.ts
  </acceptance_criteria>
  <done>ELK auto-layout replaces stub layout; preserve mode enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Pinned nodes during auto/hybrid re-layout (LAY-03)</name>
  <files>packages/cora/src/core/layout.ts, packages/cora/src/cli/commands/render.ts</files>
  <read_first>.planning/phases/02-renderer-svg-export/02-RESEARCH.md, packages/cora/src/core/schema/diagram.schema.json</read_first>
  <action>
    Per LAY-03 and RESEARCH hybrid table: nodes with pinned: true AND position set receive ELK layoutOptions { 'org.eclipse.elk.fixed': 'true' } plus x/y from YAML position when building ELK input.
    In auto mode: only pinned nodes use YAML position; unpinned nodes are free. In hybrid mode: same pinned behavior; unpinned relayout. In preserve mode: all positions from YAML (pinned flag irrelevant but preserved in IR).
    Wire render.ts: measureNodes → await computeLayout (async) → renderToSVG. Handle layout errors with exit 1 and stderr message.
    Verify with a test fixture or inline YAML: 3-node diagram, middle node pinned at explicit position, auto layout — pinned node x/y matches YAML within 1px, unpinned nodes move.
    Create examples/valid/pinned-hybrid.yaml if no existing fixture covers pinned (minimal 3 nodes, one pinned with position) — only if needed for verification.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run build && node packages/cora/dist/cli.js render examples/valid/flowchart.yaml -o /tmp/fc.svg && test -f /tmp/fc.svg</automated>
  </verify>
  <acceptance_criteria>
    - layout.ts sets org.eclipse.elk.fixed true for pinned nodes with positions
    - Pinned node retains YAML coordinates after auto/hybrid layout (within 1px)
    - cora render examples/valid/flowchart.yaml -o /tmp/fc.svg exits 0
    - render.ts awaits async computeLayout
  </acceptance_criteria>
  <done>Pinned nodes stay fixed while ELK relayouts neighbors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker thread ← ELK WASM/JS bundle | Large third-party layout binary from elkjs package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | layoutWorker.ts | accept | Single diagram per invocation; worker terminated after layout |
| T-02-04 | Tampering | ELK graph build | mitigate | Node ids from validated schema only; no eval of user strings |
| T-02-SC | Tampering | elkjs install | mitigate | Pin elkjs@0.11.1; golden suite in 02-04 catches routing drift |
</threat_model>

<verification>
- cora render examples/valid/*.yaml (all five kind fixtures) exit 0 with auto layout
- preserve mode fails clearly when positions missing
- Pinned node fixture retains fixed coordinates
</verification>

<success_criteria>
- ELK layered layout is production path for auto/hybrid
- Worker thread satisfies LAY-04 for future serve event loop
- All four layout requirements (LAY-01–04) satisfied
</success_criteria>

<output>
Create `.planning/phases/02-renderer-svg-export/02-02-SUMMARY.md` when done
</output>
