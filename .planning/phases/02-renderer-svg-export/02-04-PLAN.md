---
phase: 02-renderer-svg-export
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - packages/cora/tests/golden/box-arrows.svg
  - packages/cora/tests/golden/flowchart.svg
  - packages/cora/tests/golden/microservice.svg
  - packages/cora/tests/golden/infra.svg
  - packages/cora/tests/golden/database.svg
  - packages/cora/tests/golden/update-golden.sh
  - packages/cora/tests/golden/compare-golden.sh
  - packages/cora/src/cli/commands/render.ts
  - AGENTS.md
  - README.md
autonomous: true
requirements:
  - CLI-05
  - EXP-01
  - REN-02
user_setup: []

must_haves:
  truths:
    - "Golden SVG snapshot exists for each of five diagram kinds"
    - "CI script detects unintended SVG output changes"
    - "AGENTS.md documents cora render workflow"
    - "cora render -o writes UTF-8 SVG via renderToStaticMarkup path"
  artifacts:
    - path: packages/cora/tests/golden/box-arrows.svg
      provides: golden baseline box-arrows
      min_lines: 10
    - path: packages/cora/tests/golden/compare-golden.sh
      provides: regression check script
      contains: "diff"
    - path: AGENTS.md
      provides: render command docs
      contains: "cora render"
  key_links:
    - from: packages/cora/tests/golden/compare-golden.sh
      to: examples/valid/*.yaml
      via: "render then diff"
      pattern: "cora render|examples/valid"
    - from: AGENTS.md
      to: packages/cora/src/cli/commands/render.ts
      via: "document -o flag"
      pattern: "render.*-o"
---

## Phase Goal

**As a** maintainer, **I want to** golden SVG regression and agent docs for render, **so that** layout/theme changes are intentional and agents discover the render command.

<objective>
Add string-based golden SVG suite (one per kind), comparison script for CI, CLI render polish, and AGENTS.md/README updates for Phase 2 scope.

Purpose: ROADMAP success criterion #5; completes CLI-05 and EXP-01 documentation.
Output: Regression gate + agent contract includes render loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/02-renderer-svg-export/02-03-SUMMARY.md
</execution_context>

<context>
@.planning/phases/02-renderer-svg-export/02-RESEARCH.md
@.planning/ROADMAP.md
@AGENTS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden SVG baselines per kind</name>
  <files>packages/cora/tests/golden/*.svg, packages/cora/tests/golden/update-golden.sh</files>
  <read_first>.planning/phases/02-renderer-svg-export/02-RESEARCH.md, examples/valid/box-arrows.yaml, examples/valid/flowchart.yaml, examples/valid/microservice.yaml, examples/valid/infra.yaml, examples/valid/database.yaml</read_first>
  <action>
    Create packages/cora/tests/golden/ directory.
    Run cora render for each of five kind fixtures (not minimal.yaml — one per kind per ROADMAP): box-arrows, flowchart, microservice, infra, database.
    Save output as {kind}.svg golden files.
    update-golden.sh: rebuild project, re-render all five fixtures, overwrite golden files (for intentional visual updates).
    Normalize: optional whitespace trim in compare step only, not in stored golden (store raw render output).
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && ls packages/cora/tests/golden/*.svg | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Five golden SVG files exist: box-arrows, flowchart, microservice, infra, database
    - Each golden file size greater than 200 bytes
    - Each golden file contains xmlns="http://www.w3.org/2000/svg"
    - update-golden.sh is executable and invokes cora render
  </acceptance_criteria>
  <done>Golden baselines captured for all kinds.</done>
</task>

<task type="auto">
  <name>Task 2: Golden compare script for CI</name>
  <files>packages/cora/tests/golden/compare-golden.sh</files>
  <read_first>packages/cora/tests/golden/update-golden.sh, .planning/phases/02-renderer-svg-export/02-RESEARCH.md</read_first>
  <action>
    compare-golden.sh: build, render each kind fixture to /tmp/cora-golden-{kind}.svg, diff against packages/cora/tests/golden/{kind}.svg using diff -u; exit 1 on any mismatch with clear message listing which kind failed.
    Document in script header: string compare not raster — stable because embedded Inter + pinned ELK options.
    Root package.json optional script golden:compare calling this shell script (if package.json scripts section exists at root).
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bash packages/cora/tests/golden/compare-golden.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - compare-golden.sh exits 0 when goldens match current render
    - compare-golden.sh exits non-zero when golden file manually altered
    - Script renders all five kind fixtures before diffing
  </acceptance_criteria>
  <done>Regression gate passes on clean tree.</done>
</task>

<task type="auto">
  <name>Task 3: CLI polish + agent docs (CLI-05, EXP-01, REN-02 note)</name>
  <files>packages/cora/src/cli/commands/render.ts, AGENTS.md, README.md</files>
  <read_first>AGENTS.md, README.md, packages/cora/src/cli/commands/validate.ts, .planning/ROADMAP.md</read_first>
  <action>
    render.ts polish: validate -o required; helpful error if missing; ensure UTF-8 write; exit codes match validate (0 success, 1 validation/render failure).
    AGENTS.md: add cora render section — render after validate; -o path required; output is pure SVG; document that serve uses same render tree (REN-02 forward note); update quick start to include render example; note Phase 2 adds render (update purpose line from validate-only).
    README.md: add render one-liner under usage; link golden test script for contributors.
    Keep honest scope: no PDF/serve/ext yet.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run build && node packages/cora/dist/cli.js render 2>&1 | head -3; grep -q "cora render" AGENTS.md && grep -q "render" README.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains cora render command with -o flag
    - README.md mentions render workflow
    - cora render without -o exits non-zero with usage hint
    - cora render examples/valid/minimal.yaml -o /tmp/t.svg exits 0
  </acceptance_criteria>
  <done>Phase 2 agent contract and CLI complete.</done>
</task>

</tasks>
