---
phase: 02-renderer-svg-export
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - packages/cora/package.json
  - packages/cora/tests/golden/box-arrows.svg
  - packages/cora/tests/golden/flowchart.svg
  - packages/cora/tests/golden/microservice.svg
  - packages/cora/tests/golden/infra.svg
  - packages/cora/tests/golden/database.svg
  - packages/cora/tests/render-golden.mjs
  - packages/cora/src/cli/commands/render.ts
  - packages/cora/src/cli/index.ts
  - packages/cora/src/index.ts
autonomous: true
requirements:
  - CLI-05
  - EXP-01
user_setup: []

must_haves:
  truths:
    - "Golden SVG regression suite has at least one baseline per v1 kind (5 files)"
    - "CI-style render compare detects unintended SVG output changes"
    - "cora render --help documents -o flag; errors are actionable on invalid input"
    - "Public package API exports renderToSVG for library consumers"
  artifacts:
    - path: packages/cora/tests/golden/
      provides: Per-kind SVG baselines
      min_lines: 1
    - path: packages/cora/tests/render-golden.mjs
      provides: Golden comparison runner
    - path: packages/cora/src/cli/commands/render.ts
      provides: Polished render command
  key_links:
    - from: packages/cora/tests/render-golden.mjs
      to: packages/cora/dist/cli.js
      via: "spawn render per fixture, normalize, diff golden"
      pattern: "render.*golden"
    - from: packages/cora/src/index.ts
      to: packages/cora/src/renderer/renderToSVG.ts
      via: "re-export renderToSVG"
      pattern: "renderToSVG"
---

## Phase Goal

**As an** AI coding agent, **I want to** rely on golden SVG regression and a polished render CLI, **so that** diagram output stays stable across changes and CI catches visual regressions automatically.

<objective>
Add string-based golden SVG regression (≥1 per kind), polish cora render CLI ergonomics, export public render API, and verify Phase 2 success criteria end-to-end.

Purpose: Lock in deterministic SVG output (embedded Inter + pinned ELK options) and agent-friendly CLI contract.
Output: tests/golden/*.svg baselines, render-golden.mjs runner, polished render command, Phase 2 verification green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/02-renderer-svg-export/02-03-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-renderer-svg-export/02-RESEARCH.md
@.planning/phases/02-renderer-svg-export/02-CONTEXT.md
@examples/valid/

<interfaces>
Golden compare strategy from RESEARCH:
- String snapshot (not pixel raster) — normalize whitespace before diff
- Store under packages/cora/tests/golden/{kind}.svg
- Fixtures: box-arrows.yaml, flowchart.yaml, microservice.yaml, infra.yaml, database.yaml
- Update policy: intentional visual change requires explicit golden refresh documented in PR
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate golden SVG baselines (≥1 per kind)</name>
  <files>packages/cora/tests/golden/box-arrows.svg, packages/cora/tests/golden/flowchart.svg, packages/cora/tests/golden/microservice.svg, packages/cora/tests/golden/infra.svg, packages/cora/tests/golden/database.svg</files>
  <read_first>.planning/phases/02-renderer-svg-export/02-RESEARCH.md, examples/valid/, .planning/ROADMAP.md Phase 2 success criteria</read_first>
  <action>
    Per RESEARCH golden strategy and ROADMAP success criterion #5: render each canonical fixture to packages/cora/tests/golden/{kind}.svg using production pipeline (embedded Inter, ELK options from 02-02, default theme from 02-03).
    Map fixtures: box-arrows.yaml → box-arrows.svg; flowchart.yaml → flowchart.svg; microservice.yaml → microservice.svg; infra.yaml → infra.svg; database.yaml → database.svg.
    Normalize SVG before commit: stable attribute ordering not required but strip trailing whitespace; ensure xmlns present. Golden files are committed artifacts — byte-stable across macOS/Linux CI per D-06.
    Document in render-golden.mjs header comment: to refresh baselines run with UPDATE_GOLDEN=1 env var (executor implements in task 2).
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && ls packages/cora/tests/golden/*.svg | wc -l | grep -q '^5$'</automated>
  </verify>
  <acceptance_criteria>
    - Five golden SVG files exist under packages/cora/tests/golden/
    - Each golden file contains xmlns and viewBox
    - Each golden file size is greater than 200 bytes (non-empty diagram)
    - Golden box-arrows.svg was generated from examples/valid/box-arrows.yaml
  </acceptance_criteria>
  <done>Golden baselines captured for all five v1 diagram kinds.</done>
</task>

<task type="auto">
  <name>Task 2: Golden regression runner script</name>
  <files>packages/cora/tests/render-golden.mjs, packages/cora/package.json</files>
  <read_first>packages/cora/tests/golden/, packages/cora/package.json, .planning/phases/02-renderer-svg-export/02-RESEARCH.md</read_first>
  <action>
    Create tests/render-golden.mjs (Node ESM script, no test framework required): for each kind, run node dist/cli.js render examples/valid/{kind}.yaml to temp file, read golden and output, normalize both (collapse whitespace runs, trim lines), string compare.
    On mismatch: print diff summary (kind name, first differing line index), exit 1. On match: print pass count, exit 0.
    Support UPDATE_GOLDEN=1 to regenerate golden files from current renderer (for intentional visual updates).
    Add package.json script: "test:golden": "node tests/render-golden.mjs" with precondition that build ran (script checks dist/cli.js exists or runs build first).
    Assert structural invariants in runner: output must not contain foreignObject; must contain xmlns.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run build && cd packages/cora && node tests/render-golden.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/render-golden.mjs exits 0 against committed golden files
    - Script exits 1 when golden differs from fresh render (verify by temp edit if needed)
    - package.json includes test:golden script
    - Runner rejects SVG containing foreignObject
  </acceptance_criteria>
  <done>Automated golden regression catches unintended SVG changes.</done>
</task>

<task type="auto">
  <name>Task 3: Render CLI polish + public API export (CLI-05, EXP-01)</name>
  <files>packages/cora/src/cli/commands/render.ts, packages/cora/src/cli/index.ts, packages/cora/src/index.ts, packages/cora/package.json</files>
  <read_first>packages/cora/src/cli/commands/validate.ts, packages/cora/src/cli/output.ts, AGENTS.md, .planning/ROADMAP.md</read_first>
  <action>
    Polish render.ts per CLI-05/EXP-01: commander help text documents `cora render <file> -o <path>` (SVG only in Phase 2 — no PDF flags yet). Validate -o is provided and ends with .svg or accept any path with warning. Write UTF-8 SVG to output path; create parent directories if missing (fs.mkdirSync recursive).
    Error paths: parse errors → exit 1 with message; validation errors → print JSON array when --format json passed (reuse isJsonOutput from output.ts) else human-readable lines matching validate style; layout errors → clear stderr message.
    Export public API from src/index.ts: renderToSVG, computeLayout (or renderDiagram orchestrator function wrapping parse→validate→theme→measure→layout→svg), and LayoutedDiagram type for library consumers. Update package.json exports field with "./renderer" subpath per ARCHITECTURE.md if not present.
    Final verification sweep: bun run build, test:golden, render all fixtures manually once. Update .planning/STATE.md phase position to Phase 2 complete when executor finishes (not in this plan file — executor SUMMARY duty).
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run build && node packages/cora/dist/cli.js render --help | grep -q '\-o' && node packages/cora/dist/cli.js render examples/valid/minimal.yaml -o /tmp/final.svg && test -s /tmp/final.svg && cd packages/cora && node tests/render-golden.mjs</automated>
  </verify>
  <acceptance_criteria>
    - cora render --help mentions -o output option
    - cora render without -o exits non-zero with helpful message
    - packages/cora/src/index.ts exports renderToSVG
    - test:golden passes after build
    - EXP-01 satisfied: render writes SVG via renderToStaticMarkup pipeline
  </acceptance_criteria>
  <done>Phase 2 render CLI and API are production-ready with regression guardrails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Golden files ← renderer output | Committed baselines must match trusted CI render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | golden SVG files | mitigate | String compare in CI; UPDATE_GOLDEN=1 only for intentional refresh |
| T-02-08 | Repudiation | render output | accept | Golden baselines provide audit trail of expected output |
</threat_model>

<verification>
- bun run build && cd packages/cora && node tests/render-golden.mjs exits 0
- cora render each examples/valid/*.yaml -o /tmp/x.svg exits 0
- Phase 2 ROADMAP success criteria 1–5 satisfied
</verification>

<success_criteria>
- Golden regression suite covers all five kinds
- cora render is agent-ready with clear help and errors
- Public API exports render entry points
- Phase 2 requirements complete: LAY-*, REN-*, CLI-05, EXP-01
</success_criteria>

<output>
Create `.planning/phases/02-renderer-svg-export/02-04-SUMMARY.md` when done
</output>
