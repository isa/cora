---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - AGENTS.md
  - README.md
autonomous: true
requirements:
  - AGT-01
user_setup: []

must_haves:
  truths:
    - "AGENTS.md describes validate workflow end-to-end"
    - "AGENTS.md documents JSON error array shape and all Phase 1 error codes"
    - "AGENTS.md documents cora schema usage for agents"
    - "README points agents to AGENTS.md"
  artifacts:
    - path: AGENTS.md
      provides: Agent contract documentation
      min_lines: 80
      contains: "SCHEMA_VIOLATION"
    - path: README.md
      provides: Human entry with agent pointer
      contains: "AGENTS.md"
  key_links:
    - from: AGENTS.md
      to: packages/cora/src/core/schema/diagram.schema.json
      via: "cora schema command reference"
      pattern: "cora schema"
    - from: README.md
      to: AGENTS.md
      via: "link for AI agents"
      pattern: "AGENTS\\.md"
---

## Phase Goal

**As an** AI coding agent, **I want to** read AGENTS.md for workflows and error shapes, **so that** I can integrate Cora validate loops without guessing CLI behavior.

<objective>
Publish the Phase 1 agent contract draft: validate workflow, `cora schema` usage, JSON error schema, error code catalog, and example pointers.

Purpose: AGT-01 — agents discover contract without reading source.
Output: AGENTS.md at repo root; README agent section updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@packages/cora/src/core/errors.ts
@packages/cora/src/cli/commands/validate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AGENTS.md — workflow and machine output contract</name>
  <files>AGENTS.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, packages/cora/src/core/errors.ts, packages/cora/src/cli/commands/validate.ts, .planning/phases/01-foundation/SKELETON.md</read_first>
  <action>
    Create AGENTS.md at repo root (draft, not final Phase 6 polish). Sections required:
    1. Purpose — Cora validates YAML diagrams for architecture agents; Phase 1 is validate+schema only (no render).
    2. Quick start — install `pnpm install && pnpm build`, run `node packages/cora/dist/cli.js` or `cora` after npm link; commands `cora validate path.yaml --format json` and `cora schema`.
    3. Recommended agent loop — write YAML → validate --format json → fix errors by code/path → schema check when adding fields.
    4. JSON error shape — array of objects { code, path, message, suggestion? }; document codes SCHEMA_VIOLATION, MISSING_EDGE_TARGET, UNKNOWN_SERVICE, MISSING_EXTENSION, PARSE_ERROR with one-line meaning each.
    5. TTY behavior — default human when interactive terminal; use --format json in CI; piping stdout forces JSON per implementation.
    6. CI flags — document --yes and CORA_AUTO_INSTALL=1 for future non-interactive installs (CLI-08).
    7. Spec contract — version: 1 required; one diagram per file; kinds list matching schema enum; YAML primary, .json extension for JSON.
    8. Getting schema — `cora schema` or `cora schema --out cora-schema.json`; do not invent fields not in schema.
    9. Examples — table linking examples/valid/* and examples/invalid/* to intended use.
    Do not document render, serve, ext, doctor as available (mark deferred).
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && grep -c 'SCHEMA_VIOLATION' AGENTS.md | grep -qv '^0$' && grep -c 'cora validate' AGENTS.md | grep -qv '^0$' && grep -c 'cora schema' AGENTS.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md exists at repo root
    - AGENTS.md contains section heading or line for error codes listing all five Phase 1 codes
    - AGENTS.md documents JSON array error output with path field
    - AGENTS.md states version: 1 required
  </acceptance_criteria>
  <done>Agent workflow and JSON contract documented in AGENTS.md draft.</done>
</task>

<task type="auto">
  <name>Task 2: README agent pointer and diagram authoring primer</name>
  <files>README.md</files>
  <read_first>AGENTS.md, README.md, .planning/PROJECT.md</read_first>
  <action>
    Create or update README.md: project one-liner (YAML in → validated spec; SVG/PDF in later phases). Installation: Node 22+, pnpm 9, pnpm install, pnpm build. Commands table for Phase 1: validate, schema only.
    Prominent callout: **AI agents: read AGENTS.md first** with link to AGENTS.md.
    Minimal YAML example block showing version 1 and diagram.kind box-arrows (match examples/valid/box-arrows.yaml structure, 8–15 lines).
    License Apache 2.0. Status note: Phase 1 foundation — validate/schema only.
    Do not claim render/serve work yet.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && grep -q 'AGENTS.md' README.md && grep -q 'validate' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md links to AGENTS.md for agents
    - README.md documents validate and schema commands
    - README.md does not claim render is available in Phase 1
  </acceptance_criteria>
  <done>Humans and agents can discover AGENTS.md from README; Phase 1 scope honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs ← implementation | AGENTS.md must match actual CLI flags and error codes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | AGENTS.md | accept | No secrets; only public CLI contract |
</threat_model>

<verification>
- AGENTS.md passes grep gates for codes and commands
- README references AGENTS.md
- Manual: agent could follow AGENTS.md validate loop without reading source
</verification>

<success_criteria>
- AGT-01 satisfied with draft agent documentation
- Phase 1 success criterion #4 from ROADMAP met
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
