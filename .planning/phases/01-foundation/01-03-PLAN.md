---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - packages/cora/src/core/types.ts
  - packages/cora/src/core/validator.ts
  - packages/cora/src/core/errors.ts
  - packages/cora/src/cli/output.ts
  - packages/cora/src/cli/commands/validate.ts
  - packages/cora/src/cli/index.ts
  - examples/valid/minimal.yaml
  - examples/invalid/missing-edge-target.yaml
  - examples/invalid/unknown-service.yaml
  - examples/invalid/service-without-provider.yaml
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-08
  - AGT-03
user_setup: []

must_haves:
  truths:
    - "cora validate --format json emits JSON array of {code, path, message, suggestion?}"
    - "TTY stdout without --format json uses human-readable multi-line errors"
    - "Invalid edge target yields MISSING_EDGE_TARGET"
    - "provider+service without installed extension yields MISSING_EXTENSION (deterministic stub)"
    - "Schema failures use SCHEMA_VIOLATION code"
  artifacts:
    - path: packages/cora/src/core/errors.ts
      provides: Error code constants and semantic validators
      exports: ["ERROR_CODES", "runSemanticValidation"]
    - path: packages/cora/src/cli/output.ts
      provides: JSON and human formatters
      contains: "SCHEMA_VIOLATION"
  key_links:
    - from: packages/cora/src/core/validator.ts
      to: packages/cora/src/core/errors.ts
      via: "runSemanticValidation after AJV"
      pattern: "runSemanticValidation"
    - from: packages/cora/src/cli/commands/validate.ts
      to: packages/cora/src/cli/output.ts
      via: "formatValidationResult(errors, {format})"
      pattern: "formatValidationResult"
---

## Phase Goal

**As an** AI coding agent, **I want to** receive structured validation errors with stable codes and JSON paths, **so that** I can programmatically fix YAML diagrams in a validate loop.

<objective>
Complete the agent validation contract: AJV schema errors mapped to SCHEMA_VIOLATION, semantic checks for edge targets and extension/service references, stable JSON output, and human-readable TTY defaults.

Purpose: Delivers CLI-01–03 and AGT-03; establishes CI-friendly `--format json` and global non-interactive flags (CLI-08).
Output: errors.ts, enhanced validator, negative fixtures, polished validate command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Error codes and AJV error mapping</name>
  <files>packages/cora/src/core/errors.ts, packages/cora/src/core/types.ts, packages/cora/src/core/validator.ts</files>
  <read_first>packages/cora/src/core/validator.ts, packages/cora/src/core/types.ts, .planning/REQUIREMENTS.md</read_first>
  <action>
    errors.ts: export const ERROR_CODES = { SCHEMA_VIOLATION, MISSING_EDGE_TARGET, UNKNOWN_SERVICE, MISSING_EXTENSION, PARSE_ERROR } as const; export type ErrorCode = keyof typeof ERROR_CODES values.
    Extend StructuredError so code uses ErrorCode union.
    validator.ts: after AJV validate, map each Ajv error to { code: SCHEMA_VIOLATION, path: instancePath or empty string, message: ajv message, suggestion optional string for common cases like missing version }.
    Export validateDocument(doc: unknown): StructuredError[] combining schema + semantic phases (semantic stub empty in this task).
    Ensure validate returns stable ordering: schema errors first, then semantic.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && pnpm build && node packages/cora/dist/cli.js validate examples/invalid/missing-version.yaml --format json 2>/dev/null | grep -o '"code":"[^"]*"' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - errors.ts exports MISSING_EDGE_TARGET and SCHEMA_VIOLATION
    - JSON output for missing-version contains "code":"SCHEMA_VIOLATION"
    - Every error object includes code, path, message keys
  </acceptance_criteria>
  <done>Schema validation failures emit stable SCHEMA_VIOLATION codes with JSON paths.</done>
</task>

<task type="auto">
  <name>Task 2: Semantic validators and negative fixtures</name>
  <files>packages/cora/src/core/errors.ts, packages/cora/src/core/validator.ts, examples/valid/minimal.yaml, examples/invalid/missing-edge-target.yaml, examples/invalid/unknown-service.yaml, examples/invalid/service-without-provider.yaml</files>
  <read_first>packages/cora/src/core/schema/diagram.schema.json, .planning/PROJECT.md</read_first>
  <action>
    Implement runSemanticValidation(diagram: DiagramFile['diagram']): StructuredError[]:
    MISSING_EDGE_TARGET: for each edge, if to (or from) id not in node id set, emit path /diagram/edges/N/to (or /from) with suggestion naming valid ids.
    MISSING_EXTENSION: if any node has provider set, check stub extension resolver isExtensionInstalled(provider) returning false in Phase 1; emit MISSING_EXTENSION with suggestion `cora ext install {provider}-theme` (wording per PROJECT.md hard-fail policy). Do not silently ignore.
    UNKNOWN_SERVICE: if node has service without provider, emit UNKNOWN_SERVICE at /diagram/nodes/N/service; if provider set but service not in stub registry (empty registry in Phase 1 — treat any service as unknown when provider present), emit UNKNOWN_SERVICE with suggestion to verify extension manifest.
    missing-edge-target.yaml: valid schema shape but edge to nonexistent node ghost.
    unknown-service.yaml: infra kind node with provider aws service lambda (schema-valid; triggers MISSING_EXTENSION via stub).
    service-without-provider.yaml: node with service set but provider absent (schema-valid; triggers UNKNOWN_SERVICE).
    Confirm examples/valid/minimal.yaml still has edge endpoints matching node ids (api and db); patch if plan 01 left a dangling edge target.
    Wire runSemanticValidation into validateDocument after AJV passes.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && pnpm build && node packages/cora/dist/cli.js validate examples/invalid/missing-edge-target.yaml --format json 2>/dev/null | grep -q MISSING_EDGE_TARGET && node packages/cora/dist/cli.js validate examples/invalid/unknown-service.yaml --format json 2>/dev/null | grep -q MISSING_EXTENSION && node packages/cora/dist/cli.js validate examples/invalid/service-without-provider.yaml --format json 2>/dev/null | grep -q UNKNOWN_SERVICE && for f in examples/valid/*.yaml; do node packages/cora/dist/cli.js validate "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - missing-edge-target fixture triggers MISSING_EDGE_TARGET in JSON output
    - unknown-service fixture triggers MISSING_EXTENSION in JSON output
    - service-without-provider fixture triggers UNKNOWN_SERVICE in JSON output
    - All examples/valid/*.yaml exit 0 after semantic validation lands
    - Semantic validation runs only when AJV reports valid document
  </acceptance_criteria>
  <done>All four AGT-03 minimum error codes are producible by validate.</done>
</task>

<task type="auto">
  <name>Task 3: CLI output modes and CI non-interactive flags</name>
  <files>packages/cora/src/cli/output.ts, packages/cora/src/cli/commands/validate.ts, packages/cora/src/cli/index.ts</files>
  <read_first>packages/cora/src/cli/output.ts, .planning/ROADMAP.md, .planning/research/STACK.md</read_first>
  <action>
    validate command: --format json|text (default text). When format text and stdout is TTY, use picocolors human layout: first line `✖ N errors in [file]`, then each error `  [CODE] path: message`. When format json OR stdout not TTY, print JSON.stringify(errors, null, 2) only — no extra prose (agent-parseable).
    Root program option --yes: boolean, default false. Read env CORA_AUTO_INSTALL=1 as equivalent to --yes true for future download prompts; in Phase 1 store on program.opts() for downstream commands (no prompts yet).
    PARSE_ERROR: on yaml/json parse failure, emit single error code PARSE_ERROR path empty message includes parser detail; exit 1.
    Document in code comment that CI should use --format json or pipe stdout to force JSON.
    Smoke: script exit codes — valid 0, invalid 1, parse error 1.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && pnpm build && node packages/cora/dist/cli.js validate examples/valid/box-arrows.yaml --format json | node -e "let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{const j=JSON.parse(s);process.exit(Array.isArray(j)&&j.length===0?0:1)})" && printf 'test' | node packages/cora/dist/cli.js validate examples/invalid/missing-version.yaml 2>/dev/null | head -1 | grep -q '^\['</automated>
  </verify>
  <acceptance_criteria>
    - validate --format json on valid file prints `[]` or empty array
    - Piped non-TTY validate outputs JSON array (starts with [)
    - cli/index.ts registers global --yes option
    - cli/index.ts reads process.env.CORA_AUTO_INSTALL
  </acceptance_criteria>
  <done>CLI-01–03 and CLI-08 satisfied; agent-hostile output pitfall addressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent ← CLI JSON | Untrusted content is error messages only; must be valid JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Spoofing | extension stub | accept | Phase 1 stub always reports missing; Phase 5 adds real loader |
| T-01-03-02 | DoS | large YAML files | accept | v1 no size cap; document reasonable limits in AGENTS.md |
</threat_model>

<verification>
- `cora validate examples/invalid/missing-edge-target.yaml --format json` contains MISSING_EDGE_TARGET
- `cora validate examples/valid/box-arrows.yaml` exits 0 with `[]` JSON
- Human mode shows colored output when run in terminal (manual spot-check optional)
</verification>

<success_criteria>
- Structured JSON error shape stable and documented-ready
- All AGT-03 codes emitted by real validators
- TTY vs JSON routing per ROADMAP pitfall
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
