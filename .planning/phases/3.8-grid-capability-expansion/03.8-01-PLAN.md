---
phase: 3.8-grid-capability-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cora/src/core/grid.ts
  - packages/cora/src/core/index.ts
  - packages/cora/src/core/types.ts
  - packages/cora/src/core/schema/diagram.schema.json
  - packages/cora/tests/core/grid.test.ts
autonomous: true
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
requirements_addressed:
  - GRID-01
  - GRID-02
  - GRID-03
decision_refs:
  - D-01
  - D-02
  - D-03
  - D-13
  - D-14
  - D-15

must_haves:
  truths:
    - "D-01: DEFAULT_GRID_SPACING === 16"
    - "D-02: Snap math uses origin (0, 0) — snapScalar rounds relative to origin 0"
    - "D-03: diagram.schema.json accepts optional diagram.grid with spacing, majorEvery, visible"
    - "D-14: Grid helpers live in packages/cora/src/core/grid.ts"
    - "D-15: snapPoint and resolveGridConfig exported from packages/cora/src/core/index.ts"
    - "cora validate accepts YAML with diagram.grid and rejects invalid grid types"
  artifacts:
    - path: packages/cora/src/core/grid.ts
      provides: Pure snap/round helpers and default constants
      exports: ["DEFAULT_GRID_SPACING", "resolveGridConfig", "snapPoint", "snapSize", "snapScalar"]
    - path: packages/cora/tests/core/grid.test.ts
      provides: Unit coverage for snap math edge cases
  key_links:
    - from: packages/cora/src/core/index.ts
      to: packages/cora/src/core/grid.ts
      via: re-export
      pattern: "snapPoint"
---

## Phase Goal

**As a** Phase 4 canvas and preview implementer, **I want** shared grid configuration and snap math in core, **so that** edit surfaces snap consistently without duplicating rounding logic.

<objective>
Create `core/grid.ts`, extend schema and Diagram types with optional `diagram.grid`, export public helpers via `cora/core`, and lock behavior with unit tests.

Purpose: Foundation for preview snap wiring (Plan 03) and Phase 4 serve canvas reuse.
Output: grid.ts, schema update, types update, grid.test.ts, core barrel exports.
</objective>

<context>
@.planning/phases/3.8-grid-capability-expansion/03.8-CONTEXT.md
@.planning/phases/3.8-grid-capability-expansion/03.8-RESEARCH.md
@.planning/phases/3.8-grid-capability-expansion/03.8-PATTERNS.md
</context>

<tasks>

<task type="auto" id="3.8-01-01">
  <name>Implement core grid module with defaults and snap helpers</name>
  <files>packages/cora/src/core/grid.ts</files>
  <read_first>
    - packages/cora/src/core/edgeGeometry.ts
    - .planning/phases/3.8-grid-capability-expansion/03.8-CONTEXT.md
  </read_first>
  <action>
Create packages/cora/src/core/grid.ts with:

- DEFAULT_GRID_SPACING = 16
- DEFAULT_GRID_MAJOR_EVERY = 5
- DEFAULT_GRID_ORIGIN = { x: 0, y: 0 }
- GridConfig interface: spacing, majorEvery, visible (all required on resolved output)
- resolveGridConfig(partial?) merging defaults
- snapScalar(value, spacing, origin = 0): round to nearest cell boundary from origin
- snapPoint({x,y}, config?): snap both axes using config.spacing and origin 0
- snapSize({width,height}, config?): snap width and height independently (minimum 1 cell after snap)

No DOM/React imports.
  </action>
  <acceptance_criteria>
- packages/cora/src/core/grid.ts exports DEFAULT_GRID_SPACING equal to 16
- snapScalar(7, 16) === 0 and snapScalar(9, 16) === 16
- snapPoint({ x: 10, y: 25 }, resolveGridConfig()) === { x: 16, y: 32 }
- cd packages/cora && bun run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun run typecheck</automated>
  </verify>
  <done>Core grid math module exists with documented defaults</done>
</task>

<task type="auto" id="3.8-01-02">
  <name>Add diagram.grid to JSON Schema and Diagram type</name>
  <files>packages/cora/src/core/schema/diagram.schema.json, packages/cora/src/core/types.ts</files>
  <read_first>
    - packages/cora/src/core/schema/diagram.schema.json
    - packages/cora/src/core/types.ts
    - packages/cora/src/core/grid.ts
  </read_first>
  <action>
Add optional `grid` property to diagramBase in diagram.schema.json:

- grid.type object, additionalProperties false
- spacing: number, minimum 1, default semantics documented in resolveGridConfig only
- majorEvery: integer, minimum 1
- visible: boolean

Add to Diagram interface in types.ts:

```typescript
grid?: { spacing?: number; majorEvery?: number; visible?: boolean };
```

Do not wire grid into layout.ts or render pipeline in this plan.
  </action>
  <acceptance_criteria>
- diagram.schema.json diagramBase.properties includes grid
- Diagram type in types.ts includes optional grid field
- bun run cora schema output includes grid under diagram properties
- cora validate on a fixture with diagram.grid.spacing: 16 returns [] with --format json
- cora validate rejects diagram.grid.spacing: "sixteen" with SCHEMA_VIOLATION
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run cora schema | rg '"grid"'</automated>
  </verify>
  <done>Schema and types accept optional diagram.grid</done>
</task>

<task type="auto" id="3.8-01-03">
  <name>Export grid helpers from cora/core and add unit tests</name>
  <files>packages/cora/src/core/index.ts, packages/cora/tests/core/grid.test.ts</files>
  <read_first>
    - packages/cora/src/core/index.ts
    - packages/cora/src/core/grid.ts
    - packages/cora/tests/core/connection-anchors.test.ts
  </read_first>
  <action>
Re-export from core/index.ts: DEFAULT_GRID_SPACING, DEFAULT_GRID_MAJOR_EVERY, resolveGridConfig, snapScalar, snapPoint, snapSize, GridConfig type.

Add tests/core/grid.test.ts covering:
- resolveGridConfig() defaults (spacing 16, majorEvery 5, visible true)
- resolveGridConfig({ spacing: 8 }) override
- snapScalar at boundaries (0, 8, 16, negative inputs)
- snapPoint round-trip for several coordinates
- snapSize never returns zero width/height when input >= 1
  </action>
  <acceptance_criteria>
- packages/cora/src/core/index.ts exports snapPoint
- cd packages/cora && bun x vitest run tests/core/grid.test.ts exits 0
- cd packages/cora && bun run build exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun x vitest run tests/core/grid.test.ts && bun run build</automated>
  </verify>
  <done>Grid helpers are public via cora/core with passing tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML grid config → validator | Numeric bounds only; no code execution |
| core/grid → render paths | Grid module must not be imported by SVG/PDF/text renderers in later plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.8-01 | Tampering | Invalid spacing in YAML | mitigate | Schema minimum 1; resolveGridConfig clamps if needed |
| T-3.8-02 | Tampering | Accidental render-path grid bleed | mitigate | Plan 04 asserts no grid imports in renderToSVG/renderToText |
</threat_model>

<verification>
cd packages/cora && bun run typecheck && bun x vitest run tests/core/grid.test.ts && bun run build
</verification>

<success_criteria>
- core/grid.ts exists with 16px defaults and origin (0,0) snap math
- diagram.grid validates in schema
- cora/core exports grid helpers
- Unit tests pass
</success_criteria>

<output>
Create `.planning/phases/3.8-grid-capability-expansion/03.8-01-SUMMARY.md` when done
</output>
