---
phase: 3.8-grid-capability-expansion
plan: 04
type: execute
wave: 3
depends_on:
  - 03.8-02
  - 03.8-03
files_modified:
  - examples/valid/grid-config.yaml
  - AGENTS.md
  - packages/cora/tests/core/grid-render.test.ts
autonomous: true
requirements:
  - GRID-06
  - GRID-07
  - GRID-08
requirements_addressed:
  - GRID-06
  - GRID-07
  - GRID-08
decision_refs:
  - D-09
  - D-10
  - D-11
  - D-12
  - D-16

must_haves:
  truths:
    - "D-09: SVG/PDF/PNG render output has no grid background"
    - "D-10: ELK auto-layout output unchanged when diagram.grid present in YAML"
    - "D-11: AGENTS.md documents Phase 4 save-time snap rounding contract"
    - "D-12: Text export tests pass unchanged"
    - "D-16: Example YAML demonstrates diagram.grid; docs describe cora/core grid exports"
  artifacts:
    - path: examples/valid/grid-config.yaml
      provides: Valid fixture with diagram.grid block
    - path: AGENTS.md
      provides: Grid Capability section for agents and Phase 4 handoff
    - path: packages/cora/tests/core/grid-render.test.ts
      provides: Assert layout/render ignore grid for static output
  key_links:
    - from: AGENTS.md
      to: packages/cora/src/core/grid.ts
      via: documents snapPoint and schema fields
      pattern: "diagram.grid"
---

## Phase Goal

**As a** Cora agent and Phase 4 implementer, **I want** documented grid semantics and regression gates, **so that** grid capabilities do not leak into static exports or break existing layouts.

<objective>
Add example fixture, render/layout regression tests, and AGENTS.md grid contract documenting schema fields, snap behavior, export boundaries, and Phase 4 save rounding.

Purpose: Close Phase 3.8 full-stack delivery (D-16) with verifiable export safety.
Output: grid-config.yaml, grid-render.test.ts, AGENTS.md section.
</objective>

<context>
@.planning/phases/3.8-grid-capability-expansion/03.8-CONTEXT.md
@.planning/phases/3.8-grid-capability-expansion/03.8-RESEARCH.md
</context>

<tasks>

<task type="auto" id="3.8-04-01">
  <name>Add grid-config example and render/layout regression tests</name>
  <files>examples/valid/grid-config.yaml, packages/cora/tests/core/grid-render.test.ts</files>
  <read_first>
    - examples/valid/minimal.yaml
    - packages/cora/src/core/layout.ts
    - packages/cora/src/renderer/renderToSVG.tsx
    - packages/cora/tests/renderer/text.test.ts
  </read_first>
  <action>
Create examples/valid/grid-config.yaml:
- version: 1
- diagram.kind box-arrows
- diagram.grid: { spacing: 16, majorEvery: 5, visible: true }
- Two box nodes with edges (minimal topology)

Create tests/core/grid-render.test.ts:
1. validate grid-config.yaml succeeds
2. Render SVG from grid-config.yaml — output must NOT contain grid pattern ids or "grid" background rects (assert absence of known preview pattern names if any)
3. Render same diagram without grid block — SVG node positions identical to with-grid case (proves D-10 ELK/layout ignore grid)
4. Text render output identical with/without diagram.grid (D-12)

Use existing render pipeline imports from test helpers patterns in tests/renderer/.
  </action>
  <acceptance_criteria>
- examples/valid/grid-config.yaml exists and passes cora validate --format json
- grid-render.test.ts asserts layout positions equal with and without diagram.grid
- cd packages/cora && bun x vitest run tests/core/grid-render.test.ts exits 0
- cd packages/cora && node tests/render-golden.mjs exits 0 without UPDATE_GOLDEN
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && bun run cora validate examples/valid/grid-config.yaml --format json && cd packages/cora && bun x vitest run tests/core/grid-render.test.ts</automated>
  </verify>
  <done>Grid in YAML does not affect static layout or render output</done>
</task>

<task type="auto" id="3.8-04-02">
  <name>Document grid contract in AGENTS.md</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/3.8-grid-capability-expansion/03.8-CONTEXT.md
    - packages/cora/src/core/grid.ts
  </read_first>
  <action>
Add AGENTS.md section "Grid Capability" covering:

- Optional diagram.grid YAML fields (spacing default 16, majorEvery default 5, visible for edit surfaces)
- Grid is edit-time only — SVG/PDF/PNG/text exports never show grid
- ELK auto-layout does not snap to grid
- Preview workbench: snap toggle default ON, Shift disables temporarily, nodes/groups snap live
- Phase 4 cora serve will reuse core/grid snapPoint on save writeback (positions rounded before YAML patch) — document contract, do not implement save here
- Public API: import snapPoint, resolveGridConfig from cora/core

Keep section concise; link to examples/valid/grid-config.yaml.
  </action>
  <acceptance_criteria>
- AGENTS.md contains heading or section title including Grid
- AGENTS.md mentions diagram.grid and cora/core grid exports
- AGENTS.md states static exports do not render grid
- AGENTS.md mentions Phase 4 save-time rounding without claiming it is implemented
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "diagram\\.grid|cora/core|Phase 4" AGENTS.md</automated>
  </verify>
  <done>Grid semantics documented for agents and Phase 4</done>
</task>

<task type="auto" id="3.8-04-03">
  <name>Full phase regression verification</name>
  <files>packages/cora/tests/core/grid.test.ts</files>
  <read_first>
    - packages/cora/package.json scripts
  </read_first>
  <action>
Run full verification suite:
- bun run typecheck
- bun x vitest run tests/core/grid.test.ts tests/core/grid-render.test.ts tests/preview/grid-snap.test.ts tests/renderer/text.test.ts
- node tests/render-golden.mjs

Fix any failures from Plans 01–03 without changing golden baselines.
  </action>
  <acceptance_criteria>
- cd packages/cora && bun run typecheck exits 0
- cd packages/cora && bun x vitest run exits 0 (or at minimum all grid + text tests pass)
- node tests/render-golden.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun run typecheck && bun x vitest run && node tests/render-golden.mjs</automated>
  </verify>
  <done>Phase 3.8 regression gates green</done>
</task>

</tasks>

<verification>
cd packages/cora && bun run typecheck && bun x vitest run tests/core/grid.test.ts tests/core/grid-render.test.ts tests/preview/grid-snap.test.ts tests/renderer/text.test.ts && node tests/render-golden.mjs
</verification>

<success_criteria>
- Example YAML with diagram.grid validates
- Static render/layout/text unaffected by grid config
- AGENTS.md documents grid contract and Phase 4 handoff
- Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/3.8-grid-capability-expansion/03.8-04-SUMMARY.md` when done
</output>
