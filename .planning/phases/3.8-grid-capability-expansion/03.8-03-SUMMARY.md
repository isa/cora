# Plan 03.8-03 Summary

**Status:** Complete

## Delivered

- Snap toolbar toggle (default ON) with `grid_on` / `grid_off` icons
- Shift temporarily disables snap
- Live `snapPoint` / `snapSize` in node, multi-node, group move/resize handlers
- Connection endpoints remain unsnapped
- `packages/cora/src/preview/gridSnap.ts` + `tests/preview/grid-snap.test.ts`

## Verification

- `bun x vitest run tests/preview/grid-snap.test.ts tests/core/grid.test.ts` — pass
- `bun run typecheck` — pass
