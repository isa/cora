# Phase 3.8: Grid Capability Expansion - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-29
**Phase:** 3.8-grid-capability-expansion
**Areas discussed:** Grid coordinate model, Snap behavior policy, Static render path, Preview vs shared core split

---

## Grid coordinate model

| Option | Description | Selected |
|--------|-------------|----------|
| 24px | Match existing preview dotted canvas | |
| 16px | Finer control, common design-tool spacing | ✓ |
| 8px | Finest snap | |
| (0,0) fixed origin | World coords for YAML persistence | ✓ (Claude discretion) |
| Viewport-relative origin | Follows pan/zoom | |
| Add diagram.grid to schema now | Optional YAML grid config | ✓ |
| Defer schema to Phase 4 | Hardcode spacing only | |
| Major lines every 5 cells | 80px hierarchy at 16px spacing | ✓ |

**User's choice:** 16px cells; schema now; major every 5; origin delegated to Claude.
**Notes:** Origin locked as (0,0) fixed world coordinates for YAML/Phase 4 save consistency.

---

## Snap behavior policy

| Option | Description | Selected |
|--------|-------------|----------|
| Toolbar toggle default ON | Snap on/off control | ✓ |
| Shift override | Temporarily disable snap while toggle on | ✓ (combined with toggle) |
| Nodes + groups | Move and resize snap | ✓ |
| Connection endpoints free | No endpoint grid snap | ✓ |
| Grid only | No alignment guides | ✓ |
| Live during drag | Continuous snap while dragging | ✓ |

**User's choice:** Toggle default ON + Shift disables snap; nodes and groups only; grid-only; live snap.
**Notes:** User requested both toggle (B) and Shift-modifier together.

---

## Static render path

| Option | Description | Selected |
|--------|-------------|----------|
| Edit-time only exports | No grid in SVG/PDF/PNG | ✓ |
| ELK never snap by default | Preserve layout quality & goldens | ✓ (Claude discretion) |
| Snap on save (Phase 4) | Round YAML positions at writeback | ✓ |
| Text unaffected | No grid in ASCII export | ✓ |

**User's choice:** Edit-only grid; ELK delegated; save-time rounding semantics for Phase 4; text untouched.
**Notes:** preserve/hybrid positions render exactly until Phase 4 canvas save.

---

## Preview vs shared core split

| Option | Description | Selected |
|--------|-------------|----------|
| Core module first | Build grid.ts then preview | ✓ |
| core/grid.ts location | Alongside layout in core | ✓ |
| Public via cora/core subpath | Extension/canvas reuse | ✓ |
| Full-stack delivery | Schema + core + preview + tests + docs | ✓ |

**User's choice:** Core-first, public core subpath, full Phase 3.8 stack.

---

## Claude's Discretion

- Grid origin: (0, 0) fixed world coordinates
- ELK auto-layout: no post-layout snap by default
- Exact `diagram.grid` schema field names and preview grid rendering approach

## Deferred Ideas

- Alignment guides to other nodes — Phase 4
- Connection endpoint grid snap — Phase 3.8 out of scope
- Visible grid in static exports — rejected
- Optional ELK snapLayout opt-in — defer unless trivial
