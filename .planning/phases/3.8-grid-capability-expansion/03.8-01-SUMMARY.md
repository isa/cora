# Plan 03.8-01 Summary

**Status:** Complete

## Delivered

- `packages/cora/src/core/grid.ts` — defaults (16px spacing, major every 5), `resolveGridConfig`, `snapScalar`, `snapPoint`, `snapSize`
- Optional `diagram.grid` in JSON Schema and `Diagram` type
- Public exports via `cora/core`
- `packages/cora/tests/core/grid.test.ts`

## Verification

- `bun run typecheck` — pass
- `bun x vitest run tests/core/grid.test.ts` — pass
- `bun run build` — pass
