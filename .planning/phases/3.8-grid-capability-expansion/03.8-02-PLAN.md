---
phase: 3.8-grid-capability-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 03.8-01
files_modified:
  - packages/cora/src/preview/components/WorkbenchCanvas.tsx
  - packages/cora/src/preview/styles.css
autonomous: true
requirements:
  - GRID-04
requirements_addressed:
  - GRID-04
decision_refs:
  - D-01
  - D-04
  - D-09

must_haves:
  truths:
    - "D-01: Preview visual grid uses 16px cell spacing (not 24px CSS)"
    - "D-04: Major grid lines every 5 cells (80px at default spacing)"
    - "D-09: Grid visuals are edit-surface only — no changes to renderer SVG output"
    - "Grid pattern aligns to world coordinates and moves correctly with pan/zoom"
  artifacts:
    - path: packages/cora/src/preview/components/WorkbenchCanvas.tsx
      provides: SVG grid pattern defs and background rect using resolveGridConfig
    - path: packages/cora/src/preview/styles.css
      provides: Updated or removed 24px CSS dot fallback
  key_links:
    - from: packages/cora/src/preview/components/WorkbenchCanvas.tsx
      to: packages/cora/src/core/grid.ts
      via: resolveGridConfig import
      pattern: "resolveGridConfig"
---

## Phase Goal

**As a** preview workbench user, **I want** the canvas background grid to match the 16px snap grid with readable major lines, **so that** what I see aligns with where nodes snap.

<objective>
Replace the decorative 24px CSS dot grid with a world-aligned SVG grid pattern driven by core grid constants (16px spacing, major every 5 cells).

Purpose: Visual parity between grid affordance and snap math before live snap wiring.
Output: WorkbenchCanvas grid overlay + CSS cleanup.
</objective>

<context>
@.planning/phases/3.8-grid-capability-expansion/03.8-CONTEXT.md
@.planning/phases/3.8-grid-capability-expansion/03.8-RESEARCH.md
</context>

<tasks>

<task type="auto" id="3.8-02-01">
  <name>Add SVG world-aligned grid pattern to WorkbenchCanvas</name>
  <files>packages/cora/src/preview/components/WorkbenchCanvas.tsx</files>
  <read_first>
    - packages/cora/src/preview/components/WorkbenchCanvas.tsx
    - packages/cora/src/core/grid.ts
    - packages/cora/src/preview/styles.css
  </read_first>
  <action>
Import resolveGridConfig, DEFAULT_GRID_SPACING, DEFAULT_GRID_MAJOR_EVERY from core/grid.js.

Inside the SVG (before diagram content), add:
- defs with two patterns: minor grid (spacing px) and major grid (spacing * majorEvery px)
- Minor lines: subtle --preview-grid-dot color at 1px
- Major lines: slightly stronger stroke at majorEvery interval
- Background rect filling viewBox using minor pattern; overlay major pattern OR combined pattern

Grid must use diagram/world coordinates (pattern units userSpaceOnUse) so pan/zoom via viewBox keeps alignment.

Respect grid.visible from resolveGridConfig — when false, hide grid layers (default visible true).

Remove dependency on misaligned CSS-only dots for coordinate alignment; CSS may keep flat --preview-bg fill only.
  </action>
  <acceptance_criteria>
- WorkbenchCanvas imports resolveGridConfig from core/grid
- SVG contains pattern id for grid with width/height matching DEFAULT_GRID_SPACING (16)
- Major line interval uses DEFAULT_GRID_MAJOR_EVERY (5) * spacing
- packages/cora/src/preview/styles.css no longer uses 24px in .canvas-region background gradient (16px or no dot gradient)
- cd packages/cora && bun run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && ! rg "24px 24px" src/preview/styles.css && bun run typecheck</automated>
  </verify>
  <done>Preview canvas shows 16px grid with major lines every 5 cells</done>
</task>

<task type="auto" id="3.8-02-02">
  <name>Verify static render paths unchanged</name>
  <files>packages/cora/tests/renderer/render-golden.mjs</files>
  <read_first>
    - packages/cora/src/renderer/renderToSVG.tsx
    - packages/cora/tests/renderer/text.test.ts
  </read_first>
  <action>
Confirm preview grid changes do not import or modify renderToSVG.tsx, renderToPDF.ts, or renderToText.ts.

Run golden and text tests as smoke check (no baseline update expected).
  </action>
  <acceptance_criteria>
- rg "grid" packages/cora/src/renderer/renderToSVG.tsx returns no diagram grid background code
- cd packages/cora && bun x vitest run tests/renderer/text.test.ts exits 0
- cd packages/cora && node tests/render-golden.mjs exits 0 without UPDATE_GOLDEN
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun x vitest run tests/renderer/text.test.ts && node tests/render-golden.mjs</automated>
  </verify>
  <done>SVG goldens and text export unchanged by preview-only grid visuals</done>
</task>

</tasks>

<verification>
cd packages/cora && bun run typecheck && bun x vitest run tests/renderer/text.test.ts && node tests/render-golden.mjs
</verification>

<success_criteria>
- Preview grid is 16px with major lines every 5 cells
- Grid aligns under pan/zoom
- No static export regression
</success_criteria>

<output>
Create `.planning/phases/3.8-grid-capability-expansion/03.8-02-SUMMARY.md` when done
</output>
