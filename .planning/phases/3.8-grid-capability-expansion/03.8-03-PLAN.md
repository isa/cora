---
phase: 3.8-grid-capability-expansion
plan: 03
type: execute
wave: 2
depends_on:
  - 03.8-01
files_modified:
  - packages/cora/src/preview/components/WorkbenchCanvas.tsx
  - packages/cora/tests/preview/grid-snap.test.ts
autonomous: true
requirements:
  - GRID-05
requirements_addressed:
  - GRID-05
decision_refs:
  - D-05
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "D-05: Toolbar snap toggle default ON; Shift temporarily disables snap"
    - "D-06: Nodes and groups snap on move and resize; connection endpoints stay free"
    - "D-07: Grid-only snap — no alignment guides"
    - "D-08: Live snap during drag via snapPoint before setNodePosition/setGroupPosition"
    - "Preview imports snapPoint from core/grid — no duplicate snap math"
  artifacts:
    - path: packages/cora/src/preview/components/WorkbenchCanvas.tsx
      provides: Snap toggle UI, shift tracking, snap injection in drag handlers
    - path: packages/cora/tests/preview/grid-snap.test.ts
      provides: Unit tests for shouldSnap helper and drag snap integration seam
  key_links:
    - from: packages/cora/src/preview/components/WorkbenchCanvas.tsx
      to: packages/cora/src/core/grid.ts
      via: snapPoint in onPointerMove
      pattern: "snapPoint\\("
---

## Phase Goal

**As a** preview workbench user arranging components, **I want** nodes and groups to snap to the grid while dragging, **so that** layouts align cleanly before Phase 4 YAML save.

<objective>
Wire core snap helpers into WorkbenchCanvas drag/resize paths with toolbar toggle (default on), Shift override, and live continuous snapping.

Purpose: Deliver edit-time grid behavior locked in CONTEXT D-05–D-08.
Output: Toolbar control, keyboard modifier handling, snap in drag handlers, tests.
</objective>

<context>
@.planning/phases/3.8-grid-capability-expansion/03.8-CONTEXT.md
@.planning/phases/3.8-grid-capability-expansion/03.8-RESEARCH.md
@.planning/phases/3.8-grid-capability-expansion/03.8-PATTERNS.md
</context>

<tasks>

<task type="auto" id="3.8-03-01">
  <name>Add snap toggle and Shift modifier state to WorkbenchCanvas</name>
  <files>packages/cora/src/preview/components/WorkbenchCanvas.tsx</files>
  <read_first>
    - packages/cora/src/preview/components/WorkbenchCanvas.tsx
    - packages/cora/src/core/grid.ts
  </read_first>
  <action>
Add React state snapEnabled default true.

Add isShiftDown state; extend existing window keydown/keyup listeners (alongside Space for pan) to track event.shiftKey.

Add helper:
```typescript
function shouldSnap(snapEnabled: boolean, shiftDown: boolean): boolean {
  return snapEnabled && !shiftDown;
}
```

Add toolbar button after Pan tool:
- Icon: material-symbols grid_on when enabled, grid_off when disabled
- className includes active when snapEnabled
- aria-pressed={snapEnabled}
- title: "Snap to grid (hold Shift to temporarily disable)"
- onClick toggles snapEnabled

Export shouldSnap to tests/preview/grid-snap.test.ts if extracted to preview/gridSnap.ts (optional small module — prefer preview/gridSnap.ts if WorkbenchCanvas is too large).
  </action>
  <acceptance_criteria>
- WorkbenchCanvas contains snapEnabled state initialized to true
- Toolbar contains button with aria-label mentioning snap or grid
- shouldSnap(true, true) === false and shouldSnap(true, false) === true
- cd packages/cora && bun run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && rg "snapEnabled|shouldSnap|grid_on" src/preview/components/WorkbenchCanvas.tsx</automated>
  </verify>
  <done>Snap toggle and Shift override plumbing exists</done>
</task>

<task type="auto" id="3.8-03-02">
  <name>Apply live snap in node/group move and resize handlers</name>
  <files>packages/cora/src/preview/components/WorkbenchCanvas.tsx</files>
  <read_first>
    - packages/cora/src/preview/components/WorkbenchCanvas.tsx
    - packages/cora/src/core/grid.ts
    - packages/cora/src/preview/state.ts
  </read_first>
  <action>
Import snapPoint, snapSize, resolveGridConfig from core/grid.js.
const gridConfig = resolveGridConfig() in component scope (or memo).

In onPointerMove when shouldSnap is true, apply before state updates:

1. Single node drag: snap position with snapPoint
2. Multi node drag: snap each computed position with snapPoint
3. Group drag: snap group position with snapPoint
4. node-resize: after computing new size/position from resizeRef, snap width/height with snapSize and position with snapPoint; preserve center-anchored resize logic
5. group-resize: same as node-resize for group bounds

Do NOT snap for dragTarget kinds: connection-endpoint, pan, marquee.

Connection endpoint drags remain raw toCanvasPoint values (D-06).
  </action>
  <acceptance_criteria>
- onPointerMove node drag path calls snapPoint when shouldSnap is true
- connection-endpoint drag path does not call snapPoint
- group-resize path calls snapSize or snapPoint when shouldSnap is true
- Holding Shift (isShiftDown true) skips snapPoint calls even when snapEnabled true
- cd packages/cora && bun run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && rg "snapPoint|snapSize" src/preview/components/WorkbenchCanvas.tsx</automated>
  </verify>
  <done>Live grid snap applies to nodes and groups during drag/resize</done>
</task>

<task type="auto" id="3.8-03-03">
  <name>Add preview grid snap unit tests</name>
  <files>packages/cora/tests/preview/grid-snap.test.ts</files>
  <read_first>
    - packages/cora/src/core/grid.ts
    - packages/cora/tests/core/grid.test.ts
  </read_first>
  <action>
Create tests/preview/grid-snap.test.ts testing:

- shouldSnap helper truth table (4 combinations)
- Integration-style test: given raw drag delta producing position {x:10,y:10}, snapped output equals snapPoint result with default config
- Document in test comment that connection endpoints are intentionally unsnapped (D-06)

If shouldSnap lives inside WorkbenchCanvas, extract to packages/cora/src/preview/gridSnap.ts and test that module directly.
  </action>
  <acceptance_criteria>
- packages/cora/tests/preview/grid-snap.test.ts exists
- cd packages/cora && bun x vitest run tests/preview/grid-snap.test.ts exits 0
- cd packages/cora && bun x vitest run tests/core/grid.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd packages/cora && bun x vitest run tests/preview/grid-snap.test.ts tests/core/grid.test.ts</automated>
  </verify>
  <done>Snap UX covered by automated tests</done>
</task>

</tasks>

<verification>
cd packages/cora && bun run typecheck && bun x vitest run tests/core/grid.test.ts tests/preview/grid-snap.test.ts
</verification>

<success_criteria>
- Snap toggle default ON with Shift override
- Nodes and groups snap live during drag/resize
- Connection endpoints remain free-form
- Tests pass
</success_criteria>

<output>
Create `.planning/phases/3.8-grid-capability-expansion/03.8-03-SUMMARY.md` when done
</output>
