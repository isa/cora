---
phase: 3.1-renderer-component-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cora/src/renderer/components/index.ts
  - packages/cora/src/renderer/components/types.ts
  - packages/cora/src/renderer/components/nodes/BoxNode.tsx
  - packages/cora/src/renderer/components/nodes/RoundedNode.tsx
  - packages/cora/src/renderer/components/nodes/CylinderNode.tsx
  - packages/cora/src/renderer/components/nodes/CloudNode.tsx
  - packages/cora/src/renderer/components/nodes/DiamondNode.tsx
  - packages/cora/src/renderer/components/nodes/HexagonNode.tsx
  - packages/cora/src/renderer/components/nodes/shared.tsx
  - packages/cora/src/renderer/components/edges/Arrow.tsx
  - packages/cora/src/renderer/components/edges/EdgeLabel.tsx
  - packages/cora/src/renderer/components/edges/decorations.ts
  - packages/cora/src/renderer/components/groups/Group.tsx
  - packages/cora/src/renderer/Diagram.tsx
  - packages/cora/src/renderer/renderToSVG.tsx
  - packages/cora/src/renderer/index.ts
  - packages/cora/src/core/measureText.ts
  - packages/cora/package.json
  - packages/cora/tests/golden/box-arrows.svg
  - packages/cora/tests/golden/database.svg
  - packages/cora/tests/golden/flowchart.svg
  - packages/cora/tests/golden/infra.svg
  - packages/cora/tests/golden/microservice.svg
  - AGENTS.md
autonomous: true
requirements:
  - REN-01
  - REN-02
  - REN-03
  - REN-05
  - REN-06
  - REN-08
tags: [renderer, components, refactor, svg, public-api]

must_haves:
  truths:
    - "Component implementations live under `packages/cora/src/renderer/components/`, organized by family"
    - "`Diagram.tsx` imports renderer components only from `./components/index.js`"
    - "`NodeComponentProps`, `EdgeComponentProps`, and `GroupComponentProps` are exported from `renderer/components/index.ts`"
    - "The component prop interfaces carry current `LayoutedNode`, `LayoutedEdge`, `LayoutedGroup`, and `ThemeTokens` values"
    - "Existing SVG render behavior remains valid for all five golden fixtures after baseline refresh"
    - "`AGENTS.md` documents the Renderer Components contract and `cora/renderer/components` import target"
  artifacts:
    - path: "packages/cora/src/renderer/components/index.ts"
      provides: "Public component barrel"
      contains: "NodeComponentProps"
    - path: "packages/cora/package.json"
      provides: "Package subpath export for component consumers"
      contains: "./renderer/components"
    - path: "AGENTS.md"
      provides: "Agent-facing renderer component contract"
      contains: "Renderer Components"
  key_links:
    - from: "packages/cora/src/renderer/Diagram.tsx"
      to: "packages/cora/src/renderer/components/index.ts"
      via: "single barrel import"
      pattern: "./components/index.js"
    - from: "packages/cora/package.json"
      to: "dist/renderer/components/index.js"
      via: "exports subpath"
      pattern: "./renderer/components"
---

<objective>
Refactor the existing renderer components into a stable public component surface under `packages/cora/src/renderer/components/`.

This plan keeps Phase 3.1 focused on current renderer structure: move existing nodes, edges, groups, shared helpers, and prop interfaces; wire `Diagram.tsx` through the new barrel; fix inherited baseline blockers; document the supported component API. The broader component catalog is explicitly Phase 3.2.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/3.1-renderer-component-refactor/3.1-CONTEXT.md
@.planning/phases/02-renderer-svg-export/02-CONTEXT.md
@.planning/phases/03-pdf-export/03-CONTEXT.md
@.planning/phases/03-pdf-export/deferred-items.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Repair inherited renderer baseline blockers</name>
  <read_first>
    - packages/cora/src/core/measureText.ts
    - packages/cora/tests/render-golden.mjs
    - packages/cora/tests/golden/box-arrows.svg
    - packages/cora/tests/golden/flowchart.svg
    - packages/cora/tests/golden/microservice.svg
    - packages/cora/tests/golden/infra.svg
    - packages/cora/tests/golden/database.svg
    - .planning/phases/03-pdf-export/deferred-items.md
    - .planning/phases/03-pdf-export/03-VERIFICATION.md
  </read_first>
  <action>
Fix the pre-existing TypeScript errors in `packages/cora/src/core/measureText.ts` without changing the measured values. The current code reads `font.ascent` and `font.descent` directly from `fontkit`'s `Font` type; update it to use a local typed metric adapter or a narrow local type assertion that exposes numeric `ascent`, `descent`, and `unitsPerEm`. Do not replace fontkit or change the baseline formulas.

Then run a clean build and capture the current renderer output as the new golden baseline using `UPDATE_GOLDEN=1 node tests/render-golden.mjs` from `packages/cora/` after the build. This refresh is allowed because Phase 3 recorded the drift as pre-existing and assigned it to Phase 3.1. After refresh, run `bun run test:golden` again to prove the baseline is now stable.
  </action>
  <acceptance_criteria>
- `cd packages/cora && bun run typecheck` exits 0 or, if unrelated errors remain, no remaining error references `src/core/measureText.ts`.
- `packages/cora/src/core/measureText.ts` still imports and uses `fontkit`; no canvas, DOM, or browser dependency is introduced.
- `UPDATE_GOLDEN=1 node tests/render-golden.mjs` updates all five golden SVG files.
- `cd packages/cora && bun run test:golden` exits 0 and prints `5/5 golden checks passed`.
- No renderer component files are moved in this task; it only repairs baseline blockers before the structural refactor.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && bun run typecheck && UPDATE_GOLDEN=1 node tests/render-golden.mjs && bun run test:golden</automated>
  </verify>
  <done>Typecheck and golden baselines are green before component movement starts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Move existing components under renderer/components and wire the barrel</name>
  <read_first>
    - packages/cora/src/renderer/Diagram.tsx
    - packages/cora/src/renderer/nodes/index.tsx
    - packages/cora/src/renderer/edges/Arrow.tsx
    - packages/cora/src/renderer/edges/EdgeLabel.tsx
    - packages/cora/src/renderer/edges/decorations.ts
    - packages/cora/src/renderer/groups/Group.tsx
    - packages/cora/src/renderer/renderToSVG.tsx
    - packages/cora/src/renderer/index.ts
    - packages/cora/src/layout-ir.ts
    - .planning/phases/3.1-renderer-component-refactor/3.1-CONTEXT.md
  </read_first>
  <action>
Create the real component implementation home under `packages/cora/src/renderer/components/`:

- `types.ts` exports `NodeComponentProps`, `EdgeComponentProps`, and `GroupComponentProps`.
- `nodes/shared.tsx` contains shared node helpers currently embedded in `nodes/index.tsx`, including label rendering, shadow rendering, and stroke-width normalization.
- `nodes/BoxNode.tsx`, `RoundedNode.tsx`, `CylinderNode.tsx`, `CloudNode.tsx`, `DiamondNode.tsx`, and `HexagonNode.tsx` each export their component using `NodeComponentProps`.
- `edges/Arrow.tsx`, `edges/EdgeLabel.tsx`, and `edges/decorations.ts` move the current edge implementation and helper constants under `components/edges/`.
- `groups/Group.tsx` exports the current group component using `GroupComponentProps`.
- `index.ts` exports all components, helper types, and edge decoration helpers needed by current code.

Update `packages/cora/src/renderer/Diagram.tsx` so it imports all node, edge, and group components from `./components/index.js` and no longer imports from `./nodes`, `./edges`, or `./groups`. Preserve existing keys, SVG element order, filter IDs, and shape dispatch behavior.

Remove old implementation files or convert them to thin compatibility re-export shims only if a current internal import still needs them. Do not leave duplicate implementations.
  </action>
  <acceptance_criteria>
- `packages/cora/src/renderer/components/index.ts` exists and exports `NodeComponentProps`, `EdgeComponentProps`, `GroupComponentProps`, `BoxNode`, `RoundedNode`, `CylinderNode`, `CloudNode`, `DiamondNode`, `HexagonNode`, `Arrow`, `EdgeLabel`, and `Group`.
- `packages/cora/src/renderer/Diagram.tsx` contains the import path `./components/index.js`.
- `packages/cora/src/renderer/Diagram.tsx` does not contain `./nodes`, `./edges/Arrow`, `./edges/EdgeLabel`, or `./groups/Group`.
- `rg \"export interface NodeProps|export interface ArrowProps|export interface EdgeLabelProps|export interface GroupProps\" packages/cora/src/renderer` finds no old public prop interface names.
- `cd packages/cora && bun run build` exits 0.
- `cd packages/cora && bun run test:golden` exits 0 after the refactor, proving the component move preserved the refreshed SVG output.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg './components/index.js' packages/cora/src/renderer/Diagram.tsx && ! rg './nodes|./edges/Arrow|./edges/EdgeLabel|./groups/Group' packages/cora/src/renderer/Diagram.tsx && cd packages/cora && bun run build && bun run test:golden</automated>
  </verify>
  <done>Existing renderer components live under `renderer/components/` and `Diagram.tsx` consumes only the public barrel.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Publish and document the renderer component contract</name>
  <read_first>
    - packages/cora/package.json
    - packages/cora/src/renderer/index.ts
    - packages/cora/src/renderer/components/index.ts
    - packages/cora/src/index.ts
    - AGENTS.md
    - .planning/phases/3.1-renderer-component-refactor/3.1-CONTEXT.md
  </read_first>
  <action>
Expose the public component surface through package exports without adding it to the top-level `cora` export:

- Add a `./renderer/components` subpath to `packages/cora/package.json` pointing to `./dist/renderer/components/index.js` and `./dist/renderer/components/index.d.ts`.
- Keep `packages/cora/src/index.ts` free of component re-exports.
- Keep `packages/cora/src/renderer/index.ts` focused on renderer functions and existing renderer exports; do not flatten the component catalog there unless TypeScript declaration generation requires a local type reference.

Update `AGENTS.md` with a `Renderer Components` section documenting:

- Import target: `cora/renderer/components`.
- Supported interfaces: `NodeComponentProps`, `EdgeComponentProps`, `GroupComponentProps`.
- Current hybrid prop shape: `node: LayoutedNode`, `edge: LayoutedEdge`, `group: LayoutedGroup`, and `theme: ThemeTokens`.
- Compatibility statement: supported v1 renderer contract with minor documented evolution allowed before 1.0.
- Scope fence: Phase 3.1 exposes current components only; the larger catalog (label/icon/page/app/issue nodes, lines, markers, style vocabulary) belongs to Phase 3.2.

Run build/typecheck/golden after docs and export changes.
  </action>
  <acceptance_criteria>
- `packages/cora/package.json` contains the exact subpath key `"./renderer/components"`.
- `packages/cora/src/index.ts` does not export `NodeComponentProps`, `EdgeComponentProps`, `GroupComponentProps`, `BoxNode`, `Arrow`, or `Group`.
- `AGENTS.md` contains `Renderer Components`, `cora/renderer/components`, `NodeComponentProps`, `EdgeComponentProps`, `GroupComponentProps`, and `supported v1 renderer contract`.
- `cd packages/cora && bun run build` exits 0 and produces `dist/renderer/components/index.d.ts`.
- `cd packages/cora && bun run typecheck` exits 0.
- `cd packages/cora && bun run test:golden` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg '\"\\./renderer/components\"' packages/cora/package.json && ! rg 'NodeComponentProps|EdgeComponentProps|GroupComponentProps|BoxNode|Arrow|Group' packages/cora/src/index.ts && rg 'Renderer Components|cora/renderer/components|NodeComponentProps|EdgeComponentProps|GroupComponentProps|supported v1 renderer contract' AGENTS.md && cd packages/cora && bun run build && test -f dist/renderer/components/index.d.ts && bun run typecheck && bun run test:golden</automated>
  </verify>
  <done>The public component contract is exported, documented, and verified.</done>
</task>

</tasks>

<verification>
## Phase-Level Verification

Run from the repository root:

```bash
cd packages/cora
bun run build
bun run typecheck
bun x vitest run
bun run test:golden
```

Then verify source constraints:

```bash
rg './components/index.js' packages/cora/src/renderer/Diagram.tsx
! rg './nodes|./edges/Arrow|./edges/EdgeLabel|./groups/Group' packages/cora/src/renderer/Diagram.tsx
rg '"./renderer/components"' packages/cora/package.json
rg 'Renderer Components' AGENTS.md
```
</verification>
