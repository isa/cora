# Phase 3.1: Renderer Component Refactor - Discussion Log

**Gathered:** 2026-05-22

## Areas Discussed

### Public Component API
- Selected hybrid props: stable `NodeComponentProps`, `EdgeComponentProps`, and `GroupComponentProps` names carrying current `Layouted*` objects and `ThemeTokens`.
- Selected public extension contract now, not internal-only.
- Selected `renderer/components/index.ts` as the public surface, with no top-level `packages/cora/src/index.ts` re-export in this phase.
- Selected v1 renderer contract with documented minor evolution before 1.0.

### Component Location
- User challenged putting implementation under old `nodes/`, `edges/`, and `groups/`.
- Decision: move implementation under `packages/cora/src/renderer/components/`, organized by `nodes/`, `edges/`, and `groups/`.

### Future Component Library
- User listed desired future component families and style controls:
  - group
  - box/node
  - label node
  - label
  - icon node
  - label node with icon
  - website node
  - page node
  - app node
  - decision node
  - issue node
  - lines
  - line heads/tails
- Clarified that `label node` field is `text`, not `ext`.
- Decision: insert a new Phase 3.2 for the full renderer component library so 3.1 stays a refactor and the larger catalog is handled deliberately before preview/canvas/extensions.

## Deferred

- Full implementation of the larger catalog moves to Phase 3.2 Renderer Component Library.

