# Phase 3.1 Verification

**Status:** PASS
**Date:** 2026-05-22

## Requirement Checks

- PASS: Existing renderer node components live under
  `packages/cora/src/renderer/components/nodes/` with `NodeComponentProps`.
- PASS: Edge and group components live under
  `packages/cora/src/renderer/components/edges/` and
  `packages/cora/src/renderer/components/groups/` with shared prop interfaces.
- PASS: `packages/cora/src/renderer/components/index.ts` re-exports the public
  component surface and `Diagram.tsx` consumes `./components/index.js`.
- PASS: Golden SVG regression is stable after baseline refresh and after the
  component move.
- PASS: `AGENTS.md` documents `cora/renderer/components`, the three prop
  interfaces, and the supported v1 renderer contract.

## Commands Run

```bash
cd packages/cora && bun run typecheck
cd packages/cora && UPDATE_GOLDEN=1 node tests/render-golden.mjs
cd packages/cora && bun run build
cd packages/cora && bun run test:golden
cd packages/cora && bun run test
```

## Structural Checks

```bash
rg './components/index.js' packages/cora/src/renderer/Diagram.tsx
rg './nodes|./edges/Arrow|./edges/EdgeLabel|./groups/Group' packages/cora/src/renderer/Diagram.tsx
rg 'export interface NodeProps|export interface ArrowProps|export interface EdgeLabelProps|export interface GroupProps' packages/cora/src/renderer
rg '"\./renderer/components"' packages/cora/package.json
rg 'NodeComponentProps|EdgeComponentProps|GroupComponentProps|BoxNode|Arrow|Group' packages/cora/src/index.ts
rg 'Renderer Components|cora/renderer/components|NodeComponentProps|EdgeComponentProps|GroupComponentProps|supported v1 renderer contract' AGENTS.md
```

The negative `rg` checks returned no matches as expected.

## Residual Risk

- The component contract intentionally exposes current layout IR values. Phase
  3.2 should avoid coupling the new component catalog directly to YAML schema
  fields and should keep schema mapping outside the component layer.
