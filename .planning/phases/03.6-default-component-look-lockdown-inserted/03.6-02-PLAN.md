---
phase: 3.6-default-component-look-lockdown-inserted
plan: 02
type: execute
wave: 2
depends_on:
  - 03.6-01
files_modified:
  - packages/cora/src/renderer/components/styles.ts
  - packages/cora/src/core/measureText.ts
  - packages/cora/src/renderer/components/nodes/shared.tsx
  - packages/cora/src/renderer/Diagram.tsx
  - packages/cora/src/renderer/components/nodes/BoxNode.tsx
  - packages/cora/tests/renderer/components.test.tsx
autonomous: true
requirements:
  - LOOK-01
  - LOOK-03
  - LOOK-04
  - LOOK-05
requirements_addressed:
  - LOOK-01
  - LOOK-03
  - LOOK-04
  - LOOK-05
decision_refs:
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11

must_haves:
  truths:
    - "D-05: YAML default box nodes use slate-300 1px border via LayoutBoxNode"
    - "D-06: LayoutBoxNode renders with no default shadow filter"
    - "D-07: YAML default box nodes render with 8px radius via LayoutBoxNode rx/ry"
    - "D-08: Edges render at 2px slate-700 stroke from theme"
    - "D-09: LayoutBoxNode and catalog path use 12px SemiBold / 10px Regular labels"
    - "D-10: Size presets sm–xxl remain but dimensions tuned for compact typography and medium radius"
    - "D-11: Edge labels 10px slate-600; LabelNode 11px slate-800 via catalog path"
    - "Catalog YAML components receive radius, typography, and shadow:none from catalogDefaultProps merge in nodeCatalogProps"
    - "examples/valid/minimal.yaml renders without requiring per-node style props"
  artifacts:
    - path: packages/cora/src/renderer/components/nodes/BoxNode.tsx
      provides: LayoutBoxNode with rx/ry radius and gated shadow
      pattern: "rx="
    - path: packages/cora/src/renderer/Diagram.tsx
      provides: nodeCatalogProps merged with catalogDefaultProps
    - path: packages/cora/src/core/measureText.ts
      provides: Updated font imports and COMPONENT_MIN_SIZE tuning
  key_links:
    - from: packages/cora/src/renderer/Diagram.tsx
      to: packages/cora/src/renderer/themes/componentDefaults.ts
      via: catalogDefaultProps in nodeCatalogProps
      pattern: "catalogDefaultProps"
    - from: packages/cora/src/core/measureText.ts
      to: packages/cora/src/renderer/themes/fontTokens.ts
      via: NODE_TITLE_SIZE in FONT_SIZE_BY_ROLE
      pattern: "NODE_TITLE_SIZE"
---

## Phase Goal

**As a** Cora user rendering YAML diagrams, **I want to** see consistent locked styling on every built-in component including default box nodes, **so that** diagram output matches the canonical look without per-node overrides.

<objective>
Wire the shared look contract into the YAML render path — measurement, catalog fallbacks, nodeCatalogProps, and LayoutBoxNode — closing the box dual-path skew so all component kinds render with locked radius, typography, edges, and no default shadows.

Purpose: Deliver end-to-end renderer output using locked defaults (LOOK-04, LOOK-05).
Output: Updated styles.ts, measureText.ts, shared.tsx, Diagram.tsx, BoxNode.tsx, components.test.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-CONTEXT.md
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-RESEARCH.md
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-PATTERNS.md

<interfaces>
From Plan 01 (must exist before execution):
```typescript
// componentDefaults.ts
export function catalogDefaultProps(component: DiagramComponent): CatalogBoxNodeProps;

// fontTokens.ts
export const NODE_TITLE_SIZE = 12;
export const NODE_SUBTITLE_SIZE = 10;
export const EDGE_LABEL_SIZE = 10;

// lookTokens.ts
export const LOOK = { radius: { md: 8 }, edge: { stroke: string; width: 2 }, /* … */ };
```

From packages/cora/src/renderer/Diagram.tsx (extend nodeCatalogProps):
```typescript
function nodeCatalogProps(node: LayoutedNode, style: ThemeShapeStyle) {
  return {
    x: node.x, y: node.y,
    size: { width: node.measuredWidth, height: node.measuredHeight },
    backgroundColor: style.fill,
    borderColor: style.stroke,
    borderWidth: style.strokeWidth,
    borderStyle: borderStyleFor(style),
    text: node.label,
    textColor: style.labelFill ?? '#1A1A1A',
  };
}
```

From packages/cora/src/renderer/components/nodes/BoxNode.tsx LayoutBoxNode:
- Currently renders rect without rx/ry; ShapeShadow active when style.shadow set
</interfaces>
</context>

<tasks>

<task type="auto" id="3.6-02-01">
  <name>Tune measurement and catalog fallbacks for compact typography</name>
  <files>packages/cora/src/renderer/components/styles.ts, packages/cora/src/core/measureText.ts, packages/cora/src/renderer/components/nodes/shared.tsx, packages/cora/tests/renderer/components.test.tsx</files>
  <read_first>
    - packages/cora/src/renderer/components/styles.ts
    - packages/cora/src/core/measureText.ts
    - packages/cora/src/renderer/components/nodes/shared.tsx
    - packages/cora/src/renderer/themes/fontTokens.ts
    - packages/cora/src/renderer/themes/lookTokens.ts
    - packages/cora/tests/renderer/components.test.tsx
  </read_first>
  <action>
Update packages/cora/src/core/measureText.ts:
- Import NODE_TITLE_SIZE and EDGE_LABEL_SIZE from fontTokens (replace NODE_FONT_SIZE/EDGE_FONT_SIZE)
- Set FONT_SIZE_BY_ROLE node to NODE_TITLE_SIZE, edge to EDGE_LABEL_SIZE
- Retune NODE_PADDING_X/Y and COMPONENT_MIN_SIZE per component kind so 12px titles fit without clipping (increase heights ~15–25% vs current; keep kind-relative ordering)
- Keep fontkit + Noto paths unchanged

Update packages/cora/src/renderer/components/styles.ts SIZE_PRESETS (D-10 — keep sm/xxl vocabulary, tune dimensions only). Suggested starting points (adjust if measureText clips): sm 104×44, md 136×60, lg 184×76, xl 232×100, xxl 296×132 — executor validates against measureText output for a 12px SemiBold single-line label.

Update packages/cora/src/renderer/components/nodes/shared.tsx fallbacks to import LOOK and fontTokens instead of magic values:
- radius ?? LOOK.radius.md (8, not 4)
- borderColor ?? LOOK.border.default
- textColor ?? LOOK.text.primary
- subtitleColor ?? LOOK.text.muted
- CatalogText fontSize default NODE_TITLE_SIZE (12, not 13)
- CatalogText subtitleFontSize default NODE_SUBTITLE_SIZE (10)
- shadow ?? 'none' unchanged

Update components.test.tsx assertions that reference old theme background (#F4F9FB) or old preset dimensions to match new tokens.
  </action>
  <acceptance_criteria>
- measureText.ts imports NODE_TITLE_SIZE (not NODE_FONT_SIZE = 8)
- shared.tsx CatalogText default fontSize resolves to 12 via NODE_TITLE_SIZE
- resolvedCatalogFrame fallback radius is 8 when props.radius omitted
- SIZE_PRESETS still exports sm, md, lg, xl, xxl keys
- cd packages/cora && bun x vitest run tests/renderer/components.test.tsx exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && rg "NODE_TITLE_SIZE" src/core/measureText.ts src/renderer/components/nodes/shared.tsx && ! rg "fontSize = 13" src/renderer/components/nodes/shared.tsx && bun x vitest run tests/renderer/components.test.tsx -x</automated>
  </verify>
  <done>Measurement and catalog fallbacks align with 12/10 typography and 8px radius</done>
</task>

<task type="auto" id="3.6-02-02">
  <name>Unify YAML catalog props and LayoutBoxNode box path</name>
  <files>packages/cora/src/renderer/Diagram.tsx, packages/cora/src/renderer/components/nodes/BoxNode.tsx</files>
  <read_first>
    - packages/cora/src/renderer/Diagram.tsx
    - packages/cora/src/renderer/components/nodes/BoxNode.tsx
    - packages/cora/src/renderer/themes/componentDefaults.ts
    - packages/cora/src/renderer/themes/lookTokens.ts
    - examples/valid/minimal.yaml
    - examples/valid/flowchart.yaml
  </read_first>
  <action>
Update packages/cora/src/renderer/Diagram.tsx nodeCatalogProps:
- Import catalogDefaultProps from componentDefaults.js
- For each node, const defaults = catalogDefaultProps(node.component ?? 'box')
- Merge defaults with theme-resolved colors: backgroundColor/borderColor/borderWidth from style; add radius, titleFontSize, subtitleFontSize, shadow: 'none', textColor from style.labelFill ?? defaults
- Preserve measured size from layout (do not override with preset size string)

Update packages/cora/src/renderer/components/nodes/BoxNode.tsx LayoutBoxNode (D-06, D-07):
- Add rx={LOOK.radius.md} ry={LOOK.radius.md} to layout rect elements
- ShapeShadow already gated on style.shadow — with theme shapes omitting shadow, default box renders flat
- Do NOT route YAML box through catalog BoxNode (research recommendation a)

Verify Diagram edge rendering passes theme.edge.strokeWidth (2) to Line components — fix if hardcoded elsewhere.

Smoke render after build:
- bun run build && node dist/cli.js render ../../examples/valid/minimal.yaml -o /tmp/cora-minimal.svg
- Assert /tmp/cora-minimal.svg contains rx="8" on box rect OR rounded rect attribute
- Assert no filter="url(#cora-shadow-blur)" on default nodes in minimal output
- Assert stroke-width="2" on at least one edge path/line
  </action>
  <acceptance_criteria>
- Diagram.tsx imports and calls catalogDefaultProps in nodeCatalogProps
- LayoutBoxNode layout rect includes rx and ry equal to 8
- cora render examples/valid/minimal.yaml produces SVG without cora-shadow-blur filter on default box
- Rendered minimal SVG edge stroke-width is 2
- cd packages/cora && bun run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && rg "catalogDefaultProps" src/renderer/Diagram.tsx && rg 'rx=\{LOOK\.radius\.md\}|rx=\{8\}' src/renderer/components/nodes/BoxNode.tsx && bun run build && node dist/cli.js render ../../examples/valid/minimal.yaml -o /tmp/cora-minimal-look.svg && ! rg 'filter="url\(#cora-shadow-blur\)"' /tmp/cora-minimal-look.svg && rg 'stroke-width="2"' /tmp/cora-minimal-look.svg</automated>
  </verify>
  <done>YAML render path applies locked defaults on box and catalog components; edges are 2px</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML → measureText → SVG | User-supplied labels measured headlessly; existing escapeXml on render |
| Theme overrides → resolved style | User style props still override defaults via themeResolver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.6-04 | Tampering | nodeCatalogProps merge order | mitigate | Theme colors override factory colors; factory supplies geometry/typography only |
| T-3.6-05 | Denial of Service | Font size increase blowing layout | accept | Expected LOOK-04 behavior; goldens refresh in Plan 04 |
| T-3.6-06 | Tampering | XSS via SVG labels | mitigate | Preserve escapeXml in NodeLabel/CatalogText; no foreignObject |
| T-3.6-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
cd packages/cora && bun run typecheck && bun x vitest run tests/renderer/components.test.tsx && bun run build && node dist/cli.js render ../../examples/valid/flowchart.yaml -o /tmp/flow.svg
</verification>

<success_criteria>
- Box dual-path skew resolved: LayoutBoxNode matches catalog default appearance
- All catalog YAML components get radius 8 and 12/10 typography defaults
- LOOK-05: minimal.yaml renders polished SVG without custom style props
</success_criteria>

<output>
Create `.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-02-SUMMARY.md` when done
</output>
