---
phase: 3.6-default-component-look-lockdown-inserted
plan: 04
type: execute
wave: 3
depends_on:
  - 03.6-02
  - 03.6-03
files_modified:
  - packages/cora/tests/golden/box-arrows.svg
  - packages/cora/tests/golden/flowchart.svg
  - packages/cora/tests/golden/microservice.svg
  - packages/cora/tests/golden/infra.svg
  - packages/cora/tests/golden/database.svg
  - AGENTS.md
autonomous: true
requirements:
  - LOOK-06
  - LOOK-07
requirements_addressed:
  - LOOK-06
  - LOOK-07
decision_refs:
  - D-13

must_haves:
  truths:
    - "All five SVG golden baselines reflect the new locked look (intentional D-13 refresh)"
    - "Golden SVGs contain no default cora-shadow-blur filters on box nodes"
    - "ASCII/text export tests pass — text path has no color styling regression"
    - "PDF render tests pass with updated font sizes"
    - "AGENTS.md documents the Default Look contract for agents and extension authors"
  artifacts:
    - path: packages/cora/tests/golden/box-arrows.svg
      provides: Refreshed visual baseline
    - path: AGENTS.md
      provides: Default Look documentation subsection
      contains: "Default Look"
  key_links:
    - from: packages/cora/tests/render-golden.mjs
      to: packages/cora/tests/golden/*.svg
      via: UPDATE_GOLDEN=1 refresh workflow
      pattern: "UPDATE_GOLDEN"
---

## Phase Goal

**As a** Cora maintainer, **I want to** lock the new default look behind golden regression and documented contract, **so that** future changes cannot accidentally drift visual defaults (LOOK-06, LOOK-07, D-13).

<objective>
Refresh SVG golden baselines to the approved locked look, verify ASCII/PDF paths remain healthy, and document the default look contract in AGENTS.md.

Purpose: Close LOOK-06 golden stability and LOOK-07 documentation requirements.
Output: Refreshed golden/*.svg, AGENTS.md Default Look section, full regression green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-CONTEXT.md (D-13)
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-RESEARCH.md (Golden Refresh Strategy)
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-VALIDATION.md
@AGENTS.md

<interfaces>
Golden refresh workflow from packages/cora/tests/render-golden.mjs:
```javascript
// Refresh baselines: UPDATE_GOLDEN=1 node tests/render-golden.mjs
const FIXTURES = ['box-arrows', 'flowchart', 'microservice', 'infra', 'database'];
```

AGENTS.md existing section: ## Renderer Components (extend with ### Default Look)
</interfaces>
</context>

<tasks>

<task type="auto" id="3.6-04-01">
  <name>Regression gates before golden refresh</name>
  <files>packages/cora/tests/renderer/text.test.ts, packages/cora/tests/cli/render-text.test.ts, packages/cora/tests/pdf/render-pdf.test.ts</files>
  <read_first>
    - packages/cora/tests/renderer/text.test.ts
    - packages/cora/tests/cli/render-text.test.ts
    - packages/cora/tests/pdf/render-pdf.test.ts
    - packages/cora/tests/render-golden.mjs
    - .planning/phases/03.6-default-component-look-lockdown-inserted/03.6-CONTEXT.md (LOOK-06)
  </read_first>
  <action>
Run pre-refresh regression suite to establish baseline health:

cd packages/cora && bun run typecheck && bun x vitest run && bun run build

Run text export tests (LOOK-06 — ASCII path must not gain color/styling code):
- bun x vitest run tests/renderer/text.test.ts tests/cli/render-text.test.ts

Run PDF tests (font overlay uses theme.nodeLabel.fontSize — now 12):
- bun x vitest run tests/pdf/render-pdf.test.ts

If PDF text position tests fail due to font size change, update expected coordinates in render-pdf.test.ts to match new measured layout — do NOT revert font sizes.

Fix any failing look-defaults, look-sync, or components tests before proceeding to golden refresh.

Do NOT refresh preview visual-contract.test.tsx — preview chrome tokens are out of scope.
  </action>
  <acceptance_criteria>
- cd packages/cora && bun run typecheck exits 0
- cd packages/cora && bun x vitest run tests/renderer/text.test.ts tests/cli/render-text.test.ts exits 0
- cd packages/cora && bun x vitest run tests/pdf/render-pdf.test.ts exits 0
- cd packages/cora && bun x vitest run tests/renderer/look-defaults.test.ts tests/preview/look-sync.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && bun run typecheck && bun x vitest run tests/renderer/text.test.ts tests/cli/render-text.test.ts tests/pdf/render-pdf.test.ts tests/renderer/look-defaults.test.ts tests/preview/look-sync.test.ts -x</automated>
  </verify>
  <done>Text, PDF, and look contract tests green before baseline update</done>
</task>

<task type="auto" id="3.6-04-02">
  <name>Intentional golden baseline refresh (D-13)</name>
  <files>packages/cora/tests/golden/box-arrows.svg, packages/cora/tests/golden/flowchart.svg, packages/cora/tests/golden/microservice.svg, packages/cora/tests/golden/infra.svg, packages/cora/tests/golden/database.svg</files>
  <read_first>
    - packages/cora/tests/render-golden.mjs
    - packages/cora/tests/golden/box-arrows.svg
    - .planning/phases/03.6-default-component-look-lockdown-inserted/03.6-CONTEXT.md (D-13)
  </read_first>
  <action>
Execute approved golden refresh per D-13:

cd packages/cora && bun run build
UPDATE_GOLDEN=1 node tests/render-golden.mjs

This updates all five fixtures: box-arrows, flowchart, microservice, infra, database.

Post-refresh assertions on box-arrows.svg (representative):
- Must NOT contain filter="url(#cora-shadow-blur)" on default styled nodes (D-06)
- Must contain stroke-width="2" on edges
- Must contain font-size="12" for node labels (catalog or box path)
- Must still pass render-golden.mjs invariants (no foreignObject, valid viewBox)

Run bun run test:golden — all 5/5 must PASS without UPDATE_GOLDEN.

Commit goldens in this task with message noting intentional Phase 3.6 visual baseline update.
  </action>
  <acceptance_criteria>
- All five files under packages/cora/tests/golden/*.svg modified
- cd packages/cora && bun run test:golden exits 0 without UPDATE_GOLDEN env var
- box-arrows.svg does not contain filter="url(#cora-shadow-blur)"
- render-golden.mjs foreignObject invariant still passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && bun run test:golden && ! rg 'filter="url\(#cora-shadow-blur\)"' tests/golden/box-arrows.svg</automated>
  </verify>
  <done>SVG golden baselines locked to new default look</done>
</task>

<task type="auto" id="3.6-04-03">
  <name>Document Default Look contract in AGENTS.md (LOOK-07)</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (Renderer Components section)
    - packages/cora/src/renderer/themes/componentDefaults.ts
    - packages/cora/src/renderer/themes/lookTokens.ts
    - .planning/phases/03.6-default-component-look-lockdown-inserted/03.6-CONTEXT.md
  </read_first>
  <action>
Add ### Default Look subsection under ## Renderer Components in AGENTS.md documenting LOOK-07:

Include:
- Palette: Tailwind slate-neutral base (static hex in tailwindPalette.ts); semantic hues per component kind (violet icon/labelIcon, amber decision, rose issue, sky page, emerald app, yellow website, teal shape)
- Omission defaults: white/near-white fills, slate-300 1px borders on box-like nodes, radius 8, no default shadows, 2px slate-700 edges, 1px slate-400 dashed groups
- Typography: Noto Sans — node title 12px SemiBold, subtitle 10px Regular, edge labels 10px Regular slate-600, standalone LabelNode 11px SemiBold slate-800
- Single source: renderer/themes/componentDefaults.ts catalogDefaultProps(); preview imports same factory (D-12)
- When to override: explicit YAML style props or component props; Phase 5 owns extension themes
- Pointer: no runtime Tailwind/CSS dependency at render time (D-02)

Do not document preview workbench chrome tokens (--preview-accent).

Run full phase gate:
cd packages/cora && bun run typecheck && bun x vitest run && bun run build && bun run test:golden
  </action>
  <acceptance_criteria>
- AGENTS.md contains heading ### Default Look
- AGENTS.md mentions slate-300 border and catalogDefaultProps or componentDefaults.ts
- AGENTS.md mentions 12px and 2px edge width
- AGENTS.md does not mention --preview-accent
- cd packages/cora && bun run typecheck && bun x vitest run && bun run build && bun run test:golden all exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "### Default Look|slate-300|catalogDefaultProps|12px" AGENTS.md && ! rg -- "--preview-accent" AGENTS.md && cd packages/cora && bun run typecheck && bun x vitest run && bun run build && bun run test:golden</automated>
  </verify>
  <done>Default look contract documented; full phase regression suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Golden SVG artifacts → CI | Committed baselines are trusted after review |
| AGENTS.md → agents | Documentation guides omission defaults; not executable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.6-10 | Tampering | Accidental golden refresh mid-phase | mitigate | Refresh only in this plan after Plans 01–03 complete |
| T-3.6-11 | Denial of Service | Golden diff hides foreignObject regression | mitigate | render-golden.mjs invariant checks unchanged |
| T-3.6-12 | Information Disclosure | AGENTS.md over-exposes internals | accept | Document public omission contract only |
| T-3.6-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
cd packages/cora && bun run typecheck && bun x vitest run && bun run build && bun run test:golden
rg "### Default Look" AGENTS.md
</verification>

<success_criteria>
- D-13: Golden baselines intentionally updated and stable
- LOOK-06: Text export unaffected; golden regression protects SVG look
- LOOK-07: AGENTS.md Default Look section complete
- Full CI-equivalent suite green
</success_criteria>

<output>
Create `.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-04-SUMMARY.md` when done
</output>
