---
phase: 3.6-default-component-look-lockdown-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cora/src/renderer/themes/tailwindPalette.ts
  - packages/cora/src/renderer/themes/lookTokens.ts
  - packages/cora/src/renderer/themes/fontTokens.ts
  - packages/cora/src/renderer/themes/componentDefaults.ts
  - packages/cora/src/renderer/themes/default.ts
  - packages/cora/tests/renderer/look-defaults.test.ts
autonomous: true
requirements:
  - LOOK-01
  - LOOK-02
  - LOOK-03
requirements_addressed:
  - LOOK-01
  - LOOK-02
  - LOOK-03
decision_refs:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-11

must_haves:
  truths:
    - "D-01: Default diagram palette is neutral professional (white fills, dark ink, subtle gray borders) — not pastel theme or preview chrome"
    - "D-02: Tailwind CSS color schemes encoded as static hex map; no runtime Tailwind/CSS dependency"
    - "D-03: Tailwind slate scale is the neutral base for backgrounds, borders, text, and surfaces"
    - "D-04: Full semantic hue mapping per component kind (violet icon, amber decision, rose issue, sky page, emerald app, yellow website, teal shape)"
    - "D-05: Box-like nodes default to slate-300 1px solid border; label/icon nodes borderless or slate-200 hairline"
    - "D-06: No shadow by default on rendered components; shadow only when explicitly set"
    - "D-07: Default corner radius ~8px on box-like nodes; decision diamond and icon circles unchanged"
    - "D-08: Default edges slate-700 2px; groups slate-400 dashed with no fill"
    - "D-09: In-node typography 12px SemiBold title, 10px Regular subtitle (Noto Sans)"
    - "D-11: Edge labels 10px Regular slate-600; LabelNode 11px SemiBold slate-800"
    - "catalogDefaultProps(component) returns locked BoxStyleProps for every DiagramComponent id"
    - "No hex literals appear outside tailwindPalette.ts in new theme modules"
  artifacts:
    - path: packages/cora/src/renderer/themes/tailwindPalette.ts
      provides: Static Tailwind scale hex map
      exports: ["TAILWIND"]
    - path: packages/cora/src/renderer/themes/lookTokens.ts
      provides: Semantic LOOK roles referencing TAILWIND
      exports: ["LOOK"]
    - path: packages/cora/src/renderer/themes/componentDefaults.ts
      provides: catalogDefaultProps factory for preview and renderer
      exports: ["catalogDefaultProps"]
    - path: packages/cora/src/renderer/themes/default.ts
      provides: Rebuilt defaultTheme from LOOK tokens
      contains: "shadowBlur: 0"
    - path: packages/cora/tests/renderer/look-defaults.test.ts
      provides: Token contract unit tests
  key_links:
    - from: packages/cora/src/renderer/themes/componentDefaults.ts
      to: packages/cora/src/renderer/themes/lookTokens.ts
      via: LOOK.surface / LOOK.border imports
      pattern: "LOOK\\."
    - from: packages/cora/src/renderer/themes/default.ts
      to: packages/cora/src/renderer/themes/lookTokens.ts
      via: buildDefaultTheme or direct LOOK refs
      pattern: "TAILWIND\\.slate"
---

## Phase Goal

**As a** Cora agent authoring YAML diagrams, **I want to** rely on a canonical locked default visual language, **so that** omitted styling props still produce professional slate-neutral diagrams with semantic component hues.

<objective>
Establish the shared look contract — static Tailwind palette, semantic LOOK tokens, typography constants, catalogDefaultProps factory, and rebuilt defaultTheme — as the single source of truth for all renderer and preview defaults.

Purpose: Eliminate four divergent default layers by creating one module graph consumed downstream in Plans 02–03.
Output: tailwindPalette.ts, lookTokens.ts, fontTokens.ts, componentDefaults.ts, refactored default.ts, look-defaults.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-CONTEXT.md
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-RESEARCH.md
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-PATTERNS.md

<interfaces>
From packages/cora/src/layout-ir.ts:
```typescript
export type DiagramComponent =
  | 'box' | 'label' | 'icon' | 'labelIcon' | 'website' | 'page'
  | 'app' | 'decision' | 'issue' | 'shape';

export interface ThemeShapeStyle {
  fill: string;
  stroke: string;
  shadow?: string;
  labelFill?: string;
  strokeWidth?: number;
  strokeDasharray?: string;
}

export interface ThemeTokens {
  background: string;
  shapes: Record<string, ThemeShapeStyle>;
  edge: { stroke: string; strokeWidth: number };
  nodeLabel: { fontSize: number; fontWeight: number; fill: string };
  edgeLabel: { fontSize: number; fontWeight: number; fill: string };
  shadowOffset: { x: number; y: number };
  shadowBlur: number;
}
```

From packages/cora/src/renderer/themes/default.ts (current — replace content):
```typescript
export const defaultTheme: ThemeTokens = { /* pastel — replace entirely */ };
export function resolveNodeStyle(component: string | undefined): ThemeTokens['shapes'][string];
```

From packages/cora/src/renderer/themes/fontTokens.ts (extend):
```typescript
export const NODE_FONT_SIZE = 8;  // → NODE_TITLE_SIZE = 12
export const EDGE_FONT_SIZE = 6;  // → EDGE_LABEL_SIZE = 10
```
</interfaces>
</context>

<tasks>

<task type="auto" id="3.6-01-01">
  <name>Static Tailwind palette and semantic LOOK tokens</name>
  <files>packages/cora/src/renderer/themes/tailwindPalette.ts, packages/cora/src/renderer/themes/lookTokens.ts, packages/cora/tests/renderer/look-defaults.test.ts</files>
  <read_first>
    - .planning/phases/03.6-default-component-look-lockdown-inserted/03.6-CONTEXT.md (D-01–D-04)
    - .planning/phases/03.6-default-component-look-lockdown-inserted/03.6-RESEARCH.md (Pattern 1)
    - packages/cora/src/renderer/themes/default.ts
    - packages/cora/src/renderer/themes/transforms.ts
  </read_first>
  <action>
Create packages/cora/src/renderer/themes/tailwindPalette.ts exporting TAILWIND as const with at minimum:
- white: '#ffffff'
- slate scale 50–900 (use Tailwind v3 default hex literals; slate-50 '#f8fafc', slate-200 '#e2e8f0', slate-300 '#cbd5e1', slate-400 '#94a3b8', slate-600 '#475569', slate-700 '#334155', slate-800 '#1e293b', slate-900 '#0f172a')
- violet, amber, rose, sky, emerald, yellow, teal scales with at least 200/500/600 steps needed for semantic fills and strokes per D-04

Create packages/cora/src/renderer/themes/lookTokens.ts exporting LOOK with semantic roles consumed by theme and factory:
- surface.fill → TAILWIND.white (box-like nodes per D-01)
- surface.diagram → TAILWIND.slate[50]
- border.default → TAILWIND.slate[300] (D-05)
- border.hairline → TAILWIND.slate[200]
- text.primary → TAILWIND.slate[900]
- text.muted → TAILWIND.slate[500]
- text.edgeLabel → TAILWIND.slate[600] (D-11)
- text.standaloneLabel → TAILWIND.slate[800] (D-11)
- edge.stroke → TAILWIND.slate[700], edge.width → 2 (D-08)
- group.stroke → TAILWIND.slate[400], group.strokeWidth → 1, group.strokeDasharray → '4 4' (D-08; 1px dashed groups per research discretion)
- radius.md → 8 (D-07)
- semantic component roles: decision (amber-200 fill / amber-500 stroke), issue (rose-200/rose-500), page (sky-200/sky-500), app (emerald-200/emerald-500), website (yellow-200/yellow-500), shape (teal-200/teal-500), icon/labelIcon accent (violet-500 icon, violet-200 fill for labelIcon)

Create packages/cora/tests/renderer/look-defaults.test.ts with initial assertions:
- LOOK.edge.width === 2
- LOOK.radius.md === 8
- LOOK.border.default equals TAILWIND.slate[300]
- TAILWIND.slate[800] on white fill is a defined contrast pair (both strings non-empty; document pairing exists)

Do not npm-install tailwindcss (D-02 pure SVG). No hex literals outside tailwindPalette.ts in lookTokens — only TAILWIND references.
  </action>
  <acceptance_criteria>
- packages/cora/src/renderer/themes/tailwindPalette.ts exists and exports TAILWIND with slate and at least 7 hue scales
- packages/cora/src/renderer/themes/lookTokens.ts imports from tailwindPalette.js only
- LOOK.edge.width is 2 and LOOK.radius.md is 8
- packages/cora/tests/renderer/look-defaults.test.ts exists
- cd packages/cora && bun x vitest run tests/renderer/look-defaults.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && test -f src/renderer/themes/tailwindPalette.ts && test -f src/renderer/themes/lookTokens.ts && bun x vitest run tests/renderer/look-defaults.test.ts -x</automated>
  </verify>
  <done>Static palette and semantic LOOK roles exist with passing contract tests</done>
</task>

<task type="auto" id="3.6-01-02">
  <name>Typography tokens and catalogDefaultProps factory</name>
  <files>packages/cora/src/renderer/themes/fontTokens.ts, packages/cora/src/renderer/themes/componentDefaults.ts, packages/cora/tests/renderer/look-defaults.test.ts</files>
  <read_first>
    - packages/cora/src/renderer/themes/fontTokens.ts
    - packages/cora/src/renderer/themes/lookTokens.ts
    - packages/cora/src/renderer/components/styles.ts
    - packages/cora/src/preview/controls/defaults.ts
    - .planning/phases/03.6-default-component-look-lockdown-inserted/03.6-CONTEXT.md (D-09, D-10, D-11)
  </read_first>
  <action>
Expand packages/cora/src/renderer/themes/fontTokens.ts per D-09/D-11:
- NODE_TITLE_SIZE = 12, NODE_SUBTITLE_SIZE = 10
- EDGE_LABEL_SIZE = 10, STANDALONE_LABEL_SIZE = 11
- NODE_FONT_WEIGHT = 600, EDGE_FONT_WEIGHT = 400, LABEL_FONT_WEIGHT = 600
- Replace NODE_FONT_SIZE with NODE_TITLE_SIZE and EDGE_FONT_SIZE with EDGE_LABEL_SIZE across fontTokens exports; update any same-file aliases if needed for backward compat during Plan 02 import sweep

Create packages/cora/src/renderer/themes/componentDefaults.ts exporting catalogDefaultProps(component: DiagramComponent) returning props aligned with preview BoxStyleProps fields. Implement switch for all 10 DiagramComponent values:

box: white fill, slate-300 1px solid border, radius 8, shadow 'none', textColor slate-900, subtitleColor slate-500, titleFontSize 12, subtitleFontSize 10, size 'md' (D-05, D-06, D-07, D-09)

label: transparent/no border, titleFontSize 11, textColor slate-800, shadow 'none' (D-11)

icon: transparent fill, slate-200 hairline or borderless, iconColor violet-500, size 'md'

labelIcon: violet-200 fill, violet-500 stroke, iconColor violet-600, radius 8, shadow 'none'

website/app/page/decision/issue/shape: semantic LOOK fills/strokes per D-04, radius 8 where box-like, shadow 'none', title 12/subtitle 10

Extend look-defaults.test.ts:
- catalogDefaultProps('box').radius === 8
- catalogDefaultProps('box').shadow === 'none'
- catalogDefaultProps('label').titleFontSize === 11
- every DiagramComponent kind returns defined backgroundColor and shadow 'none'
  </action>
  <acceptance_criteria>
- fontTokens.ts exports NODE_TITLE_SIZE=12, NODE_SUBTITLE_SIZE=10, EDGE_LABEL_SIZE=10, STANDALONE_LABEL_SIZE=11
- catalogDefaultProps handles all 10 component ids without throwing
- catalogDefaultProps('box').shadow === 'none' and radius === 8
- catalogDefaultProps('label').titleFontSize === 11
- cd packages/cora && bun x vitest run tests/renderer/look-defaults.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && rg "NODE_TITLE_SIZE = 12|catalogDefaultProps" src/renderer/themes/ && bun x vitest run tests/renderer/look-defaults.test.ts -x</automated>
  </verify>
  <done>Typography constants and per-component default factory cover full catalog</done>
</task>

<task type="auto" id="3.6-01-03">
  <name>Rebuild defaultTheme from LOOK tokens</name>
  <files>packages/cora/src/renderer/themes/default.ts, packages/cora/tests/renderer/look-defaults.test.ts</files>
  <read_first>
    - packages/cora/src/renderer/themes/default.ts
    - packages/cora/src/renderer/themes/lookTokens.ts
    - packages/cora/src/renderer/themes/fontTokens.ts
    - packages/cora/src/renderer/themes/componentDefaults.ts
    - packages/cora/src/core/themeResolver.ts
  </read_first>
  <action>
Replace packages/cora/src/renderer/themes/default.ts pastel content entirely per D-01, D-06, D-08:

background: LOOK.surface.diagram (slate-50)

For each shape key matching DiagramComponent plus group: set fill/stroke/strokeWidth from catalogDefaultProps colors or LOOK semantic roles; **omit shadow property** on every default shape entry (D-06)

group: fill 'none', stroke LOOK.group.stroke, strokeWidth 1, strokeDasharray '4 4'

edge: stroke LOOK.edge.stroke, strokeWidth 2

nodeLabel: fontSize NODE_TITLE_SIZE (12), fontWeight 600, fill LOOK.text.primary

edgeLabel: fontSize EDGE_LABEL_SIZE (10), fontWeight 400, fill LOOK.text.edgeLabel (slate-600 per D-11)

shadowOffset: { x: 0, y: 0 }, shadowBlur: 0

Keep resolveNodeStyle export signature unchanged.

Extend look-defaults.test.ts:
- defaultTheme.edge.strokeWidth === 2
- defaultTheme.shadowBlur === 0
- no defaultTheme.shapes value has a shadow key defined
- defaultTheme.nodeLabel.fontSize === 12
- defaultTheme.edgeLabel.fontSize === 10
- defaultTheme.background matches TAILWIND.slate[50]

Run typecheck after font token renames touch default.ts imports.
  </action>
  <acceptance_criteria>
- defaultTheme contains no pastel hex (#C9B3FF, #F4F9FB, etc.)
- defaultTheme.edge.strokeWidth === 2 and shadowBlur === 0
- defaultTheme.nodeLabel.fontSize === 12
- resolveNodeStyle(undefined) returns box shape without shadow property
- cd packages/cora && bun run typecheck exits 0
- cd packages/cora && bun x vitest run tests/renderer/look-defaults.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && ! rg "#C9B3FF|#F4F9FB" src/renderer/themes/default.ts && bun run typecheck && bun x vitest run tests/renderer/look-defaults.test.ts -x</automated>
  </verify>
  <done>defaultTheme reflects locked slate-neutral look; theme resolver seam unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static token modules → renderer | Token files are developer-authored; no runtime user input |
| Theme → YAML render path | Diagram labels pass through existing escapeXml; no new HTML surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.6-01 | Tampering | tailwindPalette.ts | accept | Developer-maintained static hex; no npm color package |
| T-3.6-02 | Spoofing | Mislabeled token names | mitigate | look-defaults.test.ts asserts LOOK.edge.width and contrast pairs |
| T-3.6-03 | Tampering | Accidental preview chrome bleed | mitigate | LOOK tokens live under renderer/themes/ only; no preview/styles.css edits |
| T-3.6-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
cd packages/cora && bun run typecheck && bun x vitest run tests/renderer/look-defaults.test.ts
</verification>

<success_criteria>
- Shared look module graph exists and tests pass
- defaultTheme no longer uses pastel colors or default shadows
- catalogDefaultProps ready for Plan 02 renderer wiring and Plan 03 preview sync
</success_criteria>

<output>
Create `.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-01-SUMMARY.md` when done
</output>
