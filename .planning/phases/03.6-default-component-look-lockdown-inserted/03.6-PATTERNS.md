# Phase 3.6: Default Component Look Lockdown - Pattern Map

**Mapped:** 2026-05-24
**Files analyzed:** 18 (3 new, 12 modified, 3 test/doc artifacts)
**Analogs found:** 16 / 18

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `packages/cora/src/renderer/themes/tailwindPalette.ts` | config (static tokens) | transform (named scale → hex) | `packages/cora/src/renderer/themes/transforms.ts` | role-match (static color const map in themes/) |
| `packages/cora/src/renderer/themes/lookTokens.ts` | config (semantic tokens) | transform (role → palette ref) | `packages/cora/src/renderer/themes/default.ts` | role-match (shape/edge/label token grouping) |
| `packages/cora/src/renderer/themes/componentDefaults.ts` | service (factory) | transform (component id → BoxStyleProps) | `packages/cora/src/preview/controls/defaults.ts` | role-match (per-component default props object) |
| `packages/cora/src/renderer/themes/fontTokens.ts` | config | transform (typography constants) | self | exact (extend existing module) |
| `packages/cora/src/renderer/themes/default.ts` | config | transform (LOOK → ThemeTokens) | self + `transforms.ts` | exact (refactor in place) |
| `packages/cora/src/renderer/components/styles.ts` | utility | transform (preset → dimensions) | self | exact (tune `SIZE_PRESETS` values) |
| `packages/cora/src/renderer/components/nodes/shared.tsx` | component utility | transform (props → resolved frame/text) | self | exact (update fallbacks to import tokens) |
| `packages/cora/src/core/measureText.ts` | service | transform (label → measured size) | self | exact (font import + `COMPONENT_MIN_SIZE` tune) |
| `packages/cora/src/renderer/Diagram.tsx` | component router | transform (LayoutedNode → JSX props) | self | exact (`nodeCatalogProps` extension) |
| `packages/cora/src/renderer/components/nodes/BoxNode.tsx` | component | transform (theme/style → SVG rect) | self (`LayoutBoxNode` vs catalog path) | exact |
| `packages/cora/src/preview/controls/defaults.ts` | config | transform (factory re-export) | self (replace literals with imports) | exact |
| `packages/cora/src/preview/pack/builtins.tsx` | config / registry | transform (component id → preview entry) | self (`component()` helper) | exact |
| `packages/cora/tests/renderer/look-defaults.test.ts` | test | batch (assert token contract) | `packages/cora/tests/renderer/components.test.tsx` | role-match |
| `packages/cora/tests/preview/look-sync.test.ts` | test | batch (deep-equal sync gate) | `packages/cora/tests/preview/pack.test.tsx` | role-match |
| `packages/cora/tests/renderer/components.test.tsx` | test | batch | self | exact (update theme assertions) |
| `packages/cora/tests/preview/pack.test.tsx` | test | batch | self | exact (update label font expectations) |
| `packages/cora/tests/golden/*.svg` | test artifact | file-I/O | `packages/cora/tests/render-golden.mjs` workflow | exact |
| `AGENTS.md` | documentation | — | self (`## Renderer Components` section) | exact |

## Pattern Assignments

### `packages/cora/src/renderer/themes/tailwindPalette.ts` (config, transform)

**Analog:** `packages/cora/src/renderer/themes/transforms.ts`

**Static const map pattern** (transforms.ts lines 17-19, 45-58):

```typescript
const BLACK = '#000000';
const WHITE = '#FFFFFF';
const GREY = '#808080';

const MONO_SHAPES: Record<string, ThemeShapeStyle> = {
  rectangle: MONO_BLACK,
  // …
  group: {
    fill: 'none',
    stroke: BLACK,
    strokeWidth: 0.75,
    strokeDasharray: '4 4',
  },
};
```

**Apply:** Export a single `TAILWIND` (or `TAILWIND_COLORS`) object with `as const` scale steps. No runtime CSS. Match project import style: `.js` extension in consumers, no barrel re-export from package root.

```typescript
export const TAILWIND = {
  white: '#ffffff',
  slate: { 50: '#f8fafc', 200: '#e2e8f0', 300: '#cbd5e1', /* … */ },
  violet: { /* … */ },
  // amber, rose, sky, emerald, yellow, teal — full scales for D-04
} as const;
```

---

### `packages/cora/src/renderer/themes/lookTokens.ts` (config, transform)

**Analog:** `packages/cora/src/renderer/themes/default.ts`

**Semantic token grouping** (default.ts lines 4-87):

```typescript
const STROKE_WIDTH = 0.75;
const LABEL = '#1A1A1A';
const EDGE = '#37474F';

export const defaultTheme: ThemeTokens = {
  background: '#F4F9FB',
  shapes: {
    box: {
      fill: '#C9B3FF',
      stroke: '#9470DB',
      shadow: '#7050B8',
      strokeWidth: STROKE_WIDTH,
    },
    // … per-component kind
    group: {
      fill: 'none',
      stroke: '#94A3B8',
      strokeWidth: STROKE_WIDTH,
      strokeDasharray: '4 4',
    },
  },
  edge: { stroke: EDGE, strokeWidth: STROKE_WIDTH },
  nodeLabel: { fontSize: NODE_FONT_SIZE, fontWeight: 600, fill: LABEL },
  edgeLabel: { fontSize: EDGE_FONT_SIZE, fontWeight: 400, fill: LABEL },
  shadowOffset: { x: 0.5, y: 0.5 },
  shadowBlur: 1,
};
```

**Apply:** Export `LOOK` object referencing `TAILWIND.slate[300]` etc. — semantic roles (`surface.fill`, `border.default`, `text.primary`, `edge.stroke`, `radius.md`) consumed by both `buildDefaultTheme()` and `catalogDefaultProps()`. **Omit `shadow` keys** on default shapes (D-06); set `shadowBlur: 0`.

---

### `packages/cora/src/renderer/themes/fontTokens.ts` (config, transform)

**Analog:** self

**Current pattern** (fontTokens.ts lines 1-2):

```typescript
export const NODE_FONT_SIZE = 8;
export const EDGE_FONT_SIZE = 6;
```

**Apply:** Expand to named exports used by renderer, measureText, and componentDefaults:

```typescript
export const NODE_TITLE_SIZE = 12;
export const NODE_SUBTITLE_SIZE = 10;
export const EDGE_LABEL_SIZE = 10;
export const STANDALONE_LABEL_SIZE = 11;
// Keep NODE_FONT_SIZE / EDGE_FONT_SIZE as aliases or replace all imports atomically
export const NODE_FONT_WEIGHT = 600;
export const EDGE_FONT_WEIGHT = 400;
export const LABEL_FONT_WEIGHT = 600;
```

Import sites: `default.ts`, `measureText.ts` (`FONT_SIZE_BY_ROLE`), `shared.tsx` (`CatalogText` defaults), `componentDefaults.ts`.

---

### `packages/cora/src/renderer/themes/componentDefaults.ts` (service, transform)

**Analog:** `packages/cora/src/preview/controls/defaults.ts`

**Per-component defaults object** (defaults.ts lines 39-54, 73-81):

```typescript
export const baseNodeDefaults: PreviewNodeProps = {
  title: 'Component',
  subtitle: '',
  backgroundColor: '#FFFFFF',
  radius: 10,
  borderStyle: 'solid',
  borderColor: '#0A0A0A',
  borderWidth: 1,
  textColor: '#0A0A0A',
  subtitleColor: '#6F6A72',
  titleFontSize: 11,
  subtitleFontSize: 10,
  shadow: 'none',
  size: { width: 140, height: 40 },
  iconColor: '#7B3FE4',
};

export const connectionDefaults: ConnectionProps = {
  lineStyle: 'solid',
  strokeColor: '#334155',
  strokeWidth: 2,
  arrowSize: 8,
  startMarker: 'none',
  endMarker: 'arrow',
  connectionMode: 'auto-side',
};
```

**Apply:** Factory instead of flat const — one switch per `DiagramComponent`:

```typescript
import type { DiagramComponent } from '../../layout-ir.js';
import { LOOK } from './lookTokens.js';
import { NODE_TITLE_SIZE, NODE_SUBTITLE_SIZE, STANDALONE_LABEL_SIZE } from './fontTokens.js';

export function catalogDefaultProps(component: DiagramComponent): CatalogBoxNodeProps {
  switch (component) {
    case 'box':
      return {
        backgroundColor: LOOK.surface.fill,
        borderColor: LOOK.border.default,
        borderWidth: 1,
        borderStyle: 'solid',
        radius: LOOK.radius.md,
        textColor: LOOK.text.primary,
        subtitleColor: LOOK.text.muted,
        titleFontSize: NODE_TITLE_SIZE,
        subtitleFontSize: NODE_SUBTITLE_SIZE,
        shadow: 'none',
        size: 'md',
      };
    case 'label':
      return { /* D-11: transparent, no border, 11px SemiBold */ };
    // … semantic hues per D-04
    default:
      return catalogDefaultProps('box');
  }
}
```

Export `connectionLookDefaults()` or re-use `LOOK.edge` for preview connection defaults.

---

### `packages/cora/src/renderer/themes/default.ts` (config, transform)

**Analog:** self + `transforms.ts` (`withoutShadow`)

**Theme assembly** (default.ts lines 8-87):

```typescript
import type { ThemeTokens } from '../../layout-ir.js';
import { EDGE_FONT_SIZE, NODE_FONT_SIZE } from './fontTokens.js';

export const defaultTheme: ThemeTokens = {
  background: '#F4F9FB',
  shapes: { /* … */ },
  edge: { stroke: EDGE, strokeWidth: STROKE_WIDTH },
  nodeLabel: { fontSize: NODE_FONT_SIZE, fontWeight: 600, fill: LABEL },
  edgeLabel: { fontSize: EDGE_FONT_SIZE, fontWeight: 400, fill: LABEL },
  shadowOffset: { x: 0.5, y: 0.5 },
  shadowBlur: 1,
};

export function resolveNodeStyle(
  component: string | undefined,
): ThemeTokens['shapes'][string] {
  const key = component ?? 'box';
  return defaultTheme.shapes[key] ?? defaultTheme.shapes.box!;
}
```

**Shadow removal reference** (transforms.ts lines 3-14):

```typescript
export function withoutShadow(theme: ThemeTokens): ThemeTokens {
  const shapes: Record<string, ThemeShapeStyle> = {};
  for (const [key, style] of Object.entries(theme.shapes)) {
    const { shadow: _shadow, ...rest } = style;
    shapes[key] = rest;
  }
  return {
    ...theme,
    shapes,
    shadowOffset: { x: 0, y: 0 },
    shadowBlur: 0,
  };
}
```

**Apply:** Replace inline hex with `LOOK` / `TAILWIND` imports. Edge `strokeWidth: 2`, no shape `shadow` keys, `shadowBlur: 0`. Keep `resolveNodeStyle` export unchanged for downstream callers.

---

### `packages/cora/src/renderer/components/styles.ts` (utility, transform)

**Analog:** self

**Preset record pattern** (styles.ts lines 8-27):

```typescript
export const SIZE_PRESETS: Record<SizePreset, ComponentDimensions> = {
  sm: { width: 96, height: 40 },
  md: { width: 128, height: 56 },
  lg: { width: 176, height: 72 },
  xl: { width: 224, height: 96 },
  xxl: { width: 288, height: 128 },
};

export function resolveComponentSize(
  size: ComponentSize | undefined,
  fallback: ComponentDimensions,
): ComponentDimensions {
  if (!size) {
    return fallback;
  }
  return typeof size === 'string' ? SIZE_PRESETS[size] : size;
}
```

**Apply:** Tune width/height values only — keep `sm`–`xxl` vocabulary (D-10). Coordinate with `COMPONENT_MIN_SIZE` in measureText after font bump 8→12.

---

### `packages/cora/src/renderer/components/nodes/shared.tsx` (component utility, transform)

**Analog:** self

**Catalog frame fallbacks** (shared.tsx lines 65-87):

```typescript
export function resolvedCatalogFrame(props: CatalogNodeFrameProps) {
  const size = resolveComponentSize(props.size, props.fallbackSize ?? DEFAULT_CATALOG_SIZE);
  const borderWidth = props.borderWidth ?? 1;
  const noBorder = isNoBorder(props.borderStyle, props.borderWidth);

  return {
    x: props.x ?? 0,
    y: props.y ?? 0,
    width: size.width,
    height: size.height,
    radius: props.radius ?? 4,
    backgroundColor: props.backgroundColor ?? '#ffffff',
    borderColor: noBorder ? undefined : props.borderColor ?? '#94a3b8',
    borderWidth: noBorder ? undefined : borderWidth,
    borderDasharray: noBorder ? undefined : borderDasharray(props.borderStyle, borderWidth),
    text: props.title ?? props.text,
    subtitle: props.subtitle,
    textColor: props.textColor ?? '#0f172a',
    subtitleColor: props.subtitleColor ?? '#64748b',
    titleFontSize: props.titleFontSize,
    subtitleFontSize: props.subtitleFontSize,
    shadow: props.shadow ?? 'none',
  };
}
```

**CatalogText defaults** (shared.tsx lines 260-272):

```typescript
export function CatalogText({
  // …
  color = '#0f172a',
  subtitleColor = '#64748b',
  fontSize = 13,
  subtitleFontSize,
  paddingX = 4,
  minFontSize = 9,
}: { /* … */ }) {
```

**Apply:** Replace `?? 4`, `#94a3b8`, `#0f172a`, `fontSize = 13` with imports from `lookTokens.ts` / `fontTokens.ts` (`LOOK.radius.md`, `LOOK.text.primary`, `NODE_TITLE_SIZE`, `NODE_SUBTITLE_SIZE`). Do not add new abstractions — inline token refs match existing fallback style.

**NodeLabel theme path** (shared.tsx lines 14-30):

```typescript
export function NodeLabel({ node, theme }: NodeComponentProps) {
  const centerY = node.y + node.measuredHeight / 2;
  const style = node.resolvedStyle;
  const fill = style?.labelFill ?? theme.nodeLabel.fill;
  return (
    <text
      x={node.x + node.measuredWidth / 2}
      y={baselineYForVisualCenter(centerY, theme.nodeLabel.fontSize, 'node')}
      textAnchor="middle"
      fontFamily={FONT_FAMILY}
      fontSize={theme.nodeLabel.fontSize}
      fontWeight={theme.nodeLabel.fontWeight}
      fill={fill}
    >
      {escapeXml(node.label)}
    </text>
  );
}
```

NodeLabel automatically picks up 12px when `defaultTheme.nodeLabel.fontSize` changes — no separate edit unless subtitle support is added to layout box path.

---

### `packages/cora/src/core/measureText.ts` (service, transform)

**Analog:** self

**Font token import + sizing constants** (measureText.ts lines 8-27):

```typescript
import {
  EDGE_FONT_SIZE,
  NODE_FONT_SIZE,
} from '../renderer/themes/fontTokens.js';

const FONT_SIZE_BY_ROLE = { node: NODE_FONT_SIZE, edge: EDGE_FONT_SIZE } as const;
const NODE_PADDING_X = 11;
const NODE_PADDING_Y = 6;
const DECISION_EXTRA_PADDING_Y = 8;
const ICON_LABEL_EXTRA_WIDTH = 34;
const COMPONENT_MIN_SIZE = {
  app: { width: 112, height: 56 },
  icon: { width: 40, height: 40 },
  issue: { width: 112, height: 46 },
  labelIcon: { width: 112, height: 46 },
  page: { width: 128, height: 84 },
  website: { width: 128, height: 56 },
} as const;
```

**Apply:** After font token bump, retune padding and `COMPONENT_MIN_SIZE` together with `SIZE_PRESETS`. Keep fontkit + Noto path unchanged (`fontPaths` lines 46-49).

---

### `packages/cora/src/renderer/Diagram.tsx` (component router, transform)

**Analog:** self

**Dual render path** (Diagram.tsx lines 30-66, 79-91):

```typescript
function renderNode(node: LayoutedNode, diagram: LayoutedDiagram) {
  const component = node.component ?? 'box';
  const catalogProps = nodeCatalogProps(node, diagram.theme.shapes[component] ?? diagram.theme.shapes.box!);

  switch (component) {
    case 'label':
      return <LabelNode key={node.id} {...catalogProps} backgroundColor="transparent" borderStyle="none" />;
    // …
    case 'box':
    default:
      return <BoxNode key={node.id} node={node} theme={diagram.theme} />;
  }
}

function nodeCatalogProps(node: LayoutedNode, style: ThemeShapeStyle) {
  return {
    x: node.x,
    y: node.y,
    size: { width: node.measuredWidth, height: node.measuredHeight },
    backgroundColor: style.fill,
    borderColor: style.stroke,
    borderWidth: style.strokeWidth,
    borderStyle: borderStyleFor(style),
    text: node.label,
    textColor: style.labelFill ?? '#1A1A1A',
  } as const;
}
```

**Apply:** Extend `nodeCatalogProps` with `radius`, `titleFontSize`, `subtitleFontSize`, `shadow: 'none'` from `catalogDefaultProps(component)` merged with theme-resolved colors. Box path stays on `LayoutBoxNode` — do not route default box through catalog (research recommendation (a)).

**Edge stroke from theme** (Diagram.tsx lines 109, 120+):

```typescript
<LineMarkerDefs color={diagram.theme.edge.stroke} />
```

Edge width comes from theme — verify 2px propagates to `<Line strokeWidth={…}>` in edges group.

---

### `packages/cora/src/renderer/components/nodes/BoxNode.tsx` (component, transform)

**Analog:** self — `LayoutBoxNode` vs catalog `CatalogFrame` path

**Layout box path (YAML default box)** (BoxNode.tsx lines 40-66):

```typescript
function LayoutBoxNode({ node, theme }: NodeComponentProps) {
  const style = node.resolvedStyle ?? theme.shapes.box!;
  const sw = strokeWidth(style);

  const renderShape = (offsetX: number, offsetY: number, paint: string, stroke?: string) => (
    <rect
      x={node.x + offsetX}
      y={node.y + offsetY}
      width={node.measuredWidth}
      height={node.measuredHeight}
      fill={paint}
      stroke={stroke}
      strokeWidth={stroke ? sw : undefined}
    />
  );

  return (
    <g>
      <ShapeShadow
        theme={theme}
        hasShadow={Boolean(style.shadow)}
        renderShape={(ox, oy) => renderShape(ox, oy, style.shadow!)}
      />
      {renderShape(0, 0, style.fill, style.stroke)}
      <NodeLabel node={node} theme={theme} />
    </g>
  );
}
```

**Catalog preview path** (BoxNode.tsx lines 74-104): uses `CatalogFrame` + `CatalogText` with explicit props.

**Apply:** Add `rx={LOOK.radius.md}` / `ry={LOOK.radius.md}` to layout rect. With theme shapes omitting `shadow`, `ShapeShadow` renders null. Primary YAML/preview skew fix lives here.

**Dual-path dispatch** (BoxNode.tsx lines 36-38, 69-72):

```typescript
function isLayoutBoxNodeProps(props: BoxNodeProps): props is NodeComponentProps {
  return 'node' in props && 'theme' in props;
}

export function BoxNode(props: BoxNodeProps) {
  if (isLayoutBoxNodeProps(props)) {
    return <LayoutBoxNode {...props} />;
  }
  // catalog path …
}
```

Preserve this discriminated union — do not merge paths.

---

### `packages/cora/src/preview/controls/defaults.ts` (config, transform)

**Analog:** self (becomes thin re-export wrapper)

**Type exports to preserve** (defaults.ts lines 5-37):

```typescript
export type PreviewNodeProps = {
  title?: string;
  subtitle?: string;
  // … BoxStyleProps-aligned fields
};

export type ConnectionProps = {
  lineStyle: 'solid' | 'dashed' | 'dotted';
  strokeColor: string;
  strokeWidth: number;
  // …
};
```

**Apply:** Replace `baseNodeDefaults` body with:

```typescript
import { catalogDefaultProps } from '../../renderer/themes/componentDefaults.js';
import { LOOK } from '../../renderer/themes/lookTokens.js';

export const baseNodeDefaults: PreviewNodeProps = {
  title: 'Component',
  subtitle: '',
  ...catalogDefaultProps('box'),
};

export const connectionDefaults: ConnectionProps = {
  lineStyle: 'solid',
  strokeColor: LOOK.edge.stroke,
  strokeWidth: LOOK.edge.width,
  arrowSize: 8,
  startMarker: 'none',
  endMarker: 'arrow',
  connectionMode: 'auto-side',
};
```

Keep `baseNodeControls`, `pageNodeControls`, etc. unchanged — inspector schema is separate from default values.

---

### `packages/cora/src/preview/pack/builtins.tsx` (config/registry, transform)

**Analog:** self

**Component registration helper** (builtins.tsx lines 28-42):

```typescript
const component = (
  id: string,
  label: string,
  family: string,
  componentImpl: PreviewComponentDefinition<PreviewNodeProps>['component'],
  defaultProps: PreviewNodeProps = baseNodeDefaults,
  controls = baseNodeControls,
): PreviewComponentDefinition<PreviewNodeProps> => ({
  id,
  label,
  family,
  component: componentImpl,
  defaultProps: { ...defaultProps, title: defaultProps.title ?? defaultProps.text ?? label },
  controls,
});
```

**Current per-kind hex overrides to remove** (builtins.tsx lines 54-113):

```typescript
component('box', 'BoxNode', 'basic', BoxNode, {
  ...baseNodeDefaults,
  backgroundColor: '#FFFFFF',
  borderColor: '#0A0A0A',
  textColor: '#0A0A0A',
  radius: 14,
  size: { width: 168, height: 44 },
}),
// website: backgroundColor: '#FFF7CC'
// app: backgroundColor: '#DCFCE7'
// decision: backgroundColor: '#FED7AA'
// shape: backgroundColor: '#CCFBF1'
// icon/labelIcon: iconColor: '#2F7D7E'
```

**Apply:**

```typescript
import { catalogDefaultProps } from '../../renderer/themes/componentDefaults.js';

component('box', 'BoxNode', 'basic', BoxNode, {
  ...catalogDefaultProps('box'),
  title: 'BoxNode',
}),
component('website', 'WebsiteNode', 'product', WebsiteNode, {
  ...catalogDefaultProps('website'),
  title: 'WebsiteNode',
}),
```

Only override demo `title`/`subtitle`/`type`/`icon` — never color hex literals.

---

### `packages/cora/src/core/themeResolver.ts` (service, transform)

**Analog:** self — **no structural change expected**

**Merge pipeline** (themeResolver.ts lines 3-47):

```typescript
function resolveNodeStyle(node: DiagramNode, theme: ThemeTokens): ResolvedStyle {
  const key = node.component ?? 'box';
  const base = theme.shapes[key] ?? theme.shapes.box!;
  const overrides = node.style ?? {};
  return {
    fill: typeof overrides.fill === 'string' ? overrides.fill : base.fill,
    stroke: typeof overrides.stroke === 'string' ? overrides.stroke : base.stroke,
    shadow: typeof overrides.shadow === 'string' ? overrides.shadow : base.shadow,
    // …
  };
}

export function resolveTheme(
  diagram: Diagram,
  defaultTheme: ThemeTokens,
): { diagram: Diagram; theme: ThemeTokens; nodeStyles: Map<string, ResolvedStyle> } {
  const themeName = diagram.theme ?? 'default';
  if (themeName !== 'default') {
    throw new Error(`Unknown theme: ${themeName}. Only 'default' is supported.`);
  }
  // …
}
```

Phase 5 extension themes plug in here — update `defaultTheme` content only, not resolver shape.

---

### `packages/cora/tests/renderer/look-defaults.test.ts` (test, batch)

**Analog:** `packages/cora/tests/renderer/components.test.tsx`

**Vitest + theme import pattern** (components.test.tsx lines 1-4, 27, 37-47):

```typescript
import { renderToStaticMarkup } from 'react-dom/server';
import { describe, expect, it } from 'vitest';
import { defaultTheme } from '../../src/renderer/themes/default.js';

describe('renderer component primitives', () => {
  it('resolves size presets and explicit dimensions', () => {
    expect(resolveComponentSize('sm', fallback)).toEqual({ width: 96, height: 40 });
  });
});
```

**Apply:** New file asserting:
- `defaultTheme.edge.strokeWidth === 2`
- No `shadow` on default shape entries
- `catalogDefaultProps('box').radius === 8`
- Semantic hue coverage for all `DiagramComponent` kinds
- Contrast pairs: white/near-white fill + slate-800+ text

---

### `packages/cora/tests/preview/look-sync.test.ts` (test, batch)

**Analog:** `packages/cora/tests/preview/pack.test.tsx`

**Pack inspection pattern** (pack.test.tsx lines 1-34):

```typescript
import { describe, expect, it } from 'vitest';
import { builtInPack } from '../../src/preview/pack/builtins.js';

describe('built-in preview pack', () => {
  it('uses compact text defaults for label nodes', () => {
    const labelNode = builtInPack.components.find((component) => component.id === 'label');
    expect(labelNode?.defaultProps).toMatchObject({
      titleFontSize: 9,
      subtitleFontSize: 8,
    });
  });
});
```

**Apply:** Replace assertion with D-11 values (11/10). Add loop:

```typescript
import { catalogDefaultProps } from '../../src/renderer/themes/componentDefaults.js';

for (const { id, defaultProps } of builtInPack.components) {
  it(`${id} defaultProps match catalogDefaultProps`, () => {
    const expected = catalogDefaultProps(id as DiagramComponent);
    expect(defaultProps).toMatchObject(expected);
  });
}
```

Exclude scenario-only fields (`title`, `type`, `icon`) from deep-equal or strip before compare.

---

### `packages/cora/tests/golden/*.svg` (test artifact, file-I/O)

**Analog:** `packages/cora/tests/render-golden.mjs`

**Refresh workflow** (render-golden.mjs lines 1-4, 22-28):

```javascript
/**
 * Golden SVG regression runner for cora render.
 * Refresh baselines: UPDATE_GOLDEN=1 node tests/render-golden.mjs
 */
const FIXTURES = [
  'box-arrows',
  'flowchart',
  'microservice',
  'infra',
  'database',
];
```

**Invariant checks** (render-golden.mjs lines 39-48):

```javascript
function assertInvariants(svg, kind) {
  if (svg.includes('foreignObject')) {
    throw new Error(`${kind}: output must not contain foreignObject`);
  }
  // viewBox, xmlns …
}
```

**Apply:** Refresh only after all token wiring (step 12 in RESEARCH.md). Post-refresh goldens must **not** contain `filter="url(#cora-shadow-blur)"` on default box nodes. ASCII/text tests remain separate — layout may shift with larger fonts (LOOK-06).

---

### `AGENTS.md` (documentation)

**Analog:** self — `## Renderer Components` section (line 195+)

**Apply:** Add `### Default Look` subsection documenting:
- Tailwind slate-neutral base + semantic hue per component kind
- Omission defaults (radius 8, no shadow, 12/10 typography, 2px edges)
- Single source: `renderer/themes/componentDefaults.ts`
- Pointer: preview defaults import same factory (D-12)

Do not document preview chrome tokens (`--preview-accent`) — those stay Phase 3.5 local per `visual-contract.test.tsx`.

---

## Shared Patterns

### Single source of truth (D-12)

**Source:** New `componentDefaults.ts` + `lookTokens.ts`  
**Apply to:** `default.ts`, `preview/controls/defaults.ts`, `preview/pack/builtins.tsx`, `shared.tsx` fallbacks, `Diagram.tsx` catalog props

Rule: **No hex literals outside `tailwindPalette.ts`**. Preview and renderer consume the same factory.

### Theme resolution pipeline (unchanged seam)

**Source:** `packages/cora/src/core/themeResolver.ts`  
**Apply to:** All YAML render paths

```typescript
export function resolveTheme(
  diagram: Diagram,
  defaultTheme: ThemeTokens,
): { diagram: Diagram; theme: ThemeTokens; nodeStyles: Map<string, ResolvedStyle> }
```

Update `defaultTheme` content; do not fork resolver.

### Typography measurement coupling

**Source:** `packages/cora/src/core/measureText.ts` + `packages/cora/src/renderer/themes/fontTokens.ts`  
**Apply to:** `default.ts`, `shared.tsx`, `BoxNode.tsx`, PDF overlay tests

Font size changes must land atomically — measureText, SIZE_PRESETS, COMPONENT_MIN_SIZE, goldens, PDF assertions.

### Pure SVG shadow opt-in

**Source:** `packages/cora/src/renderer/themes/transforms.ts` (`withoutShadow`) + `shared.tsx` (`shadow ?? 'none'`)  
**Apply to:** `default.ts`, `BoxNode.tsx` (`ShapeShadow` gated on `style.shadow`)

Default theme: no shape shadows, `shadowBlur: 0`. Explicit YAML/prop shadow still works via catalog `CatalogShadow`.

### Preview vs diagram token boundary

**Source:** `packages/cora/tests/preview/visual-contract.test.tsx` (lines 38-47)

```typescript
it('keeps the preview palette and surface tokens local and stable', () => {
  const css = readFileSync(join(packageRoot, 'src/preview/styles.css'), 'utf8');
  expect(css).toContain('--preview-accent: #6d28d9');
  // …
});
```

**Apply to:** All Phase 3.6 work — do **not** change `preview/styles.css` chrome tokens; only diagram component defaults.

### Golden regression gate

**Source:** `packages/cora/tests/render-golden.mjs`  
**Apply to:** Final plan task after all visual wiring

```bash
cd packages/cora
bun run build
UPDATE_GOLDEN=1 node tests/render-golden.mjs
bun run test:golden
```

---

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| `packages/cora/src/layout-ir.ts` | model (types) | — | Likely unchanged; `ThemeTokens` shape already supports optional `shadow` — no new fields needed for look lockdown |

---

## Metadata

**Analog search scope:** `packages/cora/src/renderer/themes/`, `packages/cora/src/renderer/components/`, `packages/cora/src/renderer/Diagram.tsx`, `packages/cora/src/core/measureText.ts`, `packages/cora/src/core/themeResolver.ts`, `packages/cora/src/preview/controls/`, `packages/cora/src/preview/pack/`, `packages/cora/tests/renderer/`, `packages/cora/tests/preview/`, `packages/cora/tests/render-golden.mjs`, `.planning/phases/3.5-preview-visual-beauty/03.5-PATTERNS.md`
**Files scanned:** ~25
**Pattern extraction date:** 2026-05-24
