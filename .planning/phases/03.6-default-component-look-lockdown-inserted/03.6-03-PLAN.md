---
phase: 3.6-default-component-look-lockdown-inserted
plan: 03
type: execute
wave: 2
depends_on:
  - 03.6-01
files_modified:
  - packages/cora/src/preview/controls/defaults.ts
  - packages/cora/src/preview/pack/builtins.tsx
  - packages/cora/tests/preview/look-sync.test.ts
  - packages/cora/tests/preview/pack.test.tsx
autonomous: true
requirements:
  - LOOK-04
  - LOOK-05
requirements_addressed:
  - LOOK-04
  - LOOK-05
decision_refs:
  - D-04
  - D-08
  - D-11
  - D-12

must_haves:
  truths:
    - "Preview baseNodeDefaults equals catalogDefaultProps('box') for all style fields"
    - "Every built-in pack component defaultProps match catalogDefaultProps for color, radius, typography, border, and shadow fields"
    - "Preview connectionDefaults use LOOK.edge stroke and width 2"
    - "builtins.tsx contains no ad-hoc hex color overrides except demo titles/types/icons"
  artifacts:
    - path: packages/cora/src/preview/controls/defaults.ts
      provides: Thin wrapper importing catalogDefaultProps
      pattern: "catalogDefaultProps"
    - path: packages/cora/src/preview/pack/builtins.tsx
      provides: Per-component defaults from factory
    - path: packages/cora/tests/preview/look-sync.test.ts
      provides: D-12 deep-equal sync gate
  key_links:
    - from: packages/cora/src/preview/pack/builtins.tsx
      to: packages/cora/src/renderer/themes/componentDefaults.ts
      via: catalogDefaultProps spread
      pattern: "catalogDefaultProps\\("
    - from: packages/cora/tests/preview/look-sync.test.ts
      to: packages/cora/src/preview/pack/builtins.tsx
      via: builtInPack.components loop
      pattern: "builtInPack"
---

## Phase Goal

**As a** developer using cora preview, **I want to** see the same default component appearance as rendered diagrams, **so that** preview inspection reflects the locked look contract exactly (D-12).

<objective>
Sync preview pack defaults and inspector initial values to the shared catalogDefaultProps factory — eliminating duplicate hex literals in preview controls and builtins.

Purpose: Satisfy D-12 exact preview sync and LOOK-04 cross-surface normalization.
Output: Updated defaults.ts, builtins.tsx, look-sync.test.ts, pack.test.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-CONTEXT.md (D-12)
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-RESEARCH.md (Layer 3 divergence)
@.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-PATTERNS.md

<interfaces>
From Plan 01:
```typescript
export function catalogDefaultProps(component: DiagramComponent): /* BoxStyleProps-aligned */;
export const LOOK = { edge: { stroke: string; width: 2 } };
```

From packages/cora/src/preview/pack/builtins.tsx:
```typescript
export const builtInPack: PackManifest = {
  components: [
    component('box', 'BoxNode', 'basic', BoxNode, { /* hex overrides — remove */ }),
    // … label, icon, website, page, app, decision, issue, shape
  ],
};
```

Style fields to compare in sync test (exclude demo-only):
title, subtitle, type, icon, iconType, text — NOT compared for equality
</interfaces>
</context>

<tasks>

<task type="auto" id="3.6-03-01">
  <name>Wire preview control defaults to shared factory</name>
  <files>packages/cora/src/preview/controls/defaults.ts</files>
  <read_first>
    - packages/cora/src/preview/controls/defaults.ts
    - packages/cora/src/renderer/themes/componentDefaults.ts
    - packages/cora/src/renderer/themes/lookTokens.ts
    - .planning/phases/03.6-default-component-look-lockdown-inserted/03.6-CONTEXT.md (D-08, D-12)
  </read_first>
  <action>
Refactor packages/cora/src/preview/controls/defaults.ts per D-12:

Import catalogDefaultProps from ../../renderer/themes/componentDefaults.js and LOOK from lookTokens.js

Replace baseNodeDefaults body with:
- title: 'Component', subtitle: '', ...catalogDefaultProps('box')
- Remove hardcoded #0A0A0A, #7B3FE4, radius 10, titleFontSize 11

Replace pageNodeDefaults with { ...catalogDefaultProps('page'), title: 'PageNode.type', type: 'landing' } — use catalogDefaultProps skeleton colors, not hardcoded #64748B/#E2E8F0 unless they match LOOK tokens

Replace issueNodeDefaults with { ...catalogDefaultProps('issue'), title: 'IssueNode.icon', icon: 'warning' }

Replace connectionDefaults strokeColor with LOOK.edge.stroke and strokeWidth with LOOK.edge.width (2)

Preserve PreviewNodeProps, ConnectionProps types and all baseNodeControls/pageNodeControls/etc. inspector schemas unchanged — only default values change

Do NOT edit preview/styles.css (Phase 3.5 chrome stays local)
  </action>
  <acceptance_criteria>
- defaults.ts imports catalogDefaultProps
- baseNodeDefaults.radius equals catalogDefaultProps('box').radius (8)
- connectionDefaults.strokeWidth === 2
- defaults.ts contains no literal '#0A0A0A' or '#7B3FE4'
- cd packages/cora && bun run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && rg "catalogDefaultProps" src/preview/controls/defaults.ts && ! rg "#0A0A0A|#7B3FE4" src/preview/controls/defaults.ts && bun run typecheck</automated>
  </verify>
  <done>Preview inspector initial values derive from shared factory</done>
</task>

<task type="auto" id="3.6-03-02">
  <name>Sync builtins pack and add look-sync regression test</name>
  <files>packages/cora/src/preview/pack/builtins.tsx, packages/cora/tests/preview/look-sync.test.ts, packages/cora/tests/preview/pack.test.tsx</files>
  <read_first>
    - packages/cora/src/preview/pack/builtins.tsx
    - packages/cora/tests/preview/pack.test.tsx
    - packages/cora/src/renderer/themes/componentDefaults.ts
    - .planning/phases/03.6-default-component-look-lockdown-inserted/03.6-VALIDATION.md
  </read_first>
  <action>
Update packages/cora/src/preview/pack/builtins.tsx per D-04, D-11, D-12:

Import catalogDefaultProps from ../../renderer/themes/componentDefaults.js

For each component registration, replace color/radius/typography overrides with catalogDefaultProps(id) spread plus demo-only fields:
- box: { ...catalogDefaultProps('box'), title: 'BoxNode' } — remove radius 14, #0A0A0A border
- label: { ...catalogDefaultProps('label'), title: 'LabelNode' }
- icon/labelIcon: use catalogDefaultProps; keep iconType demo values only
- website/app/decision/shape/page/issue: catalogDefaultProps(id) + title string; remove #FFF7CC, #DCFCE7, #FED7AA, #CCFBF1, #2F7D7E hex overrides

Create packages/cora/tests/preview/look-sync.test.ts:
- Import builtInPack and catalogDefaultProps
- Define STYLE_KEYS array: backgroundColor, borderColor, borderWidth, borderStyle, radius, textColor, subtitleColor, titleFontSize, subtitleFontSize, shadow, size, iconColor, strokeColor, skeletonColorDark, skeletonColorLight (only assert keys present on expected)
- For each builtInPack.components entry where id is a DiagramComponent, toMatchObject catalogDefaultProps(id) on style keys

Update packages/cora/tests/preview/pack.test.tsx:
- Change label node font expectation from titleFontSize 9 / subtitleFontSize 8 to 11 / 10 per D-11
- Remove assertions that expect old preview-only hex values

Verify grep gate: no hex color literals in builtins.tsx except none expected (all from spread)
  </action>
  <acceptance_criteria>
- builtins.tsx uses catalogDefaultProps for box, label, website, app, page, decision, issue, shape, icon, labelIcon
- look-sync.test.ts exists and tests all pack component ids
- pack.test.tsx expects label titleFontSize 11
- cd packages/cora && bun x vitest run tests/preview/look-sync.test.ts tests/preview/pack.test.tsx exits 0
- rg '#[0-9A-Fa-f]{6}' packages/cora/src/preview/pack/builtins.tsx returns zero matches
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && test -f tests/preview/look-sync.test.ts && rg "catalogDefaultProps" src/preview/pack/builtins.tsx && ! rg -i '#[0-9a-f]{6}' src/preview/pack/builtins.tsx && bun x vitest run tests/preview/look-sync.test.ts tests/preview/pack.test.tsx -x</automated>
  </verify>
  <done>Preview pack defaults match renderer factory; sync test prevents drift</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview SPA → renderer components | Preview imports renderer theme modules (dev-only path) |
| Inspector defaults → canvas render | User edits props after load; initial state must match factory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.6-07 | Tampering | builtins.tsx hex reintroduction | mitigate | look-sync.test.ts + grep gate in verify |
| T-3.6-08 | Spoofing | Preview chrome mistaken for diagram defaults | mitigate | Do not edit preview/styles.css; visual-contract.test.tsx unchanged |
| T-3.6-09 | Repudiation | Sync drift undetected | mitigate | look-sync.test.ts in CI via vitest run |
| T-3.6-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
cd packages/cora && bun run typecheck && bun x vitest run tests/preview/look-sync.test.ts tests/preview/pack.test.tsx
</verification>

<success_criteria>
- D-12 satisfied: preview and renderer share identical default prop values
- No ad-hoc hex in preview pack or control defaults
- LOOK-04 normalization spans preview and renderer surfaces
</success_criteria>

<output>
Create `.planning/phases/03.6-default-component-look-lockdown-inserted/03.6-03-SUMMARY.md` when done
</output>
