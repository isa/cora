---
phase: 3.4-ascii-diagram-export-and-skills-md-file
plan: 04
type: execute
wave: 4
depends_on: ["3.4-01", "3.4-02", "3.4-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - packages/cora/tests/renderer/text.test.ts
  - packages/cora/tests/cli/render-text.test.ts
  - packages/cora/tests/smoke/clean-install.sh
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - ASCII-01
  - ASCII-02
  - ASCII-03
  - ASCII-04
  - ASCII-05
  - AGT-04
tags: [requirements, verification, regression, examples, docs]
decision_refs:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15

must_haves:
  truths:
    - "ASCII-01 through ASCII-05 and AGT-04 are recorded in `.planning/REQUIREMENTS.md`"
    - "All five valid v1 examples have text export coverage"
    - "JSON render failure behavior is covered by tests"
    - "Full verification includes typecheck, Vitest, build, golden SVG regression, and clean-install smoke"
    - "Docs describe text export limitations versus SVG/PDF"
    - "No Phase 3.4 plan removes or changes preview distribution; Phase 3.6 owns that"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ASCII and AGT-04 requirement trace"
      contains: "ASCII-01"
    - path: "packages/cora/tests/renderer/text.test.ts"
      provides: "All-kind text export coverage"
      contains: "database.yaml"
    - path: "packages/cora/tests/cli/render-text.test.ts"
      provides: "CLI behavior and JSON failure coverage"
      contains: "SCHEMA_VIOLATION"
---

<objective>
Close the phase by adding requirement traceability, all-example coverage, and full regression verification for text export and `SKILL.md`.
</objective>

<decision_coverage>
- D-01 through D-04 are covered by renderer tests and all-example coverage.
- D-05 through D-09 are covered by CLI tests and docs.
- D-10 through D-15 are covered by `SKILL.md`, package smoke checks, and docs.
</decision_coverage>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-3.4-10: Requirements remain only in ROADMAP and cannot be audited later | medium | Add ASCII-01 through ASCII-05 and AGT-04 to `.planning/REQUIREMENTS.md` and map them to Phase 3.4. |
| T-3.4-11: Text export passes minimal tests but fails another v1 kind | high | Ensure renderer tests cover box-arrows, flowchart, microservice, infra, and database examples. |
| T-3.4-12: Text changes accidentally regress SVG/PDF/goldens | medium | Run full typecheck, Vitest, build, golden SVG regression, and clean-install smoke. |
</threat_model>

<tasks>

<task type="execute" id="3.4-04-01">
  <name>Add ASCII and AGT-04 requirement traceability</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/3.4-ascii-diagram-export-and-skills-md-file/3.4-CONTEXT.md
    - .planning/phases/3.4-ascii-diagram-export-and-skills-md-file/3.4-RESEARCH.md
  </read_first>
  <action>
Update `.planning/REQUIREMENTS.md` with a `Text Export` or `ASCII/Text Export` requirement family containing:

- `ASCII-01`: `cora render diagram.yaml -o diagram.txt` selects a text export path from the output extension.
- `ASCII-02`: Text output preserves nodes, groups, labels, and directional relationships in a readable fixed-width layout suitable for terminals, Markdown, pull requests, and agent logs.
- `ASCII-03`: Text export is deterministic in CI and does not require browser, SVG, PDF, or image dependencies.
- `ASCII-04`: Validation and render failures keep the existing structured JSON error behavior when `--format json` is requested.
- `ASCII-05`: Documentation explains text export limitations versus SVG/PDF, including layout simplification and unsupported visual styling.

Add `AGT-04`: `SKILL.md` documents practical agent workflows, triggers, examples, and reusable prompts for Cora diagram authoring and review.

Map `ASCII-01` through `ASCII-05` and `AGT-04` to Phase 3.4 in the traceability table with status `Pending` or equivalent not-yet-done wording until execution summaries mark them complete.
  </action>
  <acceptance_criteria>
- `.planning/REQUIREMENTS.md` contains `ASCII-01`, `ASCII-02`, `ASCII-03`, `ASCII-04`, `ASCII-05`, and `AGT-04`.
- `.planning/REQUIREMENTS.md` contains `Text Export` or `ASCII/Text Export`.
- `.planning/REQUIREMENTS.md` maps each ASCII requirement to Phase 3.4.
- `.planning/REQUIREMENTS.md` maps `AGT-04` to Phase 3.4.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "ASCII-01|ASCII-02|ASCII-03|ASCII-04|ASCII-05|AGT-04|Phase 3.4|Text Export|ASCII/Text Export" .planning/REQUIREMENTS.md</automated>
  </verify>
</task>

<task type="execute" id="3.4-04-02">
  <name>Harden all-kind text export and JSON failure coverage</name>
  <read_first>
    - packages/cora/tests/renderer/text.test.ts
    - packages/cora/tests/cli/render-text.test.ts
    - examples/valid/box-arrows.yaml
    - examples/valid/flowchart.yaml
    - examples/valid/microservice.yaml
    - examples/valid/infra.yaml
    - examples/valid/database.yaml
    - examples/invalid/missing-version.yaml
  </read_first>
  <action>
Review and extend existing Phase 3.4 tests so they prove:

- all five v1 examples (`box-arrows`, `flowchart`, `microservice`, `infra`, `database`) render to text without throwing
- `minimal.yaml` text output contains both `API` and `Database`
- default text output contains Unicode box-drawing glyphs
- ASCII fallback output contains only plain ASCII characters for frame/line glyphs
- render failure with `--format json` still emits parseable JSON array output with a stable error `code`

Do not add golden SVG changes unless the executor can prove render output intentionally changed. Text export should not alter SVG/PDF render output.
  </action>
  <acceptance_criteria>
- `packages/cora/tests/renderer/text.test.ts` references all five v1 example fixture names.
- `packages/cora/tests/renderer/text.test.ts` asserts repeated text output is deterministic.
- `packages/cora/tests/cli/render-text.test.ts` asserts `--format json` failure output parses as JSON.
- `packages/cora/tests/cli/render-text.test.ts` asserts stdout text output when no `-o` is passed.
- `cd packages/cora && bun x vitest run tests/renderer/text.test.ts tests/cli/render-text.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "box-arrows|flowchart|microservice|infra|database|determin" packages/cora/tests/renderer/text.test.ts && rg "--format json|JSON.parse|SCHEMA_VIOLATION|stdout" packages/cora/tests/cli/render-text.test.ts && cd packages/cora && bun x vitest run tests/renderer/text.test.ts tests/cli/render-text.test.ts</automated>
  </verify>
</task>

<task type="execute" id="3.4-04-03">
  <name>Run full regression and update docs if verification exposes gaps</name>
  <read_first>
    - README.md
    - AGENTS.md
    - SKILL.md
    - packages/cora/tests/smoke/clean-install.sh
    - packages/cora/tests/render-golden.mjs
    - packages/cora/package.json
  </read_first>
  <action>
Run full verification:

- `cd packages/cora && bun run typecheck`
- `cd packages/cora && bun x vitest run`
- `cd packages/cora && bun run build`
- `cd packages/cora && bun run test:golden`
- `bash packages/cora/tests/smoke/clean-install.sh` from repo root

If verification exposes documentation wording gaps around `.txt`, stdout text render, `--charset ascii`, or `SKILL.md`, update `README.md`, `AGENTS.md`, or `SKILL.md` with the exact behavior already implemented. Do not broaden scope beyond Phase 3.4.
  </action>
  <acceptance_criteria>
- `cd packages/cora && bun run typecheck` exits 0.
- `cd packages/cora && bun x vitest run` exits 0.
- `cd packages/cora && bun run build` exits 0.
- `cd packages/cora && bun run test:golden` exits 0.
- `bash packages/cora/tests/smoke/clean-install.sh` exits 0 from repo root.
- `README.md`, `AGENTS.md`, and `SKILL.md` mention `.txt` or text output only in ways matching the implemented CLI behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora/packages/cora && bun run typecheck && bun x vitest run && bun run build && bun run test:golden && cd /Users/isa.goksu/Projects/playgrounds/node/cora && bash packages/cora/tests/smoke/clean-install.sh && rg "diagram.txt|text output|SKILL.md" README.md AGENTS.md SKILL.md</automated>
  </verify>
</task>

</tasks>

<verification>
Run from the repository root:

```bash
rg "ASCII-01|ASCII-02|ASCII-03|ASCII-04|ASCII-05|AGT-04" .planning/REQUIREMENTS.md
cd packages/cora
bun run typecheck
bun x vitest run
bun run build
bun run test:golden
cd ../..
bash packages/cora/tests/smoke/clean-install.sh
```
</verification>

<success_criteria>
- All Phase 3.4 requirement IDs are tracked in requirements.
- Text export tests cover all v1 kinds and JSON failure behavior.
- Full test/build/golden/smoke verification passes.
- Docs and package checks accurately describe the shipped text export and `SKILL.md`.
</success_criteria>
