---
phase: 3.4-ascii-diagram-export-and-skills-md-file
plan: 02
subsystem: cli
tags: [render-command, stdout, txt, json-errors, charset]
requires:
  - phase: 3.4-01
    provides: `renderToText` renderer API
provides:
  - `.txt` output support in `cora render`
  - stdout text rendering when `-o` is omitted
  - `--charset unicode|ascii` validation
  - CLI regression tests for JSON failure output
affects: [cli, agent-contract, ci]
tech-stack:
  added: []
  patterns: [extension-selected artifacts, error-format-only json flag]
key-files:
  created:
    - packages/cora/tests/cli/render-text.test.ts
  modified:
    - packages/cora/src/cli/commands/render.ts
key-decisions:
  - "`--format json` remains an error-output mode; successful stdout render prints text."
  - "Missing `-o` selects text output only; SVG/PNG/PDF still use explicit output paths."
patterns-established:
  - "Text render success writes artifacts or stdout without changing structured error output."
requirements-completed: [ASCII-01, ASCII-04]
duration: 16 min
completed: 2026-05-23
---

# Phase 3.4 Plan 02: CLI Text Export Summary

**Render CLI text lane for `.txt`, stdout, ASCII charset selection, and preserved JSON failure output**

## Performance

- **Duration:** 16 min
- **Started:** 2026-05-23T12:30:00Z
- **Completed:** 2026-05-23T12:34:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Made `-o` optional for `cora render`; no output path now prints text to stdout.
- Added `.txt` artifact selection and `--charset unicode|ascii`.
- Added subprocess CLI coverage for stdout, ASCII, `.txt`, structured JSON failure, and unsupported extension errors.

## Task Commits

1. **Wire .txt and stdout text rendering into render command** - `45d1366` (feat)
2. **Add CLI text export behavior tests** - `45d1366` (feat)

## Files Created/Modified

- `packages/cora/src/cli/commands/render.ts` - Text output integration and charset validation.
- `packages/cora/tests/cli/render-text.test.ts` - Built CLI behavior tests.

## Decisions Made

- Avoided rendering SVG for `.txt` or stdout text output; text consumes the same computed layouted diagram directly.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Docs and package smoke checks can document the exact CLI behavior now implemented.

---
*Phase: 3.4-ascii-diagram-export-and-skills-md-file*
*Completed: 2026-05-23*
