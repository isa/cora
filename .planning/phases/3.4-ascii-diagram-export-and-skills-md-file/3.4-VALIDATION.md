---
phase: 3.4
slug: ascii-diagram-export-and-skills-md-file
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-23
---

# Phase 3.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest + shell smoke |
| **Config file** | `packages/cora/vitest.config.ts` if present; otherwise package default |
| **Quick run command** | `cd packages/cora && bun x vitest run tests/renderer/text.test.ts tests/cli/render-text.test.ts` |
| **Full suite command** | `cd packages/cora && bun run typecheck && bun x vitest run && bun run build && bun run test:golden` plus `bash packages/cora/tests/smoke/clean-install.sh` from repo root |
| **Estimated runtime** | ~60-120 seconds |

---

## Sampling Rate

- **After every task commit:** Run the quick Vitest command for touched test files, or `bun run typecheck` when only exports/types changed.
- **After every plan wave:** Run `cd packages/cora && bun run typecheck && bun x vitest run`.
- **Before `$gsd-verify-work`:** Full suite and clean-install smoke must be green.
- **Max feedback latency:** 120 seconds.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 3.4-01-01 | 01 | 1 | ASCII-02/03 | T-3.4-01 | N/A | unit | `cd packages/cora && bun x vitest run tests/renderer/text.test.ts` | ❌ W0 | ⬜ pending |
| 3.4-01-02 | 01 | 1 | ASCII-02/03 | T-3.4-01 | N/A | unit | `cd packages/cora && bun x vitest run tests/renderer/text.test.ts` | ❌ W0 | ⬜ pending |
| 3.4-02-01 | 02 | 2 | ASCII-01/04 | T-3.4-02 | Structured JSON failures preserved | cli | `cd packages/cora && bun x vitest run tests/cli/render-text.test.ts` | ❌ W0 | ⬜ pending |
| 3.4-02-02 | 02 | 2 | ASCII-01/04 | T-3.4-02 | Structured JSON failures preserved | cli | `cd packages/cora && bun x vitest run tests/cli/render-text.test.ts` | ❌ W0 | ⬜ pending |
| 3.4-03-01 | 03 | 3 | AGT-04 | T-3.4-03 | N/A | docs | `rg "Triggers|architecture diagram|cora render" SKILL.md` | ❌ W0 | ⬜ pending |
| 3.4-03-02 | 03 | 3 | AGT-04 | T-3.4-03 | N/A | smoke | `bash packages/cora/tests/smoke/clean-install.sh` | ✅ | ⬜ pending |
| 3.4-04-01 | 04 | 4 | ASCII-01-05/AGT-04 | — | N/A | full | `cd packages/cora && bun run typecheck && bun x vitest run && bun run build && bun run test:golden` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements:

- Vitest already exists.
- CLI tests already exist under `packages/cora/tests/cli/`.
- Renderer tests already exist under `packages/cora/tests/renderer/`.
- Clean-install smoke already exists at `packages/cora/tests/smoke/clean-install.sh`.

---

## Manual-Only Verifications

All phase behaviors have automated verification.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-23
