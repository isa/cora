---
phase: 3.4-ascii-diagram-export-and-skills-md-file
plan: 03
type: execute
wave: 3
depends_on: ["3.4-02"]
files_modified:
  - SKILL.md
  - README.md
  - AGENTS.md
  - packages/cora/package.json
  - packages/cora/tests/smoke/clean-install.sh
autonomous: true
requirements:
  - ASCII-05
  - AGT-04
tags: [docs, skill, packaging, smoke, agents]
decision_refs:
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15

must_haves:
  truths:
    - "Root `SKILL.md` exists; `SKILLS.md` does not need to exist"
    - "`SKILL.md` contains trigger language for agent use"
    - "`SKILL.md` contains exactly one compact canonical YAML example"
    - "`SKILL.md` references `README.md` and `AGENTS.md` for deeper reading"
    - "Installed npm package includes `SKILL.md`"
    - "README and AGENTS document `.txt`, stdout text rendering, and ASCII fallback"
    - >-
      D-10: Create a root-level `SKILL.md`, singular and all caps.
    - >-
      D-11: `SKILL.md` is the quick operating guide for AI agents.
    - >-
      D-12: `SKILL.md` must include trigger language.
    - >-
      D-13: Keep `SKILL.md` compact with one canonical YAML example.
    - >-
      D-14: Include references to `README.md` and `AGENTS.md`.
    - >-
      D-15: Include `SKILL.md` in the npm package.
  artifacts:
    - path: "SKILL.md"
      provides: "Quick operating guide for AI agents"
      contains: "Triggers"
    - path: "packages/cora/tests/smoke/clean-install.sh"
      provides: "Installed package SKILL.md proof"
      contains: "SKILL.md"
---

<objective>
Add the root agent skill guide and package/docs updates so text export and the Cora agent workflow are discoverable after install.
</objective>

<decision_coverage>
- D-10: Creates root `SKILL.md`.
- D-11: Positions `SKILL.md` as quick guide while `AGENTS.md` remains full contract.
- D-12: Includes trigger language.
- D-13: Includes one canonical YAML example and compact workflow.
- D-14: Links to README/AGENTS for deeper reading.
- D-15: Package/smoke checks prove `SKILL.md` ships.
</decision_coverage>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-3.4-07: `SKILL.md` duplicates or contradicts `AGENTS.md` | medium | Keep it short and explicitly point to `AGENTS.md` as full contract. |
| T-3.4-08: Root skill guide is omitted from npm package | high | Update package files/build or smoke path and assert installed `node_modules/cora/SKILL.md` exists. |
| T-3.4-09: Docs imply text export is visual-equivalent to SVG/PDF | low | Document text export as simplified graph-like output with layout simplification. |
</threat_model>

<tasks>

<task type="execute" id="3.4-03-01">
  <name>Create root SKILL.md quick guide</name>
  <read_first>
    - .planning/phases/3.4-ascii-diagram-export-and-skills-md-file/3.4-CONTEXT.md
    - AGENTS.md
    - README.md
    - examples/valid/minimal.yaml
  </read_first>
  <action>
Create root `SKILL.md` with these sections:

- `# Cora Skill`
- `## Triggers` with trigger phrases including `architecture diagram`, `system diagram`, `infra diagram`, `flowchart`, `render YAML diagram`, `SVG artifact`, `PDF artifact`, and `text diagram`
- `## Use Cora When` describing agent-authored YAML diagrams and rendered artifacts
- `## Quick Workflow` with validate and render commands
- `## Canonical Example` containing one compact YAML example based on `examples/valid/minimal.yaml`
- `## Outputs` listing `.svg`, `.png`, `.pdf`, `.txt`, stdout text render, and ASCII fallback
- `## Common Fixes` covering `SCHEMA_VIOLATION`, `MISSING_EDGE_TARGET`, `UNKNOWN_SERVICE`, `MISSING_EXTENSION`, `LAYOUT_ERROR`, and `CHROMIUM_NOT_INSTALLED`
- `## Review Checklist`
- `## Further Reading` pointing to `README.md` and `AGENTS.md`

Do not create `SKILLS.md`. Keep `SKILL.md` concise and agent-facing.
  </action>
  <acceptance_criteria>
- `SKILL.md` exists at repo root.
- `SKILL.md` contains `## Triggers`.
- `SKILL.md` contains `architecture diagram`, `system diagram`, `infra diagram`, `flowchart`, `render YAML diagram`, and `text diagram`.
- `SKILL.md` contains `## Canonical Example`.
- `SKILL.md` contains `cora validate` and `cora render`.
- `SKILL.md` contains `README.md` and `AGENTS.md`.
- `SKILL.md` contains no more than one fenced YAML example block.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && test -f SKILL.md && rg "## Triggers|architecture diagram|system diagram|infra diagram|flowchart|render YAML diagram|text diagram|## Canonical Example|cora validate|cora render|README.md|AGENTS.md" SKILL.md</automated>
  </verify>
</task>

<task type="execute" id="3.4-03-02">
  <name>Document text export and package SKILL.md</name>
  <read_first>
    - README.md
    - AGENTS.md
    - SKILL.md
    - packages/cora/package.json
    - packages/cora/tests/smoke/clean-install.sh
    - packages/cora/src/cli/commands/render.ts
  </read_first>
  <action>
Update docs and package checks:

- In `README.md`, document `cora render diagram.yaml`, `cora render diagram.yaml -o diagram.txt`, and ASCII fallback via the selected charset flag. State text output is simplified graph-like terminal output, not SVG/PDF visual parity.
- In `AGENTS.md`, add text export to quick start and recommended loop. Preserve the JSON error contract explanation.
- In `packages/cora/package.json`, make sure root `SKILL.md` is included in the packed package. If package-relative inclusion cannot directly reference the repo root, add a build/copy step that places `SKILL.md` in the package artifact before `npm pack`.
- In `packages/cora/tests/smoke/clean-install.sh`, assert the installed package contains `node_modules/cora/SKILL.md` or the chosen installed package path for the skill guide.

Do not remove preview from package/docs in this phase; Phase 3.6 owns preview distribution cleanup.
  </action>
  <acceptance_criteria>
- `README.md` contains `diagram.txt`.
- `README.md` contains `cora render diagram.yaml` without `-o` in a text export example.
- `README.md` mentions text output is simplified or graph-like.
- `AGENTS.md` contains `diagram.txt`.
- `AGENTS.md` contains the stdout text render behavior.
- `packages/cora/tests/smoke/clean-install.sh` contains `SKILL.md`.
- `packages/cora/package.json` or package build scripts contain `SKILL.md` handling.
- `bash packages/cora/tests/smoke/clean-install.sh` exits 0 from repo root.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/node/cora && rg "diagram.txt|cora render diagram.yaml|simplified|graph-like" README.md && rg "diagram.txt|stdout|text" AGENTS.md && rg "SKILL.md" packages/cora/package.json packages/cora/tests/smoke/clean-install.sh && bash packages/cora/tests/smoke/clean-install.sh</automated>
  </verify>
</task>

</tasks>

<verification>
Run from the repository root:

```bash
rg "## Triggers|README.md|AGENTS.md" SKILL.md
rg "diagram.txt|text output" README.md AGENTS.md
bash packages/cora/tests/smoke/clean-install.sh
```
</verification>

<success_criteria>
- `SKILL.md` exists at root with triggers and one canonical example.
- README and AGENTS mention text export and stdout render behavior.
- Installed package includes `SKILL.md`.
- Preview package-surface cleanup is explicitly left to Phase 3.6.
</success_criteria>
