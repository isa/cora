---
phase: 3.4-ascii-diagram-export-and-skills-md-file
plan: 01
subsystem: renderer
tags: [text-export, layout-ir, unicode, ascii, vitest]
requires:
  - phase: 3.3-component-preview-canvas
    provides: stable renderer component catalog and layouted IR pipeline
provides:
  - Pure `renderToText(layouted)` renderer over `LayoutedDiagram`
  - Unicode default output and explicit ASCII fallback
  - Renderer unit coverage for all valid v1 fixture kinds
affects: [renderer, cli, docs, agent-output]
tech-stack:
  added: []
  patterns: [pure renderer over layouted IR, deterministic source assertions]
key-files:
  created:
    - packages/cora/src/renderer/renderToText.ts
    - packages/cora/tests/renderer/text.test.ts
  modified:
    - packages/cora/src/renderer/index.ts
    - packages/cora/src/index.ts
key-decisions:
  - "Text rendering consumes existing LayoutedDiagram geometry and does not parse source files."
  - "Dense text diagrams include a deterministic legend so labels remain available when grid drawing overlaps."
patterns-established:
  - "Text artifacts are generated from the same layouted IR used by SVG/PDF."
requirements-completed: [ASCII-02, ASCII-03]
duration: 18 min
completed: 2026-05-23
---

# Phase 3.4 Plan 01: Text Renderer Core Summary

**Pure layouted-IR text renderer with Unicode box drawing, ASCII fallback, deterministic all-kind tests, and label-preserving legend output**

## Performance

- **Duration:** 18 min
- **Started:** 2026-05-23T12:26:00Z
- **Completed:** 2026-05-23T12:30:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Added `renderToText(layouted, options)` with `TextCharset = 'unicode' | 'ascii'`.
- Exported text rendering from `renderer/index.ts` and the top-level package entry.
- Added unit coverage for Unicode output, ASCII output, deterministic output, no ANSI escapes, and all five v1 example kinds.

## Task Commits

1. **Add pure LayoutedDiagram text renderer** - `5161490` (feat)
2. **Add deterministic text renderer tests** - `5161490` (feat)

## Files Created/Modified

- `packages/cora/src/renderer/renderToText.ts` - Fixed-width text renderer over `LayoutedDiagram`.
- `packages/cora/tests/renderer/text.test.ts` - Renderer tests for text output behavior.
- `packages/cora/src/renderer/index.ts` - Renderer barrel export.
- `packages/cora/src/index.ts` - Top-level export.

## Decisions Made

- Added a deterministic node/group/edge legend below the grid. Dense layouts can overlap in a text grid; the legend preserves all labels without introducing a second layout engine.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Preserved labels in dense text layouts**
- **Found during:** Task 3.4-01-02
- **Issue:** The microservice fixture rendered without throwing, but dense edge/group drawing partially overwrote the `API Gateway` label in the grid.
- **Fix:** Added a deterministic legend listing node, group, and labeled edge text.
- **Files modified:** `packages/cora/src/renderer/renderToText.ts`
- **Verification:** `cd packages/cora && bun x vitest run tests/renderer/text.test.ts`
- **Committed in:** `5161490`

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** The fix improves text readability while keeping rendering pure and deterministic.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

CLI integration can call `renderToText` directly without touching SVG/PDF rendering.

---
*Phase: 3.4-ascii-diagram-export-and-skills-md-file*
*Completed: 2026-05-23*
