# Legacy tests

Archived test suite from before the renderer/theme reset. Paths and fixtures were kept intact so these can be consulted or selectively revived.

Not run in CI. Expect failures against the current codebase until new tests replace them.
