version: 1

diagram:
  kind: flowchart
  nodes:
    - id: start
      label: Start
    - id: load
      label: Load Input
    - id: check
      label: Valid?
      component: labelIcon
      provider: default
      service: help
    - id: process
      label: Process
    - id: reject
      label: Reject
      component: icon
      provider: default
      service: error
    - id: end
      label: Done
  edges:
    - from: start
      to: load
    - from: load
      to: check
    - from: check
      to: process
      label: yes
    - from: check
      to: reject
      label: no
    - from: process
      to: end
    - from: reject
      to: load
      label: retry
