{
  "name": "cora-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "packageManager": "bun@1.3.6",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "typecheck": "turbo run typecheck",
    "cora": "node packages/cora/dist/cli.js"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
